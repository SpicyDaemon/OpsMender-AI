services:
  aim-dev:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aim-dev
    volumes:
      # Mount local project into container so edits sync both ways
      - .:/app
      # Persist uv cache between rebuilds
      - uv-cache:/root/.cache/uv
    env_file:
      - .env.example
    stdin_open: true
    tty: true
    working_dir: /app
    depends_on:
      db:
        condition: service_healthy

  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: aim
      POSTGRES_PASSWORD: aim
      POSTGRES_DB: aim
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U aim"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  uv-cache:
  postgres_data:
