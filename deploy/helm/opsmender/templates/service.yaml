apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsmender.fullname" . }}
  labels:
    {{- include "opsmender.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "opsmender.selectorLabels" . | nindent 4 }}
