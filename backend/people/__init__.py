"""Sprint 56 — admin People surface.

Modules:
- ``smtp``: best-effort outbound email for invite + password-reset URLs.
- ``bootstrap``: first-install admin creation from env vars.
- ``tokens``: opaque-token mint + hash helpers shared by invites + resets.
"""
