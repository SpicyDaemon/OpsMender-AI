# Changelog

All notable changes to this project are documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added

- **v1.1 Notification Collaboration Phase C — shared callback framework and
  Microsoft Teams verified actions.** Added a platform-neutral normalized
  callback executor shared by Slack and Teams after platform cryptographic
  verification. Microsoft Teams Notification Channels now deliver Adaptive
  Cards through Graph app-only auth and may opt into Acknowledge, Resolve,
  Escalate, and Start AI Session after a Bot Framework app ID is configured.
  Teams activities require a valid Microsoft-signed JWT, durable Activity-ID
  idempotency, a verified Azure AD object-ID mapping, an active user, and
  Admin/Operator RBAC. The channel UI exposes Teams action readiness; safe
  authenticated-link fallback remains the default.

- **v1.1 Notification Collaboration Phase B — Slack verified native actions.**
  Slack Notification Channels can opt into Block Kit actions for Acknowledge,
  Resolve, Escalate, and Start AI Session once a signing secret is configured.
  Every callback is Slack-signature/timestamp verified, normalized through the
  Phase A coordinator, durably deduplicated, mapped to an active OpsMender
  identity, checked for Admin/Operator RBAC, and audited. Valid callbacks
  promote channel readiness from configured to verified; unmapped users,
  Viewers, disabled channels, invalid signatures, and replays remain
  fail-closed. Resolve cancels escalation, and the Escalate action immediately
  advances to the next configured level.

- **SMTP Email Notification Channel.** Added an outbound-only generic SMTP
  provider for cloud email services and infrastructure relays. Operators can
  configure host/port, STARTTLS or implicit TLS, trusted plain relay mode,
  optional username/password, sender address, and default recipient. SMTP does
  not expose inbound callbacks or interactive actions.

- **v1.1 Notification Collaboration Phase A — common verified-action foundation.** Added migration `w2x3y4z5a6b7` with per-channel native-action/callback readiness state, richer external identity metadata, delivery/update receipt status fields, enriched bot action audit context, and durable `native_action_invocations` idempotency records. The common verified-action coordinator now deduplicates callback action IDs, resolves only verified external identity mappings, applies existing active-user + Admin/Operator RBAC, invokes the existing incident action path, and audits verified/applied/rejected/deduplicated outcomes.

### Fixed

- **v1.1 Phase A cleanup — membership and Notification Channel configuration accuracy.** In default single-workspace mode, **Manage Users** no longer shows an empty add-existing-user dropdown sourced from the same org-scoped member list; account creation/invites stay in **People**, while the modal retains current-member visibility and removal. Explicit multi-org installs keep the existing-user assignment flow, now limited to eligible active, non-deleted users with a clear empty state. Notification Channel copy now uses **Discord Channel ID**, labels the email adapter **Mailgun Email** instead of claiming SMTP/IMAP support, and replaces “Enabled for chat workflows” with “Enable this notification channel.” Mailgun inbound webhooks now fail closed when the connector is disabled, credentials are absent, or the provider signature is missing/invalid. Native action and message-update capabilities remain disabled. Verification: backend **1634 passed, 2 skipped**; frontend **131 passed**; production build clean.

- **v1 finalization — stale 4-tier copy corrected to the shipped 3-tier AI Autonomy model.** The session-detail **Tier capability summary** (`TierCapabilitySummary.tsx`) still rendered the old four-tier ladder — it labeled **Tier 2 as "Assisted / executes safe + caution autonomously"** and showed a removed **Tier 3 "Advisory"** card, directly contradicting the actual advisory-only enforcement and the Start Session modal. It now shows the three real tiers (**0 Autonomous · 1 Approval Required · 2 Advisory Only**) with accurate allow/deny rows (deny-listed, generic-command, and irreversible-beyond-floor actions called out). Also corrected the `Sidebar.tsx` tier badge (`2 → "Advisory"`), the `.env.example` `OPSMENDER_TIER` comment, both example skills (`examples/SKILL.md`, `examples/SKILL.app-incident.md`) tier tables, and `docs/REFERENCE.md` (enforcement matrix + decision D-002, marked superseded). No behavior change — copy/UI accuracy only.

### Added

- **v1.1 follow-up recorded — MCP Skill Generator / Skill Studio.** Roadmap + `wiki/mcp-skills.md` now describe a future flow to select an MCP server, review discovered tools, classify actions per AI Autonomy Tier, add custom instructions, and have the AI generate a saveable/downloadable MCP Skill draft — with the explicit invariant that **the backend tier gate remains the execution authority** (a generated skill never relaxes the gate, deny lists, generic guardrail, or conservative defaults). **Not implemented in v1.**

### Security

- **Native notification actions remain fail-closed.** The new common coordinator cannot run unless the connector is enabled, `native_actions_enabled=true`, and `callback_status=verified`; unsupported platforms, unmapped users, Viewers, inactive/deleted users, and replayed callbacks cannot create a second mutation. Platform capabilities remain disabled in this phase, so no native buttons or public action URLs are exposed yet.

- **Role-based access enforced in the UI and verified at the API (Part 6).** A new `RouteRoleGuard` in the dashboard layout reuses the sidebar role model (`requiredRolesForPath`) so a user who navigates directly to a restricted URL sees a clean **Access denied** panel instead of admin data — not just a hidden nav item. The sidebar already hides admin/AI-config/paging-management items from Operators and limits Viewers to Dashboard + Incidents. Backend authorization is unchanged but now locked by a role-matrix test (`tests/test_role_access.py`): Operators/Viewers are rejected from admin mutations (config, SLA targets, user creation) and Viewers can't read admin surfaces (config, user list), while every role can manage its own profile/password.

### Security

- **Safety red-team fix — Environment Scans (auditor) could execute MCP tools outside the tier/skill gate.** The auditor's `execute_call` ran LLM-chosen MCP tools via a raw `call_tool`, bypassing `audited_tool_call`/`tier_check`. It is now gated read-only: an analyzer may invoke **only** tools the applicable MCP Skill classifies `safe`; writes/remediation/destructive/unknown/generic/deny-listed tools are blocked (conservative default-deny when no skill resolves). The audit-finding **remediate** path already spawns a normal gated session — its stale Tier 3 default is normalized to Tier 2. Red-team validation confirmed all other paths gated (execute node → `audited_tool_call`; rollback → Tier 0 sandbox allowlist; co-pilot chat is text-only and cannot call tools), malformed skill YAML **fails closed**, session tier is immutable mid-session (no mutation route; the gate uses the stored value), and approved actions are parameter-bound. New `tests/test_tier_safety_hardening.py` cases cover the auditor read-only gate, deny-precedence (incl. server-specific deny over global allow), generic case-variants, missing-identifier deny, malformed-YAML fail-closed, and a prompt-injection scenario (an injected `kubectl delete namespace prod` plan is blocked with no execution).
- **Safety hardening — AI cannot execute beyond tier + MCP Skill policy.** Every MCP tool call still flows through the single audited chokepoint (`audited_tool_call` → `tier_check`); the rollback path stays gated by the Tier 0 sandbox allowlist. Added enforceable policy primitives, evaluated from the skill's **structured** YAML front-matter (never natural-language prose):
  - **Generic command-tool guardrail** (`backend/tiers/generic_tools.py`): tools that run arbitrary commands (`shell`, `bash`, `run_command`, `kubectl`, `aws_cli`, `gcloud`, `az`, `terraform`, `sql`, `python`, `node`, and `run_*`/`*_exec`/`*_cli` patterns) are denied by default — **blocked at Tier 0 and Tier 2, approval-required at Tier 1** — unless a scoped wrapper is opted out with `allow_generic: true`.
  - **Deny lists** (`deny: true` on a skill operation) **always win** — blocked at every tier, over allow/`allow_generic`/classification, including Tier 0.
  - **Generalized approval routing**: `EnforcementResult.requires_approval` now drives the gate (destructive **and** generic at Tier 1 route to the approval service), instead of a hard-coded destructive check.
  - **Conservative defaults**: unknown/unclassified actions are never silently allowed (denied at every tier; Tier 1 destructive/generic require approval). Approved actions are **parameter-bound** — the action executed after approval carries exactly the approved parameters.
  Regression tests cover generic blocking/approval, deny precedence, approval routing + parameter binding, and conservative no-skill defaults. UI + `docs/wiki/mcp-skills.md` warn that generic command tools are high-risk and denied by default.

### Changed

- **AI Autonomy: 4 tiers → 3 tiers, default Advisory.** The tier system is now **Tier 0 — Autonomous**, **Tier 1 — Approval Required**, **Tier 2 — Advisory Only** (the default). The old "Tier 3 — advise-only" is removed and **remapped to Tier 2** everywhere (`normalize_tier` in `backend/tiers/enforcement.py`, applied in the session runner, config read, and session create; migration `v1w2x3y4z5a6` remaps any stored `runtime_config.tier='3'`). **Tier 2 is now advisory** — the tier gate blocks all execute-phase remediation (read-only observation still runs in the observe node, before the gate); Tier 0/1 behaviour is unchanged; unknown actions are denied at every tier (never silently allowed). `SessionCreate.tier` defaults to 2 server-side (`le=2`); config tier `le=2`. UI: Config shows **"Default AI Autonomy Tier"** with helper text + a red Tier 0 warning; the Start Session modal shows **"AI Autonomy Tier"** (default Tier 2, Tier 0 warning); the Activity tier filter drops Tier 3. AI Autonomy Tier is explicitly separate from incident priority and user role.

### Added

- **MCP Skill Studio — `New from Template`, Unassigned drafts, and Markdown download.** The sidebar **Skills** page is renamed **MCP Skills** (builder experience: *MCP Skill Studio*). Skills gain an **assignment** (`unassigned` / `global` / `server`, migration `v1w2x3y4z5a6`): *Unassigned* skills are saved drafts — editable and downloadable, but **never injected** into AI sessions and not used as the global fallback. Resolution precedence is server-specific → global fallback → none. New `GET /skills/template` returns a structured 3-tier Markdown policy (`backend/skills/template.py`) for **New from Template**, and `GET /skills/{id}/download` (plus a client-side download) exports any skill as Markdown. New guide: [`docs/wiki/mcp-skills.md`](docs/wiki/mcp-skills.md) — *skills guide the AI; the backend tier gate enforces what can actually run.*
- **Product roadmap / version-scope guardrail (`docs/ROADMAP.md`).** New authoritative document defining what belongs in v1.0.0 vs v1.1 / v1.2 / v2.0, an explicit "Native Chat Buttons Are Deferred From v1" decision record, the three separate notification concepts (Personal Routing / Notification Channels / Viewer Notifications), and the multi-agent Session Profile future direction. `PROMPT_CONTEXT.md` gains a SCOPE GUARDRAIL pointer + KEY DECISION #11 so future sessions don't expand v1 scope. Docs-only; no code/UI/API/migration change.
- **Notification Channel action/update foundation.** Added signed incident action tokens plus verified actor resolution/RBAC checks for future native acknowledge / resolve / escalate / start-AI-session callbacks. Tokens alone cannot mutate incidents. Added persistent notification receipts (`incident_notification_receipts`) so incident and AI-session lifecycle posts can store provider channel/message/thread ids; the notifier can use those receipts for in-place updates only when both platform capability and adapter support exist. Current v1 Notification Channel platforms still advertise no native interactive actions and no message edit-in-place support, so they continue to send authenticated-link fallback messages.
- **Team-scoped Notification Channels + AI session lifecycle posts.** Notification Channels now have a **Team Scope**: workspace-wide (default for existing channels) or one-or-more specific teams. Incident notification delivery for created / acknowledged / escalated / resolved is filtered deterministically by incident ownership: service team first, then active escalation-chain team, otherwise unowned incidents go only to workspace-wide channels. Incident-linked AI sessions now post lifecycle updates to matching notification channels when sessions start, complete, fail, or time out, with authenticated OpsMender links only. The Notification Channels table and modal show/edit Team Scope; Admin/Operator session-start RBAC remains enforced.
- **Escalation Chain Calendar for on-call coverage visibility.** Escalation Chains now have a read-only **Calendar** action that shows who will be contacted at each level over Today / 7D / 30D / 90D. New `GET /escalation-chains/{id}/calendar` resolves roster and user targets with the existing on-call engine, including daily/weekly/custom rotations, overnight coverage windows, disabled/empty rosters, inactive/deleted users, and admin/operator-only access. The frontend renders compact day cards with level, target, resolved responder, coverage window, status badges, and warnings; Viewers remain blocked from the Paging setup surface.
- **Notification Channels now deliver incident lifecycle updates (created / acknowledged / resolved / escalated).** Enabled channels with the `notifications` capability receive a formatted incident message — title, severity/priority, service/team, current responder (plus previous responder + escalation level on escalations), short description — and an **authenticated incident link**. Created/ack/resolved are hooked in the incidents route after commit; escalation is emitted by the escalation engine when a chain advances a level. Built on new `backend/bots/incident_card.py` + `notifier.deliver_incident_event` / `deliver_incident_text`. **No public/unauthenticated action URLs** — responders act inside OpsMender under login + RBAC.
- **Shared Notification Channel capability model.** New `backend/bots/capabilities.py` is the single source of truth for what each platform can do (`delivery` / `incident_card` / `interactive_actions` / `direct_message` / `shared_channel` / `delivery_only`), exposed per-platform (platforms API gains `label` + `capabilities`) and per-channel (`BotConnectorResponse` gains `platform_label` + `platform_capabilities`). Honesty guardrail: `interactive_actions` is **False for every platform** in v1 (no verified callback path ships yet); unknown platforms default to delivery-only. The channels UI advertises only what a platform supports — chat platforms show an **"Incident updates"** chip, delivery-only platforms (Twilio SMS, email, custom webhook) show **"Delivery-only"** and never an "Actions" chip. Twilio is displayed as **"Twilio (SMS)"**.
- **Direct admin user creation (no invite link required).** New `POST /auth/users` (admin-only) lets an admin create a local user with username, email, role, a temporary password, and active state; the user logs in immediately and can change the password later. Surfaced as a **New user** action on People → Users with a Create User modal (auto-generated copyable temp password + regenerate + active toggle). Invite links remain available; this is the non-invite path.
- **User profiles + avatars.** The `users` table gains `first_name`, `last_name`, and `avatar_color` (migration `q6r7s8t9u0v1`; all nullable). A new generated **initials avatar** (storage-free — a colored square with initials; color is operator-chosen or derived from the username) renders in the top-right menu and across the People surface foundations. `UserResponse` now exposes these fields.
- **Profile & Settings page + self-service editing.** New `/dashboard/settings/profile` page where any user edits their own first/last name, username, email, and avatar color (`PATCH /auth/me`) and changes their password (`POST /auth/me/password`, verifies the current password). The top-right user menu now shows the avatar + display name + email and a **Profile & settings** link, alongside the existing theme controls and a notification-preferences shortcut. Admins can also set first/last name from People management (`PATCH /auth/users/{id}` extended).

### Fixed

- **Incidents `?new=1` / `?test=1` deep links are now admin-gated.** Creating and firing test incidents is admin-only, and the New Incident / Fire Test Incident buttons were already hidden for Operators/Viewers — but the `?new=1` / `?test=1` deep links (used by the Activity and Approvals empty-state "Fire test incident" hints, reachable by non-admins) opened the create / fire-test modal regardless of role, so a non-admin got a modal that 403s on submit. The modal auto-open is now gated by `isAdmin` (the query param is still stripped from the URL for everyone). Regression tests added to `incidents/page.rbac.test.tsx`.
- **Invite links no longer 404.** `_build_invite_url` produced `/invite/<token>` (a path segment), but the static-export accept page is served at `/invite` and reads the token from `?token=` — so generated invite URLs hit a non-existent route. Invite URLs are now `/invite?token=<token>`, matching the page. Invite creation → acceptance now works end-to-end with local auth.
- **Reliability HTTP/HTTPS checks now run automatically.** The SLA poller defaulted to **disabled** (`OPSMENDER_SLA_POLLER_ENABLED=false`) with a 60s interval, so automatic uptime checks never fired even though manual "Test check" worked. It now defaults to **enabled at a 5-minute cadence** (`poller_enabled=True`, `poll_interval_default=300`); a fresh deployment records uptime samples automatically once the backend starts. Operators can still disable it or retune the interval via env. (`.env.example` updated.)
- **SLO objectives now store 3-decimal precision.** `slos.objective_pct` was `NUMERIC(5,4)` (max `9.9999`) and would overflow on PostgreSQL for any realistic objective — even `99.9`. Widened to `NUMERIC(6,3)` (migration `p5q6r7s8t9u0`) so `99.999` and `100.000` store and round-trip cleanly. The simplified SLO editor formats with adaptive precision so `99.999%` never rounds up to `100%`.

### Removed

- **Environment Scans removed from the v1 UI.** Environment Scans don't support the core v1 incident workflow, so they're gone from the sidebar (Observe group), command palette, and keyboard shortcuts, and the `/dashboard/scans` page is deleted. The freed keyboard shortcut `Alt+R` now jumps to Reliability. Backend auditor/scan routes are left intact (no UI exposure) to avoid churn; they can be removed in a later pass.

### Changed

- **People page reworked to the full-width Services-style layout.** Dropped the centered `max-w-6xl` wrapper for full width; both the Users and Invites tables now use the shared `DataTable` filter bar (search + multi-select checkbox filter dropdowns + a pagination row above the table) instead of inline pill filters.
- **Local auth is the default; SSO/SAML affordances hidden unless advanced auth is on.** The People **Auth method** column + filter (and its phone-layout block) render only when `advanced_auth_enabled || sso_configured || saml_configured`; a default v1 install shows no OIDC/SAML controls.
- **Reliability & SLA v1 UI/UX cleanup.** Repositioned Reliability as focused **HTTP/HTTPS uptime + SLA monitoring** ("Monitor HTTP/HTTPS uptime and SLA compliance for your services"), not a generic observability platform.
  - **Main page:** new compact summary row (total targets · up · down · avg 30-day uptime · active SLO warnings); target cards now surface the **monitored URL** (truncated, full on hover), monitor type (HTTP/HTTPS), **current status** (Up/Down/Unknown), 30-day uptime, last-check time, and active SLO count, with edit/delete.
  - **Target detail:** reworked into an uptime/SLA dashboard — header with status badge + clickable URL + **Test check** button; three top cards (Current Status · Last Check · Last 24 Hours with a strip + down-event count + downtime); **Last 7 / 30 / 365-day** summary panels each with **MTBF**; a **custom date-range** uptime + MTBF calculator; and a green/red/gray **uptime history strip** (no response-time/latency charts in v1).
  - **SLO modal simplified:** name, target % (up to 3 decimals), window preset (7/30/90/365 days + custom), enabled. Burn-rate alerting, burn threshold, and error-budget language are removed from the v1 UI (backend fields retained). SLO breaches show a **warning only** — OpsMender does not create incidents from SLO breaches in v1.
  - **Backend:** `/sla-targets` list + detail responses now include `url`, `monitor_type`, `current_status`, `last_check_at`, `uptime_30d_pct`, and `active_slo_count`; `/sla-targets/{id}/uptime` adds `24h`/`365d` windows, custom `start`/`end` range, and `mtbf_seconds` + `down_events` + a bucketed `series`; new `GET /sla-summary` org rollup. Maintenance-window copy corrected to state that suppressed time is **excluded from SLA calculations** (which the sample math already does), not merely silenced.
  - **Future work (documented, not built):** response-time/latency charts, TCP-port and health-check-endpoint monitoring, and SLO-breach → create-incident / notify-team / link-to-Service. Reliability targets may eventually link to Services so a target going down opens an incident for the linked Service.

### Documentation

- **Future direction captured: Multi-Agent Session Profiles.** New `docs/future-multi-agent-session-profiles.md` documents the long-term north star where a Session Profile may assign specialized agents (Observer / Diagnostician / Planner / Executor / Verifier / Reporter) to different incident-response phases — each potentially with its own model, prompt, MCP access, skill set, approval requirement, and memory scope. Added as **Theme 9** in `docs/V2_ROADMAP.md` (explicitly *beyond* committed v2 core, since it would evolve the LangGraph workflow shape) and as decision **D-029** + a glossary entry in `docs/REFERENCE.md`. Documentation only — **no code, schema, API, UI, or test changes**; the single-agent Session Profile remains the supported v1 behavior.

### Fixed

- **Log level now actually applies.** `OPSMENDER_LOG_LEVEL` was read into config but never wired to Python logging or uvicorn, so the backend always logged at INFO regardless of the env var, the dashboard Config setting, or a container restart. New `backend/logging_config.py::configure_logging()` sets the root + `uvicorn`/`uvicorn.access` logger levels (so the setting governs HTTP access logs — the `GET /config 200 OK` lines — too). It's applied in three places: `create_app()` from the env var at boot, lifespan startup re-applies the persisted dashboard value (new `RuntimeConfigRepo.get_global_value`) so a UI change survives restart, and `PUT /config` applies the new level **live** to the running process. Log level is process-global (single-workspace assumption). `.env`/`.env.example` log-level comment corrected to include `CRITICAL` (which the UI already offered).

### Added

- **Audit (AI action history) CSV export.** The Activity page gains a **Download CSV** button backed by a new `GET /audit/export.csv` endpoint that honors the same filters as `GET /audit`. The on-disk audit stays JSONL — nested `tool_parameters`/`result` objects survive intact there — while the export flattens those into JSON-string cells so the rest of each row reads cleanly in a spreadsheet.

### Changed

- **"Workflows" reframed as "Session Profiles" and moved out of the main nav.** The implementation-oriented "Workflows" surface (which asked operators to reorder raw LangGraph nodes) is too low-level for v1. It's renamed **Session Profiles** and removed from the AI Agent sidebar group; it's now reached from **Config → Advanced → Session Profiles** (and the Command Palette, relabeled "Advanced — Session Profiles"). Product framing leads now — "Session Profiles control how an AI incident session runs" (read-only investigation, standard assisted response, fast triage, …) — with the node-order editor kept but clearly labeled **Advanced** and reworded in terms of "phases" rather than nodes (the table column is now "Phases", the editor says "Phase order"). **No backend change** — profiles still map to `workflow_profiles` / graph node ordering under the hood, the default flow is unchanged, and existing custom profiles keep working. The route stays `/dashboard/workflows` for now (bookmarks intact).
- **Paging + Incidents filter bars use even, tight spacing.** The Incidents, Escalation Chains, Services, Rosters, and Maintenance Windows filter bars were laid out with a CSS grid whose `fr` columns stretched each dropdown and spread them apart with uneven gaps. They now use the same flex row as the MCP Servers `DataTable` filter bar — search grows to fill, filter controls keep their natural width at a single consistent `gap-3`, and the Clear / primary action group is pushed to the right edge. Affects `PagingFilterBar` (the four Paging tables) and the inline `IncidentFilterBar`; the single-select time range is pinned to a natural width so it doesn't stretch.
- **Config page spans full width.** Dropped the centered `max-w-5xl` wrapper on `/dashboard/config` so it matches the other widened AI Agent / Observe pages (no wasted side whitespace).
- **Multi-select checkbox filters across Incidents, Escalation, and Paging surfaces; Dashboard/Incidents/Approvals widened.** Dashboard, Incidents, and Approvals dropped their centered `max-w-*` wrappers for full width. The filter dropdowns on **Incidents** (Team / Source / Status / Severity), **Escalation Chains** (Team / Status), **Services** (Team / Status / Coverage / Incident state), **Rosters** (Team / Pattern / State), and **Maintenance Windows** (Status / Scope) are now multi-select checkbox popovers — **no selection = all**, **multiple = OR**. The shared `FilterDropdown` is extracted to `components/ui` and reused by the `DataTable` filter bar, the `PagingFilterBar`, and Incidents. Incidents filtering stays server-side: `GET /incidents` now accepts repeated `status`/`severity`/`source`/`team_id` params (OR via `IN`), backward compatible with single values.
- **Reliability + Activity pages widened; Activity gets the Services-style filter bar.** Reliability (list + detail) and Activity dropped their centered `max-w-6xl` wrappers so they use the full content width. The Activity (audit) table now uses the `filterBar` layout: search + multi-select checkbox dropdowns for **Type** (Pre/Post), **Tier** (0–3), and **Status** (Permitted/Blocked) — OR within a dimension, empty = all — plus the existing date-range picker and pagination.
- **AI Agent pages span full width; Workflows & Agent Teams move onto the shared table.** MCP Servers, Workflows, and Agent Teams dropped their centered `max-w-5xl` wrapper so they use the full content width like Skills/Memories (no wasted side whitespace). Workflows and Agent Teams — previously plain `<table>`s with no search/filter/pagination — are now on the shared `DataTable` with the Services-style filter bar (search + a Status filter dropdown + pagination, with the "Add …" button in the filter row).
- **AI Agent tables get the Services-style filter bar.** Skills, Memories, MCP Servers, and Models now render a single filter row — search + **multi-select checkbox filter dropdowns** (built from each table's existing `filterChips`; OR within a dimension, AND across) + the primary **New …** action on the right — with the "Showing N of M · Rows per page · pagination" summary directly below, matching the Services surface. Implemented as an opt-in `filterBar` layout on the shared `DataTable` (new `FilterDropdown` popover); existing chip-style tables are unchanged. Workflows and Agent Teams intentionally keep their current layout.
- **Notification platform expansion + staged escalation routing.** My Routing now drives delivery from the workspace's configured **Notification Channels** (any enabled connector — Telegram, Signal, WhatsApp, Slack, Discord, Teams, Mattermost, Matrix, Lark/Feishu, DingTalk, WeCom, WeChat, Email, SMS, Home Assistant, BlueBubbles, custom) instead of a hardcoded Slack/Teams/Email/SMS list — adding a channel makes it routable with no routing changes. Each priority is an **ordered escalation of up to 3 stages**: stage 1 fires immediately and each later stage fires after the prior stage's configurable **delay** (default 5 min) if the incident is still unacknowledged; **acknowledgement or resolution stops all remaining stages**. Backed by a new `notification_escalations` table + engine + scheduler tick (`backend/paging/notification_escalation.py`, Alembic `n4o5p6q7r8s9`); delivery is unified through the connector adapter `send_message` path (legacy delivery keys still work via the channel factory). The dispatcher delegates staged routing to the engine while legacy single-channel routing keeps its immediate fan-out — **existing preferences keep working with no data migration** (legacy routing is read as Stage 1). The My Routing UI gains per-priority stage rows (channel select + wait + reorder + remove, max 3); "Add Bot Connector" → **"Add Notification Channel"** and the term "Bot Connector" no longer appears in the UI; routing shows channel friendly-names (provider details stay in channel config). Documented future direction: chat-capable incident cards with Acknowledge/Resolve/Escalate/Start Session + auto-comments (not implemented).
- **Notifications → My Routing reworked into a priority-based routing editor.** The old per-channel checkbox list + 4×4 routing matrix is replaced by four priority rows (P0 Critical / P1 High / P2 Medium / P3 Low), each with a checkbox-dropdown channel multi-select (no Ctrl/Cmd), selected-channel chips, and an expand affordance for per-channel destinations. Channel options derive from the workspace's configured Notification Channels; an empty state links to the Notification Channels tab when none exist. A **Test notification** button sends a one-off test to routed channels via the new `POST /users/me/notification-preferences/test` endpoint (per-channel sent/skipped/failed; never 500s). **Quiet Hours** now supports time zone, start/end, and **days of week**, applies to **P1–P3 only**, and always lets **P0 (Critical)** page through (`backend/paging/dispatch.py` gains an explicit P0 bypass + optional `days` filter; the frontend now writes the flat `weekday_start`/`weekday_end` shape the dispatcher actually reads, fixing a latent mismatch). The **Outbound Hooks** tab is renamed **Viewer Notifications**. Routing Summary stays read-only; no editable team-level routing in v1.
- **v1 Paging & On-call UX finish — editable tables, consistent filters, checkbox/chip multi-selects, copyable intake URLs.** Every Paging table (Teams, Escalation Chains, Services, Rosters, Maintenance Windows) now has an **edit** action beside delete and shares the Services-style filter/search bar (new `PagingFilterBar`). All multi-value pickers moved off native Ctrl/Cmd `<select multiple>` to a new checkbox + chips `MultiSelect` (with an ordered variant for Preferred MCP servers and roster rotation members). **Teams** gain an edit modal with searchable people assignment. **Services** show their full copyable alert intake URL (`{public_base_url}/api/v1/intake/{token}`, falling back to `window.location.origin`), an escalation-chain picker, and an Active toggle. **Escalation Chains** are editable, label steps as **Level 1/2/3…**, and target a roster **or user** (team targets deferred to v1.1). **Rosters** are fully editable with ordered Admin/Operator rotation members and a larger calendar that surfaces the coverage window + overnight badge. **Maintenance Windows** moved onto the shared `DataTable` with edit + multi-scope (services/teams) selection. **Notifications** reorganized into four tabs — **My Routing**, a read-only **Routing Summary** (derived from services → chains → rosters → channels; editable team-level routing deferred to v1.1), **Notification Channels**, and **Outbound Hooks**. **Ingest Tokens** removed from the UI (the model + routes remain for internal service-intake token creation). `GET /config` now returns `public_base_url`; new `GET /services/{id}/escalation-chains`; escalation-step creation validates `user` targets.
- **v1 Paging & On-call readiness cleanup.** The sidebar and Paging tabs now center on Teams, Escalation Chains, Services, Rosters, Maintenance Windows, and Notifications. Services own `POST /api/v1/intake/{service_token}`, fixed P0-P3 priority, and ordered Preferred MCP servers. Priority Rules are removed from v1 UI/docs and service priority now drives incident priority; legacy backend routes remain compatibility-only. Rosters are schedule-first with Start Date, coverage windows, enabled state, and Viewer exclusion. Maintenance windows drop matching alerts at intake. Notifications consolidates operator delivery, viewer updates, quiet hours, routing by priority, and chat/session behavior; old Bot Connector / viewer-update route bookmarks redirect safely.
- **Release workflow now publishes Linux and Windows executables.** `.github/workflows/release.yml` builds PyInstaller binaries on `ubuntu-latest` and `windows-latest` for every `v*` tag, each packaged as `opsmender-<tag>-<linux-x64|windows-x64>.(tar.gz|zip)` with a matching `.sha256` checksum, and attached to the GitHub Release alongside the source tarball. macOS is skipped for now — GitHub-hosted macOS runners are slow and queue-prone; the matrix can be extended when there's a real operator ask.
- **Docker / GHCR publish removed from the release workflow** in this private mirror — binaries only. The public-repo workflow remains the place where any container image gets published. Repo permissions reduced to `contents: write` only.
- **Install UX overhauled.** README restructured into a product-first layout: what OpsMender is, why, core capabilities, how it works, then two clean install paths — **Run in development mode** (3 commands, `cp .env.example .env && docker compose up`) and **Run in production mode** (same compose file, edit `.env` for `OPSMENDER_DEPLOYMENT_MODE=production` + `OPSMENDER_JWT_SECRET` + `OPSMENDER_BOOTSTRAP_ADMIN_EMAIL/PASSWORD`). Added required-env-vars table, after-first-login checklist, health check, troubleshooting, and a dedicated *Contributing / developer workflow* section. Port 8000 is the canonical entry point everywhere; port 3000 only appears in the hot-reload contributor section.
- **`.env.example`** gains an `OPSMENDER_DEPLOYMENT_MODE` block at the top documenting dev-vs-prod startup-guard behavior; the existing `OPSMENDER_JWT_SECRET` and `OPSMENDER_BOOTSTRAP_ADMIN_*` comments now spell out which values are required in production.
- **`docker/docker-compose.yml`** documents the dev-vs-prod `.env` switch in a top-of-file header and **comments out the Postgres `5432:5432` host-port mapping by default** (was always-exposed) — uncomment for psql/pgAdmin access on dev hosts; the app port `8000:8000` stays the canonical entry.
- **Helm chart auto-generates `OPSMENDER_JWT_SECRET` on first install** (64-char `randAlphaNum`) and preserves it across `helm upgrade` via a `lookup` on the existing in-cluster Secret. Operators no longer need to run `openssl rand -hex 32` by hand. Pinning a value in `values.yaml` still works; `existingSecret.name` still wins. Chart README + key reference table updated.
- **Source archive renamed** from `ai-incident-manager-*` to `opsmender-ai-*` to match the current product name.
- **`scripts/build_binary.sh`** reports the correct output path (`dist/opsmender` on Unix, `dist/opsmender.exe` on Windows).
- **Repository, image, and author metadata refreshed** to the current `SpicyDaemon/OpsMender-AI` namespace and `ghcr.io/spicydaemon/opsmender-ai` container image across `pyproject.toml`, `LICENSE`, deployment recipes (Helm, AWS ECS, Azure Container Apps, GCP Cloud Run, OCI Container Instances), Helm chart metadata, frontend learn-more links, the wiki, and the GitHub issue-template config.
- **Removed stale `opsmenderai.com` references** from the wiki landing page.
- **README distribution section updated** to list the cross-platform release-asset names alongside Docker Compose, single-container `opsmender serve`, Helm, and the cloud recipes.
- **REFERENCE.md / PROMPT_CONTEXT.md project structure** refreshed to match the current repo layout; D-014 binary-distribution wording now mentions Linux, Windows, and macOS executables explicitly.

## [1.0.0] — 2026-05-27

**First final release.** Promotion of `v1.0.0-rc.1` after the final-release verification pass. No source code changes between `v1.0.0-rc.1` and `v1.0.0` — same backend (1406 tests passing), same frontend (9 vitest passing, build clean), same Dockerfile, same deployment recipes. The only commits between the two tags were release-readiness docs and these CHANGELOG / docs updates.

**What's in v1.0.0:** all of `[1.0.0-rc.1]` below. The product checklist is green, the simple-by-default install is the default experience, and the SSO/SAML/multi-org surfaces stay in the codebase behind explicit env-flag opt-ins.

**Downstream verification deferred to the owner environment** (not blocking the tag):

- Docker image build (`docker build -f docker/Dockerfile .`) — sandbox had no running daemon.
- GHCR push (`docker push ghcr.io/spicydaemon/opsmender-ai:v1.0.0`) — requires `GHCR_TOKEN` with `write:packages` scope.
- Cloud recipe live smoke (AWS ECS / Azure Container Apps / GCP Cloud Run / OCI Container Instances / Helm) — requires cloud credentials.

Per the final-release policy, if any of those downstream steps fails for the v1.0.0 commit, cut a `v1.0.1` patch — don't relitigate the v1.0.0 tag.

## [1.0.0-rc.1] — 2026-05-27

First release candidate for v1.0.0. All seven items on the v1 product definition checklist pass. Default install is simple-by-default (single workspace, email + admin invite, three roles); SSO / SAML / multi-org stay in the codebase behind explicit env-flag opt-ins. The release-blocker discovered during manual RC testing (Workspace Settings Manage Users / Domains crash) is fixed and locked down with regression tests.

This is **not** the final v1.0.0 tag — that waits on downstream packaging verification.

### Highlights since the last sprint cycle

**Sprint A (Incident Command Center)** — the incident detail page evolved from a passive read surface into a real command center across three sub-steps (Sprint 57 Steps 1-3, also tagged "Sprint A" in the post-review framing):
- **Sticky Incident Command Strip** at the top of `/dashboard/incidents/detail` surfaces lifecycle actions (Acknowledge / Take / Release / Start session / Resolve / Create postmortem) as the operator scrolls.
- **Right-side Incident Context Rail** consolidates severity / status / service / team / owner / escalation step / pending approvals / AI tier in one panel.
- **Unified Incident Timeline** interleaves response events, AI tool activity, and alert evidence into a single chronological feed via new `GET /incidents/{id}/timeline`.
- Sprint 63 follow-up (Sessions 161-162) cleaned up the strip's drift items (stale postmortem-placeholder JSDoc, owner UUID display → resolved `ownerLabel`, `aria-busy` + `aria-live`, unused imports), added vitest smoke tests for the action-visibility matrix, and verified responsive behavior at 320 / 375 / tablet / desktop with a repeatable Playwright verifier.

**Sprint 64 (Default-auth / single-workspace UX simplification)** — fully implements D-027 "Simple by Default, Enterprise-Ready Underneath". Default install lands on a single-workspace + email + admin-invite + three-role flow with no SSO buttons and no org switcher visible. Two new env flags gate the optional surfaces; the disjunction `advanced_auth_enabled || sso_configured || saml_configured` decides whether SSO/SAML admin surfaces render so settings never silently disappear from tenants with an already-configured provider. Backend org-scoping (D-008) untouched, SSO/SAML runtime routes work regardless of the flags. Docs split into [Auth Guide](docs/wiki/auth-guide.md) (default) and [Advanced Auth Guide](docs/wiki/advanced-auth-guide.md) (opt-in surfaces). See per-step entries below.

**Session 167 v1 RC hotfix** — Workspace Settings Manage Users / Domains crash fixed (Rules-of-Hooks violation in `UsersModal` + `DomainsModal`). 2 backend smoke tests + 4 frontend vitest tests added.

### Verification (at the RC tag)
- `uv run python -m pytest -q` → **1406 passed, 2 skipped** (full backend suite, ~10 min).
- `npx vitest run` → **9/9 passed** (frontend smoke + regression suite).
- `cd frontend && npm run build` → clean (existing `metadataBase` warning unchanged; 35 static pages).

### Known non-blocking follow-ups (deferred to v1.1+)
- **Workspace Settings single-card polish** — when `multi_org_enabled=false && orgs.length === 1`, collapse the page to a single Workspace Settings card. Tenants with multiple existing orgs keep the row list. Deliberately deferred from the Session 167 hotfix to avoid an RC-window regression.
- **eslint quality hints** — 31 errors + 51 warnings in the `react-hooks/immutability`, `react-hooks/set-state-in-effect`, `react-hooks/exhaustive-deps` families. None are crash sources; defer to a dedicated cleanup sprint.
- **Sprint 62 Phase 2** — OCI Generative AI provider, Azure AI Foundry non-OpenAI provider, explicit local-runtime presets. Deferred unless an operator asks.
- **MTTA tile family** on the Operations Dashboard — needs a clean ack timestamp on the incident row. Sprint 59 ships MTTR only.
- **Safety-class chip per tool call** on the session detail surface — needs SKILL.md schema work to expose per-tool classification. Sprint 58 follow-up.

### Fixed
- **v1 RC blocker — Workspace Settings "Manage Users" / "Domains" crashed the dashboard** (Session 167). Clicking either button on `/dashboard/organizations` triggered the Next.js global error boundary ("ERROR 500 / Something went wrong"). Root cause: classic React Rules-of-Hooks violation in `UsersModal` and `DomainsModal` — `useMemo(...)` was declared *after* the `if (!open || !org) return null` early return. The first render with `org=null` ran 9 hooks; the second render with `org=set` skipped past the early return and added a 10th `useMemo` hook, which React refuses ("Rendered more hooks than during the previous render"). The error bubbled through the modal and got caught by `global-error.tsx` which displays a hardcoded "ERROR 500" label regardless of the actual cause. Fix: hoist the `useMemo` calls above the early returns in both modals so every render path calls the same hook sequence. No behavior change beyond not crashing — modal contents, click handlers, and backend routes are unchanged. SSO / SAML modals audited at the same time; both already had the early return as their last hook position. **Tests:** 2 new backend smoke tests in `tests/test_api.py::TestOrganizationUsersAndDomainsRouteSmoke` confirming `GET /organizations/{org_id}/users` and `GET /organizations/{org_id}/domains` return the expected payload shape. 4 new frontend vitest tests in `frontend/app/dashboard/organizations/page.test.tsx`: page renders with the "Workspace Settings" label in single-workspace mode; multi-org-only affordances are hidden; clicking Manage Users does not crash; clicking Domains does not crash. Verification: `cd frontend && npm run build` → clean; `npx vitest run` → 9/9 passed; `uv run python -m pytest -q` → **1406 passed, 2 skipped** (+2 vs baseline).

### Added
- **Sprint 64 COMPLETE — Step 4 docs split** (Session 166). Sprint 64 closes. Two new wiki guides: [`docs/wiki/auth-guide.md`](docs/wiki/auth-guide.md) (default model: single workspace + email + admin invite + three roles + bootstrap admin + optional SMTP — what 95% of self-hosted installs run on) and [`docs/wiki/advanced-auth-guide.md`](docs/wiki/advanced-auth-guide.md) (opt-in surfaces: per-tenant OIDC + per-tenant SAML 2.0 + multi-tenant orgs + host-based domain isolation + both env flags + the "settings never silently disappear" rule). Trimmed `docs/wiki/admin-guide.md` §0 (Multi-tenancy) + §1 (Authentication) — replaced with a short Authentication-overview section that cross-links the two new guides; section numbering re-anchored at 1. Softened `docs/wiki/people-guide.md` §3 (Auth methods) + §8 (Multi-org behavior) to lead with the default-mode framing and cross-link advanced auth; updated §12 Related guides to point at both new pages. Root `README.md` cross-link reframed to point at all three auth tracks (Auth Guide → People Guide → Advanced Auth Guide). With this Sprint 64 (D-027 implementation) is complete. Verification: pure docs — no build / test cycle needed.
- **Sprint 64 Step 3 — Organizations page SSO/SAML row actions gated on `advanced_auth_enabled || sso_configured || saml_configured`** (Session 165). Pure frontend; no backend changes. The Organizations page now reads the three Sprint 64 Step 1 booleans from `/config` and computes the disjunction `advancedAuthUnlocked = advanced_auth_enabled || sso_configured || saml_configured`. The per-row **SSO** and **SAML** action buttons render only when `advancedAuthUnlocked` is true — so a default-install operator who never touched advanced auth sees a clean three-action row (Manage Users · Domains · Edit) instead of being nudged toward enterprise auth. Operators who explicitly set `OPSMENDER_ADVANCED_AUTH_ENABLED=true`, or tenants that already have a saved SSO/SAML provider, continue to see both buttons exactly as before. Per D-027 this is the "settings never silently disappear" rule: an already-configured provider keeps its admin surface reachable regardless of the env flag. The modals + the per-tenant runtime routes (`/auth/sso/...`, `/auth/saml/...`) are untouched — the change is pure visibility gating at the entry point. Verification: `cd frontend && npm run build` → clean.
- **Sprint 64 Step 2 — Sidebar + TopBar + Organizations page gated on `multi_org_enabled`** (Session 164). Pure frontend; no backend changes. Three surfaces now read `multi_org_enabled` from the existing `/config` payload (Sprint 64 Step 1) and adapt their copy/affordances:
  - **`Sidebar.tsx`** — refactored `NAV_GROUPS` into a `buildNavGroups(multiOrgEnabled)` factory so the Admin → "Organizations" entry renames to "Workspace Settings" in single-workspace mode. The route stays at `/dashboard/organizations` so existing deep-links and bookmarks keep working.
  - **`TopBar.tsx`** — the org switcher dropdown is now gated on `multi_org_enabled && !tenant.pinned && orgs.length > 0`. In single-workspace mode (the default) the switcher disappears entirely; host-pinned tenants continue to show their read-only badge as before.
  - **`/dashboard/organizations/page.tsx`** — page title swaps to "Workspace Settings" with copy that reads as a single-workspace surface; the "New Organization" / "Create Organization" buttons and per-row Delete button hide in single-workspace mode (deleting the sole workspace would orphan the install). Manage Users / Domains / SSO / SAML / Edit all stay visible since they remain useful for the single workspace.
  - All three surfaces default `multiOrgEnabled` to `false` while the `/config` fetch is in flight so a slow first paint never flashes the multi-org chrome. Verification: `cd frontend && npm run build` → clean (no static-page-count change); pytest skipped — no backend changes.
- **Sprint 64 Step 1 — Simple-by-default auth visibility flags surfaced on `/config`** (Session 163). First step of the simple-by-default auth UX sprint. Adds a new env-driven flag `OPSMENDER_ADVANCED_AUTH_ENABLED` (default `false`) and surfaces four booleans on the existing `GET /config` payload: `multi_org_enabled` (env), `advanced_auth_enabled` (env, new), `sso_configured` (per-tenant DB lookup, new), and `saml_configured` (per-tenant DB lookup, new). The flag is **purely a UI visibility hint** — SSO/SAML runtime routes (`/auth/sso/...`, `/auth/saml/...`) keep working regardless, and an org with an already-saved provider config still surfaces its admin settings even when the flag is off. The frontend rule (to land in Step 3) is the disjunction: show advanced auth settings when `advanced_auth_enabled || sso_configured || saml_configured`. New `PeopleConfig.advanced_auth_enabled` field; `.env.example` documents the flag next to `OPSMENDER_MULTI_ORG_ENABLED`. Frontend `ConfigResponse` type carries the three new optional booleans. Tests: 2 new in `tests/test_config_loader.py::TestConfigLoad` (default-false + env-flag-read) and 3 new in `tests/test_api.py::TestConfig` (default-payload, sso-configured-flips-on-existing-row, saml-configured-flips-on-existing-row). Verification: `cd frontend && npm run build` → clean; `uv run python -m pytest -q` → **1404 passed, 2 skipped** (+5 vs the Sprint 63 baseline of 1399 — the baseline shifted down from the Sprint 62 high of 1407 when the Sprint 63 terminology scrub deleted the branded-ingest-adapter tests).

### Planning
- **Post-review docs pass** (Session 158, no code shipped). Translated the consolidated post-review recommendations doc into the planning surface. Two new REFERENCE decisions: **D-027 — Simple by Default, Enterprise-Ready Underneath** (single workspace default; email + admin invite; SSO/SAML and multi-org kept in the codebase but hidden behind explicit opt-ins via `OPSMENDER_ADVANCED_AUTH_ENABLED` and `OPSMENDER_MULTI_ORG_ENABLED`; "Organizations" admin entry renames to "Workspace Settings" in single-org mode; three roles only — admin / operator / viewer; no separate portals; docs split into default-auth vs advanced-auth tracks) and **D-028 — Terminology Policy** (generic incident verbs are correct and stay; OpsMender-native AI vocabulary for governance surfaces; never reference competitors by name in code / comments / docs / sample data / commits; OpenMender status enum stays on Open / In progress / Resolved / Closed). Two cleanup sprints queued in TASKS: **Sprint 63 — Incident Command Strip cleanup + terminology scrub** (stale JSDoc fix, owner UUID → username label, accessibility, unused imports, smoke tests, repo-wide grep for competitor references) and **Sprint 64 — Single-workspace / default-auth UX simplification** (visibility gating only, no backend removal).

### Added
- **Sprint 62 Step 3 — GCP Vertex AI provider** (Session 160). Added `VertexAIProvider` in `backend/llm/providers.py` using `google-cloud-aiplatform` plus ADC. Discovery uses the Vertex AI Model Garden publisher-models REST surface for `google`, `anthropic`, and `meta`; invocation uses the Vertex `generateContent` REST surface. The generic `provider_meta` JSON contract now also carries Vertex `project` + `location`, and the dashboard model-config modal gained matching fields plus a **Refresh Catalog** action. CLI `opsmender config model list|set|bootstrap` gained `--project` and `--location`. Discovery results are normalized to explicit `publisher/model` IDs so partner models remain unambiguous. 15 new tests across provider / registry / CLI / API coverage. Verification: `cd frontend && npm run build` → clean; `uv run python -m pytest -q` → **1407 passed, 2 skipped**.
- **Sprint 62 Step 2 — AWS Bedrock provider** (Session 159). Added `BedrockProvider` in `backend/llm/providers.py` using `boto3.Session(...)` and the native AWS credential chain (env vars, shared credentials/config, IAM role, ECS/EKS task role). Discovery uses `bedrock.list_foundation_models(byOutputModality="TEXT")`; invocation uses `bedrock-runtime.converse(...)`. Model configs now carry a generic `provider_meta` JSON payload so Bedrock can persist `region` plus optional `profile` without provider-specific columns; this also sets up the same seam for future Vertex/OCI metadata. CLI `opsmender config model list|set|bootstrap` gained `--region` and `--profile`. The dashboard model-config modal now shows AWS Region + optional AWS Profile and adds a **Refresh Catalog** action so operators can re-query Bedrock after filling those fields. 14 new tests across provider / registry / CLI / API coverage. Verification: `cd frontend && npm run build` → clean; `uv run python -m pytest -q` → **1392 passed, 2 skipped**.
- **Sprint 62 Step 1 — OpenAI-compatible / custom base URL provider** (Session 157). New `OpenAICompatibleProvider` in `backend/llm/providers.py` covers vLLM, LM Studio, OpenRouter, Together, Groq, Fireworks, Anyscale, and most local OpenAI-shape runtimes via a single provider shape. The OpenAI SDK is reused, but the constraints differ: `base_url` is **required** (that's what distinguishes it from the plain `openai` provider) and `api_key_env_var` is **optional** (some local endpoints accept any string or none — the SDK gets a `"no-key"` placeholder when nothing's configured). `list_models` falls back to `[self.model]` if the endpoint doesn't implement `/v1/models`, so the sprint acceptance criterion "manual model entry works even when discovery fails" holds. New `openai_compatible` entry in `ProviderRegistry._SPECS` (`requires_base_url=True`, `requires_api_key=False`, `default_api_key_env_var=None`). Factory wired in `backend/llm/factory.py`. `discover_models` cache gained per-provider TTL overrides — `bedrock` / `vertex_ai` / `oci_genai` / `azure_ai_foundry` reserved at 1h, everyone else stays at the default 60s. Frontend `ConfigSections.tsx` model form marks the API Key field as `(optional)` and adjusts placeholder + helper text when the selected provider's `requires_api_key=false` ("Leave blank for endpoints that don't require auth (e.g. local LM Studio, vLLM)"). 11 new tests (6 provider-level in `tests/test_llm_providers.py::TestOpenAICompatibleProvider`: requires base_url, works without key, uses key when set, missing-env-var-raises-when-configured, list_models success, list_models fallback on error; 5 registry-level in `tests/test_llm_registry.py`: spec registered, validate-requires-base-url, validate-allows-no-key, manual-id-warns-when-allowed, TTL-overrides). Admin guide model section rewritten with a provider table and the new OpenAI-compatible flow. Verification: `cd frontend && npm run build` → clean; `uv run python -m pytest -q` → **1378 passed, 2 skipped** (+11 vs the perf-fix baseline of 1367).

### Fixed
- **Sprint 63 command-strip cleanup + responsive closeout** (Sessions 161-162). The incident detail command strip no longer shows a truncated owner UUID when someone else owns the incident; the detail page now resolves and passes a human `ownerLabel` into the strip. The stale "Create postmortem (placeholder)" wording is gone, and the strip root now exposes `aria-busy` plus `aria-live="polite"` so assistive tech can track action state changes more reliably. Added a focused Vitest smoke suite covering the action-visibility matrix (open/unassigned, assigned-to-me, assigned-to-someone-else, resolved/closed) and a repeatable Playwright-based responsive verifier (`npm run test:incident-responsive`) that seeds a long-title incident, captures `320 / 375 / 768 / 1440` screenshots, and fails on missing strip/actions or horizontal overflow. Verification: `cd frontend && npm run test -- IncidentCommandStrip` → **5 passed**; `cd frontend && npm run test:incident-responsive` → passed; `cd frontend && npm run build` → clean; `uv run python -m pytest -q` → **1399 passed, 2 skipped**.
- **`/dashboard/models` page load** (Session 156). The Models page was slow because every paint re-ran `ProviderRegistry.discover_models()` synchronously across all four providers, each making a live API call. The dominant stalls were (a) `OpenAIProvider.list_models()` hitting `api.openai.com` with the SDK's default 600s timeout × 2 retries on bad keys, and (b) `OllamaProvider.list_models()` calling `urllib.request.urlopen` with **no timeout** against `localhost:11434` — fast `ECONNREFUSED` on Windows but a full 75s TCP connect timeout on Linux behind a dropping firewall. Three changes: (1) OpenAI discovery now uses `client.with_options(timeout=2.0, max_retries=0).models.list()` so a bad key fails in 2s instead of 20 min; (2) Ollama discovery passes `timeout=2.0` to `urlopen`; (3) `ProviderRegistry.discover_models` gets a process-level 60s TTL cache keyed on the discovery params tuple so repeat page paints within the window are instant. New `clear_discovery_cache()` test-only helper plus a `use_cache=False` escape hatch for callers that genuinely need a fresh read. Test coverage: new `test_discover_models_caches_within_ttl` in `tests/test_llm_registry.py` plus an autouse fixture that clears the cache before/after each test in that module so monkeypatched providers always take effect. Net effect: cold-paint wall-clock bounded at `~2s × providers_attempted` (was unbounded on Ollama/OpenAI failure); warm-paint within 60s is essentially free.

### Added
- **Sprint 61 COMPLETE — Step 5 docs sweep** (Session 155). Closes the UX-direction Sprint E series. New operator-facing wiki page [docs/wiki/postmortem-guide.md](docs/wiki/postmortem-guide.md) covering the editor, the seven recommended sections, the Edit/Preview toggle, Save/Clear/Reset semantics, role permissions (admin+operator write / viewer read), the `GET / PUT /incidents/{id}/postmortem` REST surface, retention rules, and the memory-candidates → `/dashboard/memories` curation hand-off. `docs/wiki/README.md` index linked. `docs/wiki/operator-guide.md` gained a new "Writing Postmortems" section (#5) plus a "Keyboard quick access" section (#6) covering the `Cmd+K` / `Ctrl+K` Command Palette discovery surface; the Audit Log paragraph now points at the postmortem editor's Timeline section. README's incident-response loop diagram step 5 mentions the editor explicitly, and the feature bullet list gained both an AI-memory ↔ postmortem cross-link and a new Command Palette bullet. `docs/REFERENCE.md` gained **D-026 — Postmortem Authoring (Sprint 61)** documenting the seven locked design decisions (single markdown column, template returned from the API not hardcoded, empty PUT clears, role gate, operator-curated memory hand-off, lifetime tracks the incident, command-strip status gate is UX-only). With this the entire UX-direction series (Sprints A through E / Sprints 57-61) is complete.
- **Sprint 61 (UX direction "Sprint E") Step 4 — Postmortem authoring surface** (Session 154). The "Create postmortem" button on the Incident Command Strip stops being a placeholder toast and opens a real editor. **Backend:** new Alembic migration `k1l2m3n4o5p6_add_incident_postmortem` adds `incidents.postmortem_md` (nullable Text) and `incidents.postmortem_updated_at` (nullable DateTime); SQLAlchemy `Incident` model gains the matching `Mapped[]` columns; new `IncidentRepo.set_postmortem(...)` (treats empty/whitespace as a clear and zeroes the timestamp); new `IncidentPostmortemResponse` / `IncidentPostmortemUpdate` schemas plus a canonical `DEFAULT_POSTMORTEM_TEMPLATE` shipping the recommended sections (Summary · Impact · Timeline · Root cause · Resolution · Lessons learned · Memory candidates); new routes `GET /incidents/{id}/postmortem` (returns stored markdown + last-edit + the section template so the frontend doesn't hardcode the structure) and `PUT /incidents/{id}/postmortem` (admin/operator only; viewers get 403). **Frontend:** new route `/dashboard/incidents/postmortem?id=...` with a side-by-side editor — left pane toggles between a full-height monospace Markdown textarea (Edit) and a lightweight H2/paragraph/bullet preview (Preview); right rail lists the recommended sections with hints plus a "Reset to template" action and a memory-candidates tip; toolbar carries Save (disabled until dirty), Clear (only when a stored postmortem exists), and a back-to-incident link. New API helpers `getIncidentPostmortem` / `putIncidentPostmortem` in `frontend/lib/api.ts`; matching `IncidentPostmortemResponse` / `IncidentPostmortemUpdate` types. `IncidentCommandStrip` now navigates to the editor instead of showing the placeholder toast. **Tests:** 5 new in `tests/test_api.py::TestIncidentPostmortem` cover the template-on-fresh GET, round-trip PUT, empty-string-clears semantics, viewer-forbidden 403, and missing-incident 404. Verification: `cd frontend && npm run build` → clean (35 → 36 static pages); `uv run python -m pytest -q` → **1366 passed, 2 skipped** (+5 vs the Step 3 baseline).
- **Sprint 61 (UX direction "Sprint E") Step 3 — Loading-state polish** (Session 153). The Operations Dashboard (`/dashboard`) used to print a generic `Loading…` text label in five places — the four Attention Queue cards, the On-call coverage panel, both Service health / Noisy services panels, and the Recent activity feed — so a fresh page looked broken until data arrived. Each of those is now a **layout-matching skeleton + per-panel operational text** so the loading state preserves the eventual row geometry and tells the operator what's happening. New in-file helpers in `frontend/app/dashboard/page.tsx`: `PanelRowSkeleton` (row-shaped placeholder with leading text lines + trailing badge pill), `CoverageGridSkeleton` (3-card grid mirroring the on-call team cards), and `LoadingHint` (small status text styled as `role="status"`). Per-panel copy: `Scanning incidents…`, `Checking approval inbox…`, `Polling agent sessions…`, `Looking for failed sessions…`, `Resolving on-call rotations…`, `Loading service health…`, `Building activity feed…`. `AttentionCard` gained a required `loadingHint` prop so each of the four cards explains its own work. Verification: `cd frontend && npm run build` → clean; `uv run python -m pytest -q` → **1361 passed, 2 skipped** (pure frontend; zero regressions).
- **Sprint 61 (UX direction "Sprint E") Step 2 — Empty-state CTAs** (Session 152). Three top-of-funnel empty states now offer a one-click way into a meaningful flow instead of just linking to a wiki page. **Incidents** empty state gains a **Fire Test Incident** secondary button alongside the existing **New Incident** primary — operators landing on a fresh install can walk the full response loop without crafting a manual incident first. **Approvals** empty state (no status filter) gets a **Fire test incident** link as its action — a synthetic Tier 1 action is the fastest path to seeing the approval inbox populate. **Activity (audit)** empty state gets the same Fire-test link plus copy framing it as the way to generate the first MCP tool calls. All three use the existing `?test=1` deep link into the incidents page (the same one Sprint 61 Step 1's command palette wired up), so the fire-test modal opens on arrival. Pure frontend; no backend changes. Verification: `cd frontend && npm run build` → clean; `uv run python -m pytest -q` → **1361 passed, 2 skipped** (zero regressions).
- **Sprint 61 (UX direction "Sprint E") Step 1 — Command Palette** (Session 151). Global **Cmd+K / Ctrl+K** opens a type-to-filter command palette. New `frontend/components/CommandPalette.tsx` (~390 LOC) mounted globally inside `DashboardLayout` alongside the existing `KeyboardShortcuts`. The palette has two categories — **Navigate** (every sidebar route, ~25 items) and **Actions** (New incident · Fire test incident · Open pending approvals · Show who's on-call · Run environment scan). Keyboard model: `Cmd+K` opens (even from inside an input — the shortcut is reserved), `↑` / `↓` move the highlight, `Enter` executes, `Esc` closes, outside-click closes. Each row carries a fuzzy-match `keywords` field beyond the label so common synonyms work (e.g. "audit log" → Activity, "schedule" → Rosters). Action items use query-param deep-linking — `/dashboard/incidents?new=1` opens the create-incident modal on arrival, `?test=1` opens the fire-test modal; both params are consumed once and stripped from the URL on `router.replace` so refresh doesn't re-open the modal. The keyboard-shortcut help modal (the existing `?` overlay) now lists `Cmd K` so the palette is discoverable. Verification: `cd frontend && npm run build` → clean; `uv run python -m pytest -q` → **1361 passed, 2 skipped** (pure frontend; zero regressions).
- **Sprint 60 — Escalation chain preview timeline (UX direction "Sprint D" finish)** (Session 150). The escalation chain editor inside `/dashboard/paging/escalation-chains` now renders a **preview timeline** between the steps list and the add-step form. For each step in step-index order, the timeline shows the cumulative time elapsed (`T+0` / `T+5m` / `T+15m` / `T+1h 5m`) plus "Page {target_name} ({target_type})." An exhaustion row at the bottom shows the total cumulative timeout: "T+30m · Escalation exhausted (no further steps)." Pure presentation on top of the existing steps array — no extra fetches, no backend changes. New `fmtCumulativeTime(seconds)` helper formats short timeline labels. Sprint 49 had shipped the drag-reorder + inline timeout editor + "Where this chain is used" pill list; this commit finishes UX-direction Sprint D's remaining ask (the preview timeline). The doc's "simulation mode" is implicitly satisfied by the static preview — operators can now answer "if a P0 fires now, who gets paged and when?" without scrolling. Verification: `cd frontend && npm run build` → clean; `uv run python -m pytest -q` → **1361 passed, 2 skipped** (pure frontend; zero regressions).
- **Sprint 59 COMPLETE — Operations Dashboard (UX direction "Sprint C")** (Session 149, continued). Steps 3-6 land together, closing the sprint. The dashboard now answers every question the UX doc said it should:
  - **Step 3 — Service health + Noisy services panels.** Two side-by-side cards below the QuickStat band. *Service health* lists the top 5 services with open incidents, sorted by open count then last activity; rows show service name, team, open-count badge (critical when ≥3, high otherwise), and "Last activity {relative}." *Noisy services* lists the top 5 services by incidents created in the last 24h (per locked decision — incident count, not raw alert volume). Both panels source from the already-loaded incidents array + a new `listServices` fetch; no backend changes.
  - **Step 4 — Recent activity feed.** Full-width panel below the service cards. Per locked decision, scope is *incident lifecycle + tool blocks only* — incidents opened / resolved synthesized from the incidents array, plus blocked tool calls pulled from `/audit?permitted=false&limit=25`. Merged, sorted descending by timestamp, top 12 shown. Each row deep-links: incident events → `/dashboard/incidents/detail`, blocked tool calls → `/dashboard/sessions/detail`. Footer links to the full audit log.
  - **Step 5 — MTTR rolling windows.** Three tiles (24h / 7d / 30d) showing median time from `created_at` to `resolved`/`closed` for incidents that landed in that window. New `fmtDuration(ms)` + `medianResolveTime(incidents, windowMs)` helpers. MTTA is **deferred** — we don't store a clean ack timestamp on the incident row today; computing it would mean joining `IncidentAssignment` + chain-ack events into a derived view. Tracked as a Sprint 59 follow-up.
  - **Step 6 — Tests + docs sweep.** 3 new tests in `tests/test_api.py::TestSessions` cover the new `GET /sessions` route: returns all sessions for the org in started_at-desc order, status_filter narrows the result set, anonymous request is refused. Full suite: **1361 passed, 2 skipped** (+3 vs the Sprint 58 baseline).
- **Sprint 59 Step 2 — On-call coverage panel** (Session 149, continued). The Operations Dashboard gains a per-team on-call panel below the QuickStat band. For every team in the org, the panel shows the team name (links to `/dashboard/paging/teams`) and one row per roster with the resolved on-call user. Teams without a roster surface as a coverage gap ("No roster — coverage gap" in status-medium). Rosters with no current on-call show "Uncovered." Resolution path: dashboard loads teams + rosters + users in parallel with the Attention Queue fetches, then `Promise.all`s `resolveOnCall(rosterId)` for every roster. Misses (network errors, expired-anchor rosters) leave the entry null and render the Uncovered badge; the dashboard never blocks on a roster resolution failure. Username lookup uses the loaded users list; a miss renders the short user id so the row never collapses. Layout: 1 column on mobile, 2 on `md`, 3 on `xl`. Teams with rosters are ordered first (alphabetically); coverage-gap teams come after so they don't dominate the panel top. Verification: `cd frontend && npm run build` → clean; `uv run python -m pytest -q` → **1358 passed, 2 skipped** (pure frontend; no backend changes — `listTeams`, `listRosters`, `listUsers`, and `resolveOnCall` are pre-existing).
- **Sprint 59 (UX direction "Sprint C") Step 1 — Operations dashboard + Attention Queue** (Session 149). Net-new `/dashboard` index route replaces the legacy "redirect to /dashboard/incidents" behavior. Page answers the four questions an on-call operator opens the app to answer:
  - **Critical, open** — incidents with `severity = critical` and `status in (open, in_progress)`. Top 4 listed; "View all" deep-links to the Incidents page.
  - **Awaiting approval** — pending approval requests across all sessions. Tool name extracted best-effort from the `action.tool` JSON; "requested {relative}" timestamp. Rows deep-link to the originating session.
  - **Active AI sessions** — sessions in `active` or `awaiting_approval` status, sorted most-recent-first. Each row shows session id + status + tier + relative start time and deep-links to `/dashboard/sessions/detail`.
  - **Recent failures** — sessions in `failed` or `timed_out` status, also recent-first. Same deep-link target.
  - Each Attention Queue card carries a tone-colored border (status-critical / status-high / status-medium / status-low), a count badge, an empty-state message, and a "View all" footer link.
  - **Secondary band**: three quick-stat tiles (Open incidents · Critical severity · Resolved last 24h) that link to the Incidents page.
  - The existing `<SetupChecklist>` is rendered above the queue so fresh installs still get the Day-1 setup nudge.
  - **New backend route**: `GET /sessions` lists sessions for the active organization with optional `status_filter` query param (`active` / `awaiting_approval` / `completed` / `failed` / `timed_out`). Backed by the existing `SessionRepo.list_all` repo method which already supported status filtering.
  - **New frontend helper**: `listSessions(params)` in `lib/api.ts`.
  - **Sidebar**: gained a "Dashboard" entry at the top of the Incident Management group with the `LayoutDashboard` icon. `NavItem` type gained an `exact?: boolean` field so `/dashboard` doesn't light up the active state for every `/dashboard/*` child route; the active-state computation switches from `startsWith` to `===` when `exact` is true.
  - **Root redirect**: `/` now goes to `/dashboard` (was `/dashboard/incidents`). Post-login redirects in `app/login/page.tsx`, `app/register/page.tsx`, and `app/invite/page.tsx` updated similarly.
  - **Deferred**: On-call coverage panel, Service health panel, Noisy services, Recent activity, MTTA / MTTR tiles — all in scope for Sprint 59 follow-up steps but not in Step 1.
  - Verification: `cd frontend && npm run build` → clean; `uv run python -m pytest -q` → **1358 passed, 2 skipped** (new GET /sessions route covered indirectly by existing SessionRepo tests; a focused API test is a follow-up).
- **Sprint 58 Steps 2+3+4 — Tier capability summary + Tool call cards + Blocked-action callouts** (Session 148, continued). Three components ship together, closing Sprint 58 (Governed AI UX):
  - **Step 2 — `TierCapabilitySummary`** (`frontend/components/sessions/TierCapabilitySummary.tsx`). Always-visible headline ("Tier N · Headline — one-liner") at the top of the session detail page; click to expand into a four-card matrix comparing all four tiers (Tier 0 Autonomous · Tier 1 Approval · Tier 2 Assisted · Tier 3 Advisory) with explicit "Allowed" + "Not allowed" rows per tier. Active tier visually highlighted with its tone color. Default-collapsed so the page stays lean; the headline alone satisfies the UX-doc commitment to "always show what the AI is allowed to do."
  - **Step 3 — `ToolCallCard`** (`frontend/components/sessions/ToolCallCard.tsx`). Replaces the flat tool-event row in the session detail page's event stream with a richer structured card. Per-event surface: tool name (mono, semibold), phase pill (Executing / Completed / Blocked), MCP server (best-effort tool-name → loaded-server-list lookup; falls back gracefully when no match), runtime (when known), inline `<View parameters>` / `<View result>` disclosure toggles for the JSON blobs, and a timestamp on the right. Border + background colors reflect the phase: accent for executing, status-low for completed, status-critical for blocked.
  - **Step 4 — Blocked-action callout** lives inside `ToolCallCard` as a dedicated `data-testid="blocked-action-callout"` block. When `phase === "blocked"` or `permitted === false`, the `block_reason` from the WS payload is surfaced in a status-critical-bordered panel with a "Blocked by tier gate" header — out of a tooltip and into the page, where operators can read it without hovering.
  - **Page wiring.** `mcpServers` state is now loaded once at the top of `/dashboard/sessions/detail` so the new card can do its lookup. The event-stream `.map()` now branches on `ev.kind === "tool"` and renders `ToolCallCard`; every other event kind keeps the existing generic-row rendering. No backend changes — the card reads `phase`, `parameters`, `result`, `permitted`, `block_reason`, and `duration_ms` that the WS event already carries from `backend/api/session_runner.py`.
  - **Deferred:** safety-class chip (safe / caution / destructive). Today that classification lives in the MCP server's SKILL.md markdown body rather than in a structured field; surfacing it cleanly per-tool would need a backend schema change. Tracked as a Sprint 58 follow-up.
  - Verification: `cd frontend && npm run build` → clean (existing `metadataBase` warning unchanged); `uv run python -m pytest -q` → **1358 passed, 2 skipped**.
- **Sprint 58 (UX direction "Sprint B") Step 1 — Session workflow-state header** (Session 148). New `frontend/components/sessions/SessionWorkflowState.tsx` renders an eight-stage pipeline at the top of `/dashboard/sessions/detail` so operators can see at a glance which LangGraph stage the agent is in. Pipeline: **Observing · Diagnosing · Planning · Waiting for approval · Executing · Verifying · Summarizing · Done**. Stages map to the existing LangGraph node names from `backend/agent/graph.py` — `recall` folds into Observing, `remember` into Summarizing, and `tier_gate` shows as a small inline "Tier check" sub-badge over the Execute pill when active (rather than as a primary state). Per-stage status: `done` (checkmark, low-status colored), `current` (accent border + pulsing icon), `pending` (muted), `skipped` (line-through, used for the Approval row when no approval was needed). `awaiting_approval` is derived from session status, not from a node event. No new backend dependency, no new fetches — pure presentation on top of the LogEvent list the session detail page already maintains from WebSocket messages. The component echoes the Tier badge on the right for at-a-glance visibility next to the workflow state. Verification: `cd frontend && npm run build` → clean; `uv run python -m pytest -q` → **1358 passed, 2 skipped** (pure frontend change).
- **Branding/theme — `BrandingProvider` scope decision** (Session 147). Per-org branding no longer pretends to override the app-wide accent or the new light/dark theme tokens. `frontend/context/branding.tsx` no longer writes dead `--primary` / `--secondary` CSS variables; instead it only applies browser chrome that is actually live today (`document.title` + favicon). The provider also now restores the product defaults (`OpsMender — OpsMender AI`, `/OpsMender-Dark.png`) when branding is absent or cleared, which fixes stale tenant title/favicon leakage across logout and org switches. Documentation now explicitly treats org colors as future tenant-surface tokens rather than global theme overrides. Verification: `cd frontend && npm run build` → clean (Next.js `metadataBase` warning unchanged); `uv run python -m pytest -q` → **1358 passed, 2 skipped**.
- **Branding/theme — light mode + toggle** (Session 146). The frontend now supports `system`, `light`, and `dark` modes. New `frontend/context/theme.tsx` persists the operator's choice in `localStorage`, resolves `system` via `prefers-color-scheme`, responds to OS theme changes, and updates the root `data-theme` + `color-scheme`. `frontend/app/layout.tsx` injects an early theme bootstrap script to avoid a pre-hydration flash, and `frontend/app/globals.css` now defines a full `[data-theme="light"]` override set for the existing semantic design tokens instead of hard-forcing dark mode. New `ThemeToggle` appears in the auth shell and the logged-in TopBar user menu. `Sidebar.tsx` and `AuthShell.tsx` also swap between the committed dark/light icon + wordmark PNGs based on the resolved theme. Verification: `cd frontend && npm run build` → clean (Next.js `metadataBase` warning unchanged); `uv run python -m pytest -q` → **1358 passed, 2 skipped**.
- **Branding cleanup — removed dead asset copy helper** (Session 145). Deleted `scripts/copy_brand_assets.sh`, which had no callers and still copied into the deleted `frontend/public/logo.png` and `frontend/public/og-image.png` paths from the old brand system. The current app already references the committed `OpsMender-Dark.png` square asset plus the dark/light icon and wordmark PNGs directly, so keeping the stale helper only preserved a broken workflow. Verification: `cd frontend && npm run build` → clean (Next.js `metadataBase` warning unchanged); `uv run python -m pytest -q` → **1358 passed, 2 skipped**.
- **Mobile follow-up — DataTable phone cards** (Session 144). The shared `frontend/components/ui/DataTable.tsx` primitive now renders stacked cards below `md` instead of forcing the tabular layout onto narrow viewports. Search, chip filters, date-range filters, pagination, row selection, bulk actions, row actions, and expanded rows continue to work; only the presentation changes. The primitive also gained `phoneLayout?: (row) => ReactNode` so pages can provide a custom mobile summary when the generic fallback would be too verbose. Initial custom card layouts shipped on Incidents and People (Users + Invites); all other DataTable consumers inherit the generic mobile fallback automatically. Verification: `cd frontend && npm run build` → clean (Next.js `metadataBase` warning unchanged); `uv run python -m pytest -q` → **1358 passed, 2 skipped**.
- **Mobile follow-up — operator-surface audit** (Session 143). Audited the Config-extracted routes and People detail page for phone-width layout issues. Shared fixes in `frontend/components/config/ConfigSections.tsx` reduce card/skeleton padding on narrow viewports, switch dense table shells from clipping to horizontal scroll, allow row-level action groups to wrap, and relax a few leftover fixed-width cells in Models, MCP servers, and Webhook Triggers. The extracted route wrappers (`/dashboard/config`, `/dashboard/models`, `/dashboard/mcp-servers`, `/dashboard/workflows`, `/dashboard/agent-teams`, `/dashboard/bot-connectors`, `/dashboard/webhooks`, `/dashboard/ingest-tokens`) now rely on the dashboard shell for outer spacing instead of stacking an extra inner `p-6`. People detail also drops a lingering fixed-width role editor control and stacks the delete modal footer correctly on phones. Verification: `cd frontend && npm run build` → clean (Next.js `metadataBase` warning unchanged); `uv run python -m pytest -q` → **1358 passed, 2 skipped**.
- **Mobile follow-up — phone nav drawer** (Session 142). The dashboard shell now uses a real phone navigation pattern: below `sm`, the persistent sidebar is hidden and the TopBar exposes a hamburger button that opens a full-width slide-over drawer with the same nav groups, tier indicator, and account actions as desktop. The drawer closes on outside-tap, explicit close, and route change, and body scroll is locked while it is open. Desktop collapse persistence and icon-only mode remain unchanged. Verification: `cd frontend && npm run build` → clean (Next.js `metadataBase` warning unchanged); `uv run python -m pytest -q` → **1358 passed, 2 skipped**.
- **Sprint 56 follow-up — Bulk invite import** (Session 141). `/dashboard/people` now supports a bulk-import flow on the Invites tab: admins can paste `email, role` lines, preview valid vs invalid rows before submit, and batch-create invites through the existing per-invite backend route. The result modal preserves every newly minted one-time URL exactly once, shows per-invite SMTP delivery status, and reports per-line failures without discarding the successful rows. Documentation updated in `docs/wiki/people-guide.md`. Verification: `cd frontend && npm run build` → clean (Next.js `metadataBase` warning unchanged); `uv run python -m pytest -q` → **1358 passed, 2 skipped**.
- **Sprint 56 follow-up — Resend invite from the list** (Session 140). Pending org invites can now be reissued in one click from `/dashboard/people`: the backend `POST /organizations/{org_id}/invites/{invite_id}/resend` route revokes the old token, mints a fresh invite, and returns the same one-time URL + SMTP-delivery payload shape as the original create flow. The Invites table now shows a resend action next to revoke for pending rows and reuses the existing minted-link modal with "Invite resent" copy. Added regression tests covering old-token invalidation and non-pending invite rejection. Verification: `uv run python -m pytest tests/test_invites.py -q` → **14 passed**; `uv run python -m pytest -q` → **1358 passed, 2 skipped**; `cd frontend && npm run build` → clean (Next.js `metadataBase` warning unchanged).
- **Sprint 56 follow-up — Operator-facing People guide** (Session 139). Added new wiki page `docs/wiki/people-guide.md` covering invites, password resets, auth-method badges, deactivation vs soft delete, bootstrap-admin env vars, SMTP/public-base-url settings, and multi-org behavior. Linked it from the wiki index, README, and `docs/REFERENCE.md` so the People surface now has an operator-facing runbook. Also corrected the stale SAML note in `docs/wiki/admin-guide.md` — SAML org configuration is live, not roadmap-only. Verification: `uv run python -m pytest -q` → **1356 passed, 2 skipped**.
- **Sprint 56 follow-up — Per-user auth-method badges** (Session 138). Users now carry a persisted `auth_source` value (`local`, `oidc:<org-slug>`, `saml:<org-slug>`). New Alembic migration `j0k1l2m3n4o5_add_user_auth_source.py` backfills existing rows to `local`; OIDC and SAML JIT login flows now stamp the source on both newly-created and previously-existing users at successful login time. The People list adds an **Auth method** column with filter chips, the per-user detail summary shows the same badge, and SSO/SAML badges deep-link into `/dashboard/organizations` and auto-open the relevant modal. Verification: `uv run python -m pytest -q` → **1356 passed, 2 skipped**; `cd frontend && npm run build` → clean (Next.js `metadataBase` warning unchanged).
- **Sprint 57 Step 3 — Incident command timeline** (Session 137). `/dashboard/incidents/detail` now replaces the old session-card stack with a single interleaved command feed. New backend route `GET /incidents/{id}/timeline` aggregates response events (incident open, session start/end, ownership changes, escalation-step fires, page acknowledgements, final incident status), AI tool activity (paired audit entries with safety class, tier decision, params, result, duration), and inbound alert evidence (raw `ingest_log` payloads). New frontend component `frontend/components/incidents/IncidentTimeline.tsx` renders that feed with per-item session annotations (`S1`, `S2`, ...), inline payload/result disclosure, and direct sidecar/full-session actions from timeline rows. Supporting changes: `IngestLogRepo.list_for_incident`, new timeline schemas/types, new frontend API helper `getIncidentTimeline`, and a focused API regression test covering the mixed response/tool/evidence shape. Verification: `uv run python -m pytest -q` → **1355 passed, 2 skipped**; `cd frontend && npm run build` → clean (Next.js `metadataBase` warning unchanged).
- **Sprint 57 Step 2 — Incident Context Rail** (Session 136). New `frontend/components/incidents/IncidentContextRail.tsx` replaces the previous "Context" + "Suggested Next Step" cards on the incident detail page. Rail rows: Status, Severity, Service (deep-linked to `/dashboard/paging/services`), Team, Current owner, Escalation step (resolved from chain state — `Step N · status`), Pending approvals (count across all incident sessions, with high-priority badge when non-zero), AI tier (from the most recent active/awaiting_approval session, else "No session yet"). The rail fetches its own supplemental data (services, teams, users, chain state, approvals) so the parent detail page doesn't grow more orchestration. `listApprovals` API helper extended to accept a `session_id` filter (the backend already supported it). Cleaned up: the redundant "Start Session" button inside the old Suggested-Next-Step card (third instance on the page) is gone. **On-call resolution is deferred** to Step 3 — the paging panel doesn't currently carry the team's roster id, and resolving "who's on call right now" needs an extra round-trip; the escalation-step row covers most of that need today. Verification: `cd frontend && npm run build` → clean (32 static pages); `uv run python -m pytest -q` → **1354 passed, 2 skipped**.
- **Sprint 57 (UX direction "Sprint A") Step 1 — Incident Command Strip** (Session 136). The incident detail page now leads with a sticky command strip at the top of the viewport: status badge + severity badge + truncated title on the left, lifecycle action buttons on the right (Acknowledge / Take or Release / Start session / Resolve / Create postmortem). Visibility is driven by `incident.status` + `pagingPanel.assignment` so the operator never sees an action that would no-op (e.g. Acknowledge disappears once the incident is in-progress; Take flips to Release when the current operator already owns the incident; Resolve flips to "Create postmortem" once the incident lands in `resolved` / `closed`). The strip stays visible during scroll via `position: sticky` + backdrop blur. New component at `frontend/components/incidents/IncidentCommandStrip.tsx`. The page's `load()` was hoisted into a `useCallback reload` so the strip can re-fetch incident + sessions + paging panel after each action. The duplicated "Start Session" button inside the big header card was removed; the strip is now the single point of entry. **No competitor-derived vocabulary anywhere in the surface** — status labels stay on our own enum (Open / In progress / Resolved / Closed). "Create postmortem" is a placeholder toast until the postmortem surface lands in a follow-up step. Verification: `cd frontend && npm run build` → clean; `uv run python -m pytest -q` → **1354 passed, 2 skipped** (pure frontend change).

### Changed
- **Removed competitor-derived vocabulary from status labels and historical docs** (Session 136). The status filter chip labels on `/dashboard/incidents` no longer carry parenthesized bridge suffixes — chips now read just Open / In progress / Resolved / Closed. Past CHANGELOG / CURRENT_STATE / TASKS / LOGS entries that framed Sprint 55 around migration shorthand have been rewritten on the page where they're visible to neutral language.

- **Sprint 56 close-out — full-lifecycle smoke test + live verification** (Session 135). New `tests/test_people_e2e.py::test_people_full_lifecycle` walks the entire Sprint 56 surface against the real FastAPI app in one continuous scenario (13 assertions across admin bootstrap → invite mint → public validate → public accept → teammate login → password reset mint → public consume → old-pw-fails + new-pw-works + replay-rejected → blocked-delete-while-active → deactivate → login-refused → preconditions report → soft-delete → username-preserved + email-scrubbed + PATCH-on-deleted-returns-404 → listing-shows-deleted-row → self-delete-blocked). **Suite: 1354 passed, 2 skipped** (+1 e2e on top of the 40 unit tests already covering each step in isolation). Also live-smoked the running dev server: `curl` against `POST /organizations/{id}/invites` → `GET /invites/{token}` → `POST /invites/{token}/accept` works end-to-end, the FastAPI static mount serves `/dashboard/people` + `/invite` at 200, and the `UserResponse.deleted_at` field flows correctly through the JSON serializer.
- **Sprint 56 Steps 5-8 — People UI (list + per-user + accept + sidebar)** (Session 135). Closes Sprint 56. Three new frontend routes + a sidebar entry, all wired into the Step 1-4 backend.
  - **`/dashboard/people`** — admin-only list page with two tabs: **Users** and **Invites**. Both use the shared `<DataTable>` for consistency with the rest of the dashboard (search, sort, multi-select chip filters, date-range, persisted preferences). Users tab: columns Username · Role · Status · Joined with role + active/inactive chips and a per-row "Manage" link to the detail page. Invites tab: columns Email · Role · Status · Expires · Sent with role + status chips (pending/accepted/expired/revoked), a toolbar **New invite** button, and a row-level Revoke action that's only shown for pending invites. A counter badge next to each tab pill shows total users + pending invites at a glance.
  - **New-invite flow.** The "New invite" modal takes `email` + `role`, calls `POST /organizations/{org_id}/invites`, and on success opens a follow-up "Invite created" modal that surfaces the one-time URL with a Copy button and reports SMTP delivery status (✓ Email also sent / ✗ Email failed (URL still works) / SMTP not configured). The URL is shown exactly once — refreshing the list does not re-mint.
  - **`/dashboard/people/detail?id=…`** — admin-only per-user page. Summary card (Username · Email · Role · Status · Joined · User ID). Role editor (Save button disabled until the dropdown changes; disabled entirely when editing yourself). Activate / Deactivate toggle (disabled when editing yourself; copy explains "Deactivated users cannot log in. Required before deletion."). Password section with a "Send password reset" button that mints a 24-hour URL and renders the same Copy + SMTP-status modal as the invite flow. Danger zone with a "Delete user" button that opens a preconditions modal: walks the operator through "Account is deactivated" + "Removed from all rosters" checks (calls `GET /auth/users/{id}/delete-preconditions`); the final Delete button stays disabled until both pass. Deleted users (and 404s) render the dedicated **"User no longer in OpsMender"** placeholder per owner direction.
  - **`/invite?token=…`** — public invite-accept page. Validates the token on mount via `GET /invites/{token}`, renders the `org_name + role` heading, pre-fills a suggested username from the email local part, and on submit accepts the invite + sets the returned JWT + redirects to `/dashboard/incidents`. Invalid/expired/used tokens render a friendly "Invite no longer valid — ask the admin to send a fresh link" message with a sign-in link.
  - **`useSearchParams` Suspense wrap.** Both the People detail page and the invite accept page wrap their content in a `<Suspense>` boundary so Next.js 16 static export doesn't bail out (matches the pattern already used by `/dashboard/incidents/detail`).
  - **Sidebar: `Admin → People`** added between Organizations and Config with the `UserCog` lucide icon. Role-gated to admin (matches Organizations).
  - **`GET /auth/users/{id}` backend route** added (admin/operator) so the detail page can fetch a single user. Returns soft-deleted users too — the UI renders the deleted-state placeholder when `deleted_at` is set.
  - **Frontend `lib/api.ts`** gained `getUser(id)`. The rest of the People-related API helpers (`updateUser`, `getUserDeletePreconditions`, `softDeleteUser`, `mintPasswordReset`, `createInvite`, `listInvites`, `revokeInvite`, `getInviteByToken`, `acceptInvite`) shipped in Steps 3-4 are wired by these pages.
  - **Auth-method badge deferred.** The original Step 4 spec called for a per-user "local / oidc:slug / saml:slug" badge linking out to Organizations → SSO config. Implementing it cleanly needs an `auth_source` column on `users` populated by the JIT-provisioning paths in `backend/api/routes/{sso,saml}.py`. Deferring to a Sprint 56 follow-up since the user-management surface is functional without it.
  - **Verified.** `cd frontend && npm run build` → clean (**35 static pages**, up from 32: +`/dashboard/people`, +`/dashboard/people/detail`, +`/invite`). `uv run python -m pytest -q` → **1353 passed, 2 skipped** (no new test failures; the new GET route is covered indirectly by the UI tests landing later if needed).
- **Sprint 56 Step 4 — Invite CRUD + public accept** (Session 135). New `backend/api/routes/invites.py` exposes two routers:
  - **Admin** (mounted under `/organizations`, all admin-only): `POST /organizations/{org_id}/invites` mints a 7-day URL-safe token (sha256-hashed at rest), persists the invite, fires best-effort SMTP using the same delivery path as password resets, and returns `{invite, url, email_sent, email_error}`. Refuses (409) when the email already maps to an active member of the org. `GET /organizations/{org_id}/invites` lists every invite for the org with a derived `status` field (`pending` / `accepted` / `expired` / `revoked`). `POST /organizations/{org_id}/invites/{invite_id}/revoke` is idempotent — re-revoking a revoked invite returns the same row.
  - **Public** (mounted under `/invites`, no auth): `GET /invites/{token}` validates without consuming and returns a deliberately limited payload (`email`, `role`, `org_name`, `expires_at`) so the accept page can render without leaking internal IDs. `POST /invites/{token}/accept` creates the user, links them to the inviting org with the role recorded on the invite, marks the invite accepted, and returns a freshly-minted JWT so the recipient drops directly into the dashboard.
  - **Token lifecycle**: 7-day TTL (vs. 24 hours for password resets — invites travel through email queues and people don't click immediately). Single-use; revoked / accepted / expired tokens all return the same generic 400 "Invite is invalid or no longer active" on accept attempts to avoid enumeration.
  - **New `OrgInviteRepo`** (`create`, `get_by_hash`, `get_by_id`, `list_for_org`, `mark_accepted`, `mark_revoked`). All persistence uses the hashed token; the raw value lives only in the admin's response + the email body.
  - **New schemas**: `InviteCreateRequest`, `InviteResponse` (includes derived `status`), `InviteListResponse`, `InviteCreatedResponse`, `InvitePublicResponse`, `InviteAcceptRequest`.
  - **Frontend `lib/api.ts`** ships matching helpers (`createInvite`, `listInvites`, `revokeInvite`, `getInviteByToken`, `acceptInvite`) and the corresponding TypeScript interfaces — ready for the Step 5+7 UI to wire directly.
  - **12 new tests** in `tests/test_invites.py` cover create happy path, admin-only enforcement (operator gets 403), reject-when-already-member, list with mixed statuses, revoke + revoke-idempotency + revoke-404, public GET returns the safe-fields-only payload, public GET on invalid token, accept happy path (creates user + binds to org + mints JWT), accept-is-single-use, accept-rejects-revoked-token, accept-rejects-duplicate-username.
  - **Verified.** `uv run python -m pytest -q` → **1353 passed, 2 skipped** (+12). `cd frontend && npm run build` → clean.
- **Sprint 56 Step 3 — User admin routes + password reset backend** (Session 135). Admin-only backend surface for the People page that lands in Steps 5+6. New routes on `backend/api/routes/auth.py`:
  - `PATCH /auth/users/{id}` — change `role` (admin/operator/viewer) and/or `is_active` (admin only). Returns 400 if neither field is supplied; 404 if the user doesn't exist or has been soft-deleted.
  - `GET /auth/users/{id}/delete-preconditions` — surfaces whether the user can be deleted (returns `is_active`, `roster_memberships` count, and a derived `can_delete` boolean). The People page modal will call this before showing the delete confirmation.
  - `POST /auth/users/{id}/soft-delete` — admin only. Refuses if the target is the calling admin (400), active (409), or has any roster memberships (409). On success sets `users.deleted_at`, scrubs `email` to `deleted-{uuid}@deleted.opsmender.local` (frees the unique constraint for future re-invites of the same address), clears `password_hash`. The historical `username` is preserved per owner direction so past incidents continue to render the original name.
  - `POST /auth/users/{id}/reset-password` — admin mints a one-time reset URL bound to a sha256-hashed token (24-hour TTL). Returns `{url, expires_at, email_sent, email_error}`. The URL is the source of truth; SMTP delivery is best-effort and uses `OPSMENDER_PUBLIC_BASE_URL` when set (falls back to the request's derived base URL otherwise).
  - `POST /auth/password-reset/{token}` — public consume endpoint. Recipient POSTs `{password}`. 400 on any failure (invalid / expired / used / user unavailable). Token is single-use; replay returns 400. Returns 204 on success.
  - Defense in depth on `POST /auth/login`: soft-deleted users (with empty `password_hash` after scrubbing) short-circuit before `verify_password` runs, returning the same generic 401 to avoid enumeration.
  - New helpers: `UserRepo.update_fields`, `UserRepo.count_roster_memberships`, `UserRepo.soft_delete`; new `PasswordResetTokenRepo` (`create`, `get_by_hash`, `mark_used`).
  - New schemas: `UserUpdateRequest`, `PasswordResetMintResponse`, `PasswordResetConsumeRequest`, `SoftDeletePreconditions`. `UserResponse` gained optional `deleted_at`.
  - Frontend `lib/api.ts` ships matching helpers (`updateUser`, `getUserDeletePreconditions`, `softDeleteUser`, `mintPasswordReset`, `consumePasswordReset`) so the Steps 5+6 pages can land directly.
  - **13 new tests** in `tests/test_user_admin_routes.py` cover role/active patch happy paths, admin-only enforcement, missing-field 400, missing-user 404, mint → consume round-trip (with old-password-fails + new-password-works + replay-rejection assertions), invalid-token rejection, mint requires admin, soft-delete refuses active users + self-delete + users-on-rosters, and the precondition endpoint reporting `can_delete=true/false` correctly.
  - **Verified.** `uv run python -m pytest -q` → **1341 passed, 2 skipped** (+13). `cd frontend && npm run build` → clean.
- **Sprint 56 Step 2 — Close self-registration in production** (Session 135). Anonymous `POST /auth/register` now returns **403 Forbidden** in production once any user exists. Self-registration stays open in two cases: (a) deployment mode is `development` — covers test fixtures + local dev — or (b) the users table is empty, so a fresh production install can still bootstrap a first admin via `/register` when the env-var bootstrap path isn't used. The decision is made by a new `_self_registration_open(db)` helper on `backend/api/routes/auth.py` and exposed publicly via `GET /auth/registration-open` (returns `{ "open": true | false }`). The login page calls this on mount and hides the "Register" link — replacing it with "Self-signup is closed. Ask an admin for an invite." — when registration is closed. The `/register` route page itself is unchanged; submitting against a closed system returns the existing 403 with a clear error message. `tests/conftest.py` defaults `OPSMENDER_DEPLOYMENT_MODE=development` so every fixture that hits `/auth/register` keeps working. 4 new tests in `tests/test_registration_close.py` cover dev-mode-always-open, prod-fresh-install-allows-first-user, prod-closes-after-first-user, and the public-no-auth contract of the new endpoint. Full suite **1328 passed, 2 skipped** (+4); frontend build clean.
- **Sprint 56 Step 1 — People surface schema + bootstrap admin + SMTP helper + multi-org flag** (Session 135). First commit of Sprint 56. Plumbing only — no UI surface yet; routes + frontend land in subsequent steps. Specifically:
  - **Schema** (Alembic `i9j0k1l2m3n4`): new `org_invites` table (id, org_id, email, role, token_hash, invited_by_user_id, expires_at, accepted_at, accepted_by_user_id, revoked_at), new `password_reset_tokens` table (id, user_id, token_hash, expires_at, used_at, issued_by_user_id), `users.deleted_at` column (nullable, sets the soft-delete marker per owner direction so past incidents preserve `username` while a delete-aware UI can render a "user no longer in OpsMender" page).
  - **Bootstrap admin**: when `OPSMENDER_BOOTSTRAP_ADMIN_EMAIL` + `OPSMENDER_BOOTSTRAP_ADMIN_PASSWORD` are set AND `users` is empty at API startup, `backend/people/bootstrap.py::bootstrap_admin` creates a default org (`slug=main`) if none exists, then mints the admin user bound to it. Idempotent — re-running with non-empty users is a no-op. Wired into the API lifespan immediately after `set_session_factory`.
  - **Multi-org flag** (`OPSMENDER_MULTI_ORG_ENABLED`, default `false`): per owner direction, single-org is the simple-by-default path. The flag is surfaced to the UI via a new `multi_org_enabled` field on `GET /config`'s `ConfigResponse`. The multi-tenant schema + per-tenant SSO/SAML stays intact; the flag only gates UI affordances (TopBar org switcher, invite-create org picker) — those land in Steps 5+.
  - **SMTP helper** (`backend/people/smtp.py`): stdlib `smtplib`-backed best-effort outbound. `is_configured(cfg)` + `send_email(cfg, to, subject, body) -> (sent, error)`. Never raises; failures log a warning. `OPSMENDER_SMTP_HOST/PORT/USER/PASSWORD/FROM/USE_TLS` env vars. New `smtp_configured` field on `ConfigResponse` so the People UI knows whether to advertise "email sent" alongside the copy-paste URL.
  - **Token helper** (`backend/people/tokens.py`): mints a URL-safe 256-bit token and returns `(raw, sha256_hex)`. Only the hash is persisted. Shared by invites + password resets.
  - **New env vars documented in `.env.example`**: `OPSMENDER_BOOTSTRAP_ADMIN_EMAIL`, `OPSMENDER_BOOTSTRAP_ADMIN_PASSWORD`, `OPSMENDER_MULTI_ORG_ENABLED=false`, `OPSMENDER_PUBLIC_BASE_URL` (used to build invite URLs), `OPSMENDER_SMTP_HOST/PORT/USER/PASSWORD/FROM`, `OPSMENDER_SMTP_USE_TLS=true`.
  - **11 new tests** in `tests/test_people_bootstrap.py` cover token round-trip, SMTP not-configured + connection failure, bootstrap no-op (env unset / users exist), bootstrap happy path (creates org + admin), reuse-existing-org behavior, and the `_username_from_email` sanitization (lowercases, replaces non-alphanumeric runs with `-`, falls back to `admin` on degenerate input).
  - **Verified.** `uv run python -m pytest -q` → **1324 passed, 2 skipped** (+11 vs Session 135); `cd frontend && npm run build` → clean (no UI changes in this step, but the new optional `multi_org_enabled` + `smtp_configured` fields on the `ConfigResponse` TypeScript interface compile).
- **Incidents page Team column + filter** (Session 135). The Incidents dashboard now loads services + teams alongside incidents and resolves each incident's team via its `service_id → service.team_id`. New **Team** column (sortable, searchable, with a multi-select chip filter populated from the teams actually represented in the visible incident set). Status chip labels stay on our own vocabulary (Open / In progress / Resolved / Closed). Search placeholder updated to call out the new searchable surface ("Search by title, description, team, or source…"). Pure frontend; no schema or API changes — `IncidentResponse.service_id` was already on the wire from Sprint 48.
- **Teams + Priority Rules panels now use `<DataTable>`** (Session 135). Both panels had been hand-rolled `<ul>` lists with no search or sort. Now both share the rest of the dashboard's table primitive:
  - **Teams** (`/dashboard/paging/teams`): columns Team · Slug · Description · Created. Search across name + slug + description. Sort by name, slug, and created_at. Created-at date-range filter. Persisted under `opsmender:teams-table`.
  - **Priority Rules** (`/dashboard/paging/priority-rules`): columns # · Rule · Priority · Response · Match condition. Search across rule name + condition JSON. Sort by rule_index, name, priority, response. Multi-select priority chips (P0/P1/P2/P3) and response-mode chips (auto_resolve / notify / page / escalate_immediate). Persisted under `opsmender:priority-rules-table`.
  - Toolbar slot carries the "New …" button; row actions slot carries Delete.
  - With this, every operator-facing list in the Paging surface is on the shared primitive.

### Changed
- **Mobile responsiveness pass** (Session 135). Two quick wins:
  - Dashboard layout's main padding is now `p-4 sm:p-6 lg:p-8` (was a flat `p-8`). On phones the content area picks up ~24 px of horizontal room per side.
  - Sidebar auto-collapses to icon-only on first visit when `window.matchMedia("(max-width: 768px)").matches` is true. Operators can still toggle expand manually; the choice is then persisted to `opsmender:sidebar-collapsed` as usual. Returning operators with a saved preference still get whatever they chose.
- **Sprint 54 Steps 5+6 — docs sweep** (Session 134). README, `docs/REFERENCE.md`, `docs/wiki/paging-guide.md`, `docs/wiki/notification-preferences.md`, and `docs/wiki/mobile-incident-response.md` updated to reflect the new IA. Every operator-facing reference to `/dashboard/paging` now points at the specific sub-route (`/teams`, `/services`, `/rosters`, `/priority-rules`, `/escalation-chains`, `/maintenance-windows`, `/my-notifications`); every reference to `/dashboard/config?section=…` is replaced with the new lifted route (e.g. `/dashboard/models`, `/dashboard/mcp-servers`, `/dashboard/ingest-tokens`, `/dashboard/webhooks`, `/dashboard/workflows`, `/dashboard/agent-teams`, `/dashboard/bot-connectors`); the README's "Config page groups" table is rewritten as a "Sidebar groups" table that mirrors the new IA. OAuth callback documentation in `REFERENCE.md` now records the correct return URLs (`/dashboard/mcp-servers?mcp_oauth=…`, `/dashboard/bot-connectors?bot_oauth=…`). The historical Sprint 12 note in the README's roadmap section was left as-is (it accurately records where things lived at the time). Pure docs; no code touched.
- **Sprint 54 Steps 3+4 — Config sections lifted into top-level routes; Config page shrunk** (Session 134). The seven Config sections that aren't day-1 operator settings each have their own route now: `/dashboard/models`, `/dashboard/mcp-servers`, `/dashboard/workflows`, `/dashboard/agent-teams`, `/dashboard/bot-connectors`, `/dashboard/webhooks`, `/dashboard/ingest-tokens`. `/dashboard/config` shrinks to just **Runtime defaults** + **Storage & retention** (the two things that genuinely belong on a system-settings page). The ~6300-LOC implementation moved from `frontend/app/dashboard/config/page.tsx` to `frontend/components/config/ConfigSections.tsx` via `git mv` (preserves blame). 14 section components and helpers now have `export` keywords (`Section`, `ConfigCard`, `ConfigPageSkeleton`, `formatLastSeen`, `describeMCPStatus`, `TierSection`, `IngestAutoStartSection`, `ModelSection`, `MCPSection`, `BotConnectorSection`, `IngestTokenSection`, `AgentTeamProfileSection`, `WorkflowProfileSection`, `WebhookTriggerSection`, `RetentionSection`). The dropped orchestrator artifacts (`ConfigSectionId`, `ConfigGroupId`, `CONFIG_GROUPS`, `SECTION_LABELS`, `ConfigPageLinkCard`, `ConfigGroupDeepLink`, the original `ConfigPage` default export) covered ~370 lines.

  **Backend OAuth callbacks re-pointed** so the MCP + bot OAuth flows return to the right new pages: `backend/api/routes/mcp_servers.py` now redirects to `/dashboard/mcp-servers?mcp_oauth=…&detail=…` (was `/dashboard/config?…`); `backend/api/routes/bot_oauth.py` redirects to `/dashboard/bot-connectors?bot_oauth=…&detail=…`. `tests/test_bot_oauth.py::TestCallbackRoute::test_callback_writes_credentials_and_redirects` was updated to match.

  **Sidebar gains 7 new entries** across two groups: **AI Agent** grows from 2 → 6 (adds MCP Servers, Models, Workflows, Agent Teams to Skills + Memories); new top-level **Integrations** group with Bot Connectors + Webhook Triggers + Ingest Tokens. **`SetupChecklist`** deep links re-pointed: "Configure an AI model" → `/dashboard/models`, "Add an MCP server" → `/dashboard/mcp-servers`, "Create an ingest token" → `/dashboard/ingest-tokens`.

  Verification: `cd frontend && npm run build` → clean (32 static pages, up from 25); `uv run python -m pytest -q` → **1313 passed, 2 skipped** (zero regressions after the one test URL update).
- **Sprint 54 Step 2 — Paging route split** (Session 134). The `/dashboard/paging` surface is now seven independent routes — `/dashboard/paging/{teams, services, rosters, priority-rules, escalation-chains, maintenance-windows, my-notifications}`. Bare `/dashboard/paging` is now a server-component redirect to `/dashboard/paging/services` (uses `redirect()` from `next/navigation`). The previous single-page implementation (~2700 LOC, all seven tab panels in one file) moved to `frontend/components/paging/PagingShell.tsx`; each new route is a ~5-line `"use client"` wrapper that mounts `<PagingShell initialTab="<tab>" />`. `PagingShell` accepts the active tab via its `initialTab` prop and uses `useRouter().push()` for in-page tab pill clicks so the URL stays in sync. The in-page tab pill nav is preserved as a sub-nav within the section. Sidebar **Paging & On-call** group expanded from one entry to seven, one per tab, with per-item lucide icons (`Users`, `Server`, `Repeat`, `ListOrdered`, `GitBranch`, `Wrench`, `Bell`). `SetupChecklist`'s "Add a paging service" item now points at `/dashboard/paging/services` directly. Verification: `cd frontend && npm run build` → clean (25 static pages, up from 16); `uv run python -m pytest -q` → **1313 passed, 2 skipped** (pure frontend; zero regressions).
- **Sprint 54 Step 1 — sidebar IA v2 (shell)** (Session 134). First commit of Sprint 54. `frontend/components/Sidebar.tsx` group structure replaced: **Respond → Incident Management**, **Configure** split into a dedicated **Paging & On-call** group and a new **AI Agent** group (Skills, Memories), **Observe** unchanged, **Admin** unchanged. Per-group collapse persistence dropped — only sidebar width is persisted now. The legacy `opsmender:sidebar-groups-collapsed` `localStorage` key is cleared on hydrate so it doesn't sit around stale. Group headers are now plain labels, not buttons. Items inside the new groups still all point at currently-existing routes — the route extractions (Paging tab split into 7 sub-routes; Models / MCP servers / Workflows / Agent teams / Bot connectors / Webhook triggers / Ingest tokens lifted out of `/dashboard/config` into their own routes; new **Integrations** group) land in subsequent Sprint 54 commits. Verification: `cd frontend && npm run build` → clean (16 static pages); `uv run python -m pytest -q` → **1313 passed, 2 skipped** (pure frontend; zero regressions).
- **TopBar cleanup** (Session 129). Removed the always-visible Incident/Session UUID jump field from the dashboard top bar. Direct UUID navigation remains available through existing detail links and audit/activity surfaces, while the header now prioritizes tenant context, approvals, shortcuts, and user controls. Verification: `cd frontend && npm run build` → clean; full suite **1293 passed, 2 skipped**.

### Added
- **Log Level picker bumped to five levels with operator context** (Session 133). The Config → Runtime Config "Log Level" dropdown now includes Python's full standard hierarchy: **DEBUG · INFO · WARNING · ERROR · CRITICAL**. Backend `ConfigUpdate` schema validation (`backend/api/schemas.py`) widened to `^(DEBUG|INFO|WARNING|ERROR|CRITICAL)$`. Each option in the dropdown now carries a short verbosity hint inline (e.g. "INFO — normal operation + above (default)"), and selecting a level shows a longer operator-facing description below explaining what is captured, what is dropped, and the tradeoffs (DEBUG = highest verbosity / loudest logs; CRITICAL = quietest, fatal events only — troubleshooting becomes harder). New module-scope `LOG_LEVEL_DETAILS` map drives the description panel. Pure frontend wording + 1-character backend schema widening; no logic changes. Verification: `cd frontend && npm run build` → clean; full suite still green.
- **Sprint 53 — Data retention & garbage collection** (Session 132). Logs auto-prune after **90 days by default** to bound storage growth and avoid OOM / out-of-disk errors on long-running deployments. **Owner-revised defaults**: pruner enabled by default via `OPSMENDER_RETENTION_ENABLED=true` (not opt-in), all four log categories (`audit_entries` · `ingest_log` · `incident_memory_recall_log` · `bot_action_audit`) default to 90 days, operators can shorten / extend / disable per category from Config → "Storage & retention." **Memories are never auto-deleted** (D-025 + D-026) — they appear in the storage estimate panel flagged as non-prunable. Backend: new `RetentionConfig` model + Alembic `h8i9j0k1l2m3` + `RetentionConfigRepo` (with `effective_ttl_days` falling back to the 90-day default when no row exists). New `backend/retention/pruner.py` — cross-DB safe via a per-category `(model, ts_col, org_filter)` registry; `IncidentMemoryRecallLog` org-scoped via a subselect through `incident_memories` (it doesn't carry `org_id` directly); 50k row cap per category per pass to avoid lock storms; per-row failures don't abort the rest. New `RetentionScheduler` (6h poll, restart-safe via per-row `last_pruned_at`, wired into the app lifespan alongside the existing escalation + audit schedulers). REST API in `backend/api/routes/retention.py`: `GET /retention` (status + storage estimate, admin/operator), `PUT /retention` (admin, bulk per-category TTL update with validation — `ttl_days = null` disables a category), `POST /retention/run` (admin, one-shot trigger that returns the per-category outcome). New frontend "Storage & retention" group on the Config page with per-category TTL editor, row count + estimated bytes per category, "current default vs override" badge, last-pruned timestamps, inline Save + Disable buttons, three summary tiles (estimated log storage, memories rows + size, scheduler state), "Run pruner now" button + per-category outcome panel, and a clearly-flagged Memories row with a deep-link to `/dashboard/memories`. `.env.example` updated with `OPSMENDER_RETENTION_ENABLED=true` + operator-facing rationale. 20 new tests in `tests/test_retention.py` (14 unit) and `tests/test_api.py::TestRetentionAPI` (6 API). Verification: `cd frontend && npm run build` → clean (17 static pages); full suite stays green. **With this, the entire IA/UX series (Sprints 46–53) is shipped.**
- **Sprint 52 — Rosters + Chains table conversions** (Session 131). Both remaining Paging-page lists in `frontend/app/dashboard/paging/page.tsx` now run on the shared `<DataTable>`. **Rosters tab** (`RostersPanel`) replaces its bespoke `<ul>` with `<DataTable>`: columns Roster (name + Calendar icon) · Team (sortable + searchable + chip filter derived from active rosters) · Pattern (chip filter: weekly/daily/custom) · Time zone · Handoff. Per-row Calendar (opens the existing `RosterCalendarModal`) + Delete actions. "New roster" moved into the table toolbar; `<EmptyState>` still handles the zero-rosters case. Persisted under `opsmender:rosters-table`. **Chains tab** (`ChainsPanel`) replaces its bespoke `<ul>` with `<DataTable>`: columns Chain (name + ListOrdered icon) · Team (sortable + searchable + chip filter) · Description (line-clamped, searchable) · Status (active/inactive chip). Per-row Delete. The existing `<StepsEditor>` (HTML5 drag-reorder + inline timeout + Where-used pill list) now renders through the table's `expandedRow` hook — clicking the row chevron lazy-loads + displays a chain's steps via `loadStepsFor`. Replaces the old "Steps / Hide steps" toggle button. Persisted under `opsmender:chains-table`. With this, every operator-facing dashboard table — Activity · Memories · Skills · MCP Servers · Model configs · Ingest tokens · Webhook triggers · Organizations (modals) · Incidents · Services · Rosters · Chains — now runs on the same primitive. Only Sprint 53 (Data retention & GC) remains before the v1.0.0 tag. Verification: `cd frontend && npm run build` → clean (17 static pages); full suite **1293 passed, 2 skipped** (zero regressions — purely frontend).
- **Sprint 51 — DataTable rollout sweep: close-out** (Session 130). Final four conversions land in one pass. **Model configs** (`frontend/app/dashboard/config/page.tsx::ModelSection`) gets a provider chip derived from the active configs + a hidden-by-default default/alternate state chip + per-row Default/Edit/Delete actions, persisted under `opsmender:model-configs-table`. **Ingest tokens** gets search across name + provider, provider chips sourced from `listIngestProviders`, Active/Revoked status chips, Created date-range, Revoke/Delete row actions, persisted under `opsmender:ingest-tokens-table`. **Webhook triggers** gets search across name + URL, format chip filter, Active/Inactive status chips, per-row Test/Edit/Delete; the existing `WebhookTestPill` now lives inside the Status column cell so transient test state stays adjacent to the active/inactive badge. Persisted under `opsmender:webhook-triggers-table`. **Organizations members + domains modals** (`frontend/app/dashboard/organizations/page.tsx`) — `MembersModal` gets sort + admin/operator/viewer role chips; `DomainsModal` gets sort + Verified/Unverified + Primary/Alternate chips; both use smaller page-size options (10/25/50) and skip `storageKey` since they're modal-local. With this, all eight Sprint 51 rollout targets are on the shared primitive. Verification: `cd frontend && npm run build` → clean (17 static pages); backend suite still green.
- **Sprint 51 — DataTable rollout sweep: MCP Servers** (Session 128). Config → MCP Servers now uses the shared `DataTable` instead of the page-local bespoke table. Operators get consistent search, sort, transport/auth/runtime/state filter chips, created-at date-range filtering, column show/hide, persisted preferences under `opsmender:mcp-servers-table`, and shared pagination. Existing admin workflows are preserved as row actions: Test, OAuth Connect/Reconnect, Edit, and Delete, with runtime status dots and test-result pills still inline. Verification: `cd frontend && npm run build` → clean; full suite **1293 passed, 2 skipped**.
- **Sprint 51 — DataTable rollout sweep: Skills** (Session 127). `/dashboard/skills` now uses the shared `DataTable` instead of grouped card/list sections. The former global-vs-MCP grouping is now a sortable/filterable MCP server column with a Global fallback chip, while focus areas get their own searchable column. Existing admin workflows are preserved as row actions: Edit, Clone, Delete, plus the existing Import .md and New skill header actions. Table preferences persist under `opsmender:skills-table`. Verification: `cd frontend && npm run build` → clean; full suite **1293 passed, 2 skipped**.
- **Sprint 51 — DataTable rollout sweep: Memories** (Session 126). `/dashboard/memories` now uses the shared `DataTable` instead of the page-local card list, search box, and service chip row. Memory rows now get consistent search, sort, service/visibility filter chips, column show/hide, persisted preferences under `opsmender:memories-table`, and shared pagination. The summary text is preserved through the existing `expandedRow` detail pattern, while row actions keep edit, hide/unhide, delete, and thumbs-up/down feedback controls. Verification: `cd frontend && npm run build` → clean; full suite **1293 passed, 2 skipped**.
- **Sprint 51 — DataTable rollout sweep: Activity** (Session 125). `/dashboard/activity` now uses the shared `DataTable` instead of its page-local table, filters, column picker, and pagination. The table keeps the existing audit detail workflow through a new generic `expandedRow` option on `DataTable`, so activity rows still expand into the syntax-highlighted Parameters / Result JSON viewer. Activity now gets consistent search, sort, type/tier/status filter chips, timestamp date-range filtering, column show/hide, local table preference persistence under `opsmender:activity-table`, and shared pagination styling. Verification: `cd frontend && npm run build` → clean; full suite **1293 passed, 2 skipped**.
- **Sprint 50 — Incidents page polish + bulk actions** (Session 124). The shared `DataTable` now supports controlled row selection: per-row checkboxes, a page-level select-all checkbox with indeterminate state, selected-row highlighting, and a bulk-action bar that appears whenever one or more rows are selected. The Incidents dashboard now uses `DataTable` with sortable columns, generic search, status/severity/source filter chips, a "Last activity" date-range filter, column show/hide, persisted page-size/columns under `opsmender:incidents-table`, and row selection. New `POST /incidents/bulk` accepts `acknowledge`, `resolve`, and `reassign` actions across up to 200 incident ids, returns per-row success/error results instead of aborting on the first missing row, and commits successful updates as one batch. Frontend bulk actions currently expose the high-frequency actions, Acknowledge and Resolve; the backend reassign action is available for API and later UI expansion. Six new API tests in `tests/test_api.py::TestIncidentBulkActions` cover bulk resolve, bulk acknowledge, per-row not-found reporting, reassign validation, unknown-action rejection, and the 200-id cap. Verification: `cd frontend && npm run build` → clean; full suite **1293 passed, 2 skipped** (+6 vs Session 123; zero regressions).
- **Sprint 49 — Escalation chain visual editor** (Session 123). Closes the services ↔ chains ↔ schedules story visually. New backend routes: `PATCH /paging/escalation-chains/{id}/steps/{step_id}` (inline timeout / `notify_channels` edits via `EscalationStepUpdate` body; uses a `notify_channels_set` sentinel to distinguish "omit" from "set to null"), `POST /paging/escalation-chains/{id}/reorder-steps` body `{step_ids: [uuid]}` (drag-reorder; validates the id set is an exact permutation of the chain's existing steps and rejects with HTTP 400 otherwise), `GET /paging/escalation-chains/{id}/services` returns `ChainWhereUsedResponse` enriched with service + team names per row so the UI doesn't need to round-trip per service. New repo methods: `EscalationStepRepo.update_fields`, `EscalationStepRepo.reorder` (two-pass write that parks targeted rows in negative indices first to dodge the `(chain_id, step_index)` unique-index collision before assigning final indices), `ServiceEscalationChainRepo.list_for_chain`. Frontend `StepsEditor` upgraded: each step row is now HTML5-draggable with `onDragStart`/`onDragOver`/`onDrop`/`onDragEnd`; the active drag target gets an accent-bg highlight; the dragged row dims to 60% opacity; each row's timeout is now an inline numeric input that PATCHes on blur or Enter (clamped to 10–86400s); new "Where this chain is used" section renders attached services as pills with the team name appended. New `api.patch` helper alongside the existing get/post/put/del. Four new backend tests in `tests/test_escalation.py::TestEscalationAPI` (inline-patch happy + 404, reorder reindexes correctly, reorder rejects partial id set with 400, where-used returns 2 enriched rows). Verification: `cd frontend && npm run build` → clean; full suite **1287 passed, 2 skipped** (+4 vs Session 122; zero regressions).
- **Sprint 48 — DataTable primitive + Service directory table** (Session 122). New `frontend/components/ui/DataTable.tsx` (~430 LOC) is the reusable table chrome that backs every later table sprint. Generic over the row type. Features: column sort (asc/desc/none cycle on header click with `ArrowUp/ArrowDown` indicators and a faint `ArrowUpDown` placeholder for unsorted-sortable columns), per-column multi-select filter chips (active = filled accent; inactive = ghost), top-level date-range picker bound to one column via `dateRangeColumn` config (from + to `datetime-local` inputs + 7-day and 30-day shortcut buttons), column show/hide via a "Columns" dropdown menu, client-side pagination with a page-size selector (10/25/50/100), and optional `storageKey` that persists hidden-columns and page-size to `localStorage`. Optional toolbar slot (`toolbarRight`) for page-level actions like "New …". Optional row-action slot for per-row buttons. The Paging Services tab is the first consumer: converted from a vertical card list into a real sortable/filterable table with columns **Service** · **Team** · **On call now** · **Open** · **Last incident** · **Status**. On-call now resolves client-side via `resolveOnCall` per team's first roster; per-service open-incident count + last-incident timestamp aggregate from `listIncidents`. "New service" moved into the table toolbar. Backend: `IncidentResponse` schema gained `service_id: Optional[uuid.UUID]` (already on the DB model, just unsurfaced) so the per-service incident stats actually work; frontend type updated to match. Pure frontend + additive schema change; no logic changes. Verification: `cd frontend && npm run build` → clean; full suite **1283 passed, 2 skipped** (zero regressions).
- **Sprint 47 — Calendar-driven on-call schedules** (Session 121). Every roster on `/dashboard/paging` → Rosters now opens a 14-day calendar view through a new "Calendar" action. New backend `GET /paging/rosters/{id}/on-call/range?from=ISO&to=ISO&step_hours=24` resolves who is on call across a window in one bulk request; capped at 200 sample points and rejects inverted ranges (HTTP 400). The endpoint normalizes override timestamps to UTC-aware before passing them through `OnCallContext` so SQLite (which drops tzinfo on persisted aware datetimes) compares cleanly against the always-aware query cursor. New `<RosterCalendarModal>` component (`frontend/components/RosterCalendarModal.tsx`, ~360 LOC) renders a 7-column grid; each day is colored by a deterministic 10-color hash of the resolved user id so the same engineer always reads as the same color, and override days carry a "✦ override" badge. Click any day → admin/operator gets `<OverrideCreateModal>` with the day prefilled and a roster-member picker; the create submits via the existing `POST /paging/rosters/{id}/overrides`. Active overrides listed below the grid with admin/operator delete. API client extended with `resolveOnCallRange`; types extended with `OnCallRangeItem` + `OnCallRangeResponse`. 3 new backend tests in `tests/test_paging.py::TestPagingAPI` (range happy path with override carve-out, oversize-range 400, inverted-range 400). True drag-reorder of the base rotation is intentionally deferred — click-to-create-override delivers the operational value with a much smaller surface. Verification: `cd frontend && npm run build` → clean; `uv run python -m pytest -q` → **1283 passed, 2 skipped** (+3 vs Session 120; zero regressions).
- **Sprint 46 — IA reset: sidebar groupings + standing work cycle** (Session 120). First sprint in the IA/UX polish series (46-50) that now gates the v1.0.0 tag alongside Sprint 45. `frontend/components/Sidebar.tsx` reorganized from a flat list of 10 entries into four collapsible groups: **Respond** (Incidents, Approvals), **Configure** (Paging, Skills, Memories), **Observe** (Environment Scans, Reliability, Activity), **Admin** (Organizations, Config). Per-group collapse state persists to `localStorage` under `opsmender:sidebar-groups-collapsed`. When the entire sidebar is collapsed to icon-only the group headers drop and items render as a flat icon list — keeps the icon-only mode usable. New shared `renderNavLink` helper backs both render paths so styling stays consistent. The Organizations entry now uses the `Building2` lucide icon so it doesn't visually collide with Approvals (`CheckSquare`). `docs/PROMPT_CONTEXT.md` gains a new **Standing Work Cycle** section locking in the "once user says start/continue/go, do changes → full suite tests → docs sweep → commit + push without re-asking" loop so every future session follows it. `docs/TASKS.md` adds new entries for Sprints 46-50 (calendar schedules · service directory table · escalation chain editor · incidents bulk actions). Verification: `cd frontend && npm run build` → clean; full suite still **1280 passed, 2 skipped**.
- **Sprint 45 Step 8 — end-to-end memory loop tests** (Session 119). New `tests/test_e2e_memory_loop.py` covers the cross-component integration the unit tests left out. Three E2E tests that drive real `build_graph` invocations with `memory_factory` + `org_id` + `service_id` + a deterministic `_SequenceLLM` and assert the loop closes: (a) `test_remember_then_recall_across_sessions` — Session 1 produces a memory via `remember`; Session 2 on a similar incident surfaces it via `recall`, the memory block appears in the actual `observe` prompt seen by the LLM, the recall-log row is recorded, and a malformed `remember` response on Session 2 correctly skips writing without corrupting state; (b) `test_failed_workflow_does_not_write_memory` — trivial summaries trigger the `should_remember` skip path through the graph, no memory row is written, and the `remember` LLM response stays unconsumed (verified by call count); (c) `test_operator_authored_memory_surfaces_via_recall` — an operator-authored memory (via repo, mirroring the Step 6 API path) surfaces via `recall` identically to agent-authored ones and a distinct new memory is also written. Sprint 45 now Steps 1–9 done; only Step 10 (CHANGELOG sweep + Sprint 44 tag unblock) remains. Verification: focused → 3 passed; full suite → **1280 passed, 2 skipped** (+3 vs Session 118; zero regressions).
- **Sprint 45 Step 7 — operator UI for AI incident memory** (Session 118). New `/dashboard/memories` page at `frontend/app/dashboard/memories/page.tsx`: list rendered as cards, server-side `service_id` filter via `FilterChips` ("All" / "Global" / each service), client-side search across title/summary/tags, admin-only `include_hidden` checkbox, create + edit modals through a shared `MemoryEditor`, admin-only hide + delete buttons, inline thumbs-up/down counters on every memory (operator + admin), tags rendered as default badges, hidden rows muted with a "Hidden" badge. Sidebar entry "Memories" added between Skills and Activity using the lucide `Brain` icon (`frontend/components/Sidebar.tsx`). New `frontend/components/SessionMemoriesPanel.tsx` — collapsible "Memories used" section mounted on `/dashboard/sessions/detail`; lazy-fetches `GET /sessions/{id}/memories-used` only when expanded; renders each surfaced memory with surfaced-at timestamp + relevance score + inline thumbs-up/down that updates the local counter optimistically. API client functions added to `frontend/lib/api.ts` (`listMemories`, `getMemory`, `createMemory`, `updateMemory`, `deleteMemory`, `recordMemoryFeedback`, `setMemoryHidden`, `getSessionMemoriesUsed`); type definitions in `frontend/lib/types.ts` (`IncidentMemoryResponse`, `IncidentMemoryListResponse`, `IncidentMemoryCreate`, `IncidentMemoryUpdate`, `SessionMemoriesUsedItem`, `SessionMemoriesUsedResponse`). Pure frontend; no backend changes. Verification: `cd frontend && npm run build` → clean; full suite still **1277 passed, 2 skipped**.
- **Sprint 45 Step 6 — AI incident memory REST API** (Session 117). New `backend/api/routes/memories.py` ships the full operator-curation surface. `GET /memories` lists for the active org with `service_id` + `include_hidden` filters; `GET /memories/{id}` fetches a single memory; `POST /memories` lets an operator (admin or operator role) author a memory by hand (tags get lower-cased + trimmed; `service_id` validated against the active org); `PUT /memories/{id}` edits (admin or operator, with a `service_id_set` sentinel that lets the caller explicitly null the service binding vs leave it untouched); `DELETE /memories/{id}` is admin-only and returns 204; `POST /memories/{id}/feedback` records thumbs up/down (`{helpful: bool}`, admin or operator) and atomically increments the right counter; `POST /memories/{id}/hide` is admin-only and toggles `is_hidden` without deleting. New `GET /sessions/{id}/memories-used` enforces the org boundary on the session itself, then returns the per-session recall trail with surfaced memories + scores; memories deleted after recall are skipped rather than leaking stale rows. Schemas added under `backend/api/schemas.py` (`IncidentMemoryResponse`, `IncidentMemoryListResponse`, `IncidentMemoryCreate`, `IncidentMemoryUpdate`, `IncidentMemoryFeedbackRequest`, `IncidentMemoryHideRequest`, `SessionMemoriesUsedItem`, `SessionMemoriesUsedResponse`); routers mounted in `backend/api/app.py`. 11 new tests in `tests/test_api.py::TestIncidentMemoryAPI`. Verification: focused class → 11 passed; full suite still green.
- **Sprint 45 Step 9 — operator memory guide + README polish** (Session 116). New `docs/wiki/memory-guide.md` covers the 30-second model, what a memory row looks like, the writeback gate (which session outcomes earn memory), trust boundaries (per-org isolated, advisory only, no direct agent writes, failed sessions don't earn memory, bounded growth, operator-curated), auto-compaction mechanics, the self-improvement loop preview, the v2-deferred surface (service playbooks, operator-preference memory, pgvector, cross-org sharing — explicitly out of v1), and a complete "Where memory lives in the codebase" index. New entry in `docs/wiki/README.md` operator section. `README.md` gains a new "AI incident memory" bullet under "Why OpsMender" and a new "AI incident memory — *carrying lessons forward*" subsection under "How OpsMender thinks" with the harness framing, the recall → remember → compact loop description, and the six trust guarantees. Audit-log bullet updated to note that memory recall + writeback are audited. `docs/TASKS.md` Sprint 45 checklist Steps 1–5 marked complete with attributions; `docs/CURRENT_STATE.md` What's In Progress reflects that the agent-side loop is shipped and only the operator-visible surface (Steps 6–7) remains. Pure docs.
- **Sprint 45 Steps 4 + 5 — `remember` LangGraph node + auto-compaction** (Session 115). New `backend/memory/writeback.py` carries the post-session writeback path. `should_remember(state)` gates the LLM call on workflow status + tool-call errors + non-trivial summary/diagnosis; failed, timed-out, and rolled-back sessions never produce memories. `MemoryDraft.from_json()` parses the LLM's strict JSON response with code-fence stripping, tag normalisation (lowercase + dedupe + 5-tag cap), and length capping (title ≤ 200 chars, summary ≤ 4000 chars). `remember_for_session(factory, *, llm, org_id, service_id, source_incident_id, state)` runs the gate, calls the LLM once, persists via `IncidentMemoryRepo.create`, and (on success) triggers one bounded auto-compaction pass. `maybe_compact(factory, *, llm, org_id, service_id)` runs only when per-service memory count exceeds 50: layer 1 deletes exact-title duplicates (newer survives, no LLM call); layer 2 asks the LLM for up to 5 `{action: "delete", id, reason}` operations and applies the valid ones transactionally. Compaction is bounded (one pass per `remember` call, MAX_COMPACTION_OPS=5, never recursive), always audit-logged, and best-effort (failures swallowed). New `_build_remember` closure + stub `remember` node in `backend/agent/nodes.py`; `remember` added to `DEFAULT_WORKFLOW_NODE_ORDER` at the end, after `summarize`; `build_graph` accepts `source_incident_id` and registers the closure when memory is enabled. `IncidentState` gains `memorized_id: str`. Session runner passes `incident.id` into `source_incident_id`. 25 new tests in `tests/test_memory_writeback.py` cover `MemoryDraft.from_json` happy + edge cases (6), `should_remember` gate (6), `remember_for_session` (4), the LangGraph node closure (3), and auto-compaction (4 — under-threshold no-op, exact-title dedup keeps newer, LLM compaction applies + ignores junk, op cap enforced) + 2 workflow-order tests. Verification: focused tests → **60 passed**.
- **Sprint 45 Steps 2 + 3 — retrieval module + `recall` LangGraph node** (Session 114). New `backend/memory/retrieval.py` wraps `IncidentMemoryRepo.find_relevant` with the side effects a workflow node needs: writes a row to `incident_memory_recall_log` per surfaced memory, stamps `last_used_at`, and returns a markdown context block + the surfaced memory ids. Public surface: `MemoryRetrievalResult` dataclass, `derive_query()` + `derive_tags()` helpers (strip stop-words + punctuation; lift severity), `format_memories_as_markdown()` (truncates at 1200 chars per memory to keep the prompt bounded), and `recall_for_session(factory, *, org_id, session_id, service_id, incident, limit=5)`. Failures are swallowed — memory must never break a session. New `recall` node in `backend/agent/nodes.py` (stub + `_build_recall(memory_factory, *, org_id, service_id)` closure factory). Node is inserted at position 0 of `DEFAULT_WORKFLOW_NODE_ORDER`; existing workflow profiles saved before Sprint 45 keep working because `recall` is optional, not required. `IncidentState` gains `memory_context: str` + `recalled_memory_ids: list[str]`. `observe` node prepends `memory_context` to its prompt when present so prior lessons inform the first pass. `backend/api/session_runner.py` threads `memory_factory`, `org_id`, and `service_id` into `build_graph` so live REST sessions get memory automatically. 19 new tests in `tests/test_memory_retrieval.py` cover query/tag derivation, markdown formatting, recall happy + empty + failure paths, the closure node, and that the default node order starts with `recall` (while still allowing legacy orders without it). Verification: focused tests → **34 passed**.
- **Sprint 45 Step 1 — AI incident memory schema + repo** (Session 113). New `incident_memories` table (org-scoped, `service_id`/`source_incident_id` FKs with safe ON-DELETE behavior, `title`/`summary_md`/`tags` content, `helpful_count`/`unhelpful_count` feedback counters, `is_hidden` soft-flag, `last_used_at` recall stamp) + `incident_memory_recall_log` table (one row per memory surfacing event, per session). Alembic migration `g7h8i9j0k1l2_add_incident_memories.py` (down-revision `f6b7c8d9e0f1`). `IncidentMemoryRepo` covers create / get_by_id / list_for_org / count_for_service / find_relevant / record_feedback / set_hidden / update / delete / touch_last_used; `IncidentMemoryRecallLogRepo` covers record / list_for_session. Retrieval scoring is pure SQL: `2.0 * service_match + tag_overlap + 0.5 * keyword_match + helpful_ratio_boost`. 15 new repo tests covering org isolation (cross-org reads, writes, feedback, delete all blocked), retrieval scoring (service rank, tag overlap, helpful-ratio tiebreaker, hidden memories never surface), and recall logging. Verification: `uv run python -m pytest tests/test_incident_memory_repo.py -q` → 15 passed. Full suite still green. Memory locked decisions live in D-025 (`docs/REFERENCE.md`).
- **Cross-page polish: shared `<PageHeader>`, `<FilterChips>`, and a redesigned paging-flow diagram** (Session 112). New `frontend/components/ui/PageHeader.tsx` standardizes title sizing, subtitle tone, optional left icon badge, and right-aligned action slot; rolled across Incidents, Paging, Skills, Environment Scans, Activity, and Approvals so the dashboard reads as one product instead of six bespoke headers. New `frontend/components/ui/FilterChips.tsx` provides a typed pill-row primitive sharing the active-accent / ghost styling already used on the Paging tabs; converted the Incidents Status / Severity / Source `<Select>` dropdowns to chip rows and replaced the Approvals status `<Select>` with chips. The "How paging works" modal's dense 8-node SVG was replaced with a 3-stage horizontal flow (Triage → Route → Page) carrying numbered tone-coloured badges, larger type, and a four-card invariants strip — mirrors the modal's opening sentence instead of fighting it. Pure frontend; no API, routing, or backend behavior change. Verification: `cd frontend && npm run build` clean; full suite **1206 passed, 2 skipped**.
- **Paging page visual refresh** (Session 111). `/dashboard/paging` now uses icon + label pill tabs in place of the previous cramped 7-card row. Active tab is filled with the accent color; inactive tabs are ghost-styled with hover affordance. Each tab carries a dedicated lucide icon (Users, Server, Repeat, ListOrdered, GitBranch, Wrench, Bell), and the selected section's description has moved into a clearer subhead in the body so descriptions no longer word-wrap across narrow tab cards. Header copy tightened. No behavior or routing change. Verification: `cd frontend && npm run build` clean; full suite still green.

### Fixed

### Removed

## [1.0.0] — 2026-05-23

First public release. MIT-licensed. All work from Sprints 1–43 ships in this tag.

### Added
- **Sprint 43 P1 #8 — debounced server-side incidents search** (Session 109). `GET /incidents` now accepts `q=...`, and incident listing applies case-insensitive title/description matching in SQL so operators can search large incident sets without pulling everything into browser-only filtering. `/dashboard/incidents` now debounces the search box by 300 ms, sends `status` + `q` together to the API, keeps severity/source as local refinements, updates the placeholder to `Search title or description…`, and shows a small `Searching…` hint while the debounced request catches up. Added API coverage for case-insensitive query matching and composition with the existing status filter. Verification: `npm run build` clean; full suite **1206 passed, 2 skipped**.
- **Sprint 43 P1 #7 — one-click synthetic test incidents** (Session 108). `/dashboard/incidents` now has a **Fire Test Incident** button that opens a small modal with a pre-filled high-severity synthetic payload, optional service binding, and `source: "opsmender-test"`. Submitting the modal uses the normal incident-creation API, immediately starts a session, and shows a success toast with an `Open session` deep-link. The manual incident API now accepts optional `service_id`, `external_id`, and `external_source`, with server-side service validation. Incident/session detail views recognize `opsmender-test` and render the resulting path as a clearly marked `TEST · synthetic alert` session. Verification: `npm run build` clean; full suite **1204 passed, 2 skipped**.
- **Sprint 43 P1 #6 — live connection status indicators on Config** (Session 107). `mcp_servers` now persists `last_successful_call_at` plus the latest `last_error`, backed by Alembic migration `f6b7c8d9e0f1_add_mcp_server_connection_status.py`. Runtime MCP connections in `backend/mcp/pool.py` stamp those fields automatically, and the existing `POST /mcp-servers/{id}/test` path updates them too. New `GET /mcp-servers/status` returns per-server runtime health (`healthy` / `stale` / `error`), which the Config page polls every 30 seconds. New shared `frontend/components/ui/StatusDot.tsx` renders the green/amber/red dots inline on the Models and MCP sections, with hover text showing the exact last-success timestamp and latest error for red states. Verification: `npm run build` clean; full suite **1202 passed, 2 skipped**.
- **Sprint 43 P1 #5 — MCP server templates in the Add Server modal** (Session 106). The Config page's MCP server modal now offers six curated templates above the manual form: Kubernetes (`@anthropic/mcp-server-k8s`), Postgres (`@modelcontextprotocol/server-postgres`), GitHub Copilot MCP, generic HTTP + OAuth, generic HTTP + bearer token, and a custom stdio starter. Selecting a template pre-fills the existing manual form, links out to the relevant docs, and OAuth-shaped templates switch the primary action to **Create & Connect** so the newly created server can drop directly into the Sprint 42 OAuth flow. Pure frontend/docs change; `npm run build` clean and full suite still **1198 passed, 2 skipped**.
- **Sprint 43 P0 #2 — contextual empty states across the dashboard** (Session 105). `frontend/components/ui/EmptyState.tsx` now supports an optional `learnMoreHref` + `learnMoreLabel` deep-link rendered under the primary action. Reliability's remaining hand-rolled empty states were converted to the shared component, and the dashboard's operator-facing list surfaces now point to the right wiki page when empty: incidents + approvals + activity + reliability → Operator Guide, skills → Skills Guide, paging tabs → Paging Guide, and the Environment Scans page links back to the operator docs. Pure frontend/docs change; `npm run build` clean and full backend suite remains green.
- **Sprint 43 P0 #1 — first-run setup checklist** (Session 104). New `GET /config/setup-checklist` endpoint returns five booleans (`model_configured`, `mcp_server_added`, `skill_defined`, `ingest_token_created`, `paging_service_added`) plus a derived `all_complete`. New `<SetupChecklist>` component (`frontend/components/SetupChecklist.tsx`) renders on `/dashboard/incidents`, deep-links each row to the right Config section, hides itself when every step is checked, and supports operator dismissal via `localStorage["opsmender:setup-checklist-dismissed"]`. The endpoint is readable by `admin` / `operator` / `viewer` so viewers can see what's still pending. 4 new tests in `tests/test_api.py::TestSetupChecklist`. Full suite: **1198 passed, 2 skipped** (+4 vs Session 103's 1194). `npm run build` clean.
- **Sprint 43 P0 #3 — `opsmender doctor` command** (Session 103). New CLI subcommand runs every production-readiness check in `backend/doctor.py` and prints a one-line glyph + name + detail per result, exiting non-zero when any check fails. Checks (in order): JWT secret strength (refuses placeholders in prod, warns on short secrets), frontend static mount discoverability, audit log writeability, ingest auto-start config sanity, DB reachability via `SELECT 1`, MCP server connectivity via a transient `connect()`+`list_tools()` probe against every active server in the pool, and a paging-chain age check that warns when any `IncidentChainState` row has been `running` for more than 24h. Pure functions in `backend.doctor`; the CLI just prints. ASCII-safe glyphs (`[ok]` / `[!!]` / `[XX]`) so Windows cp1252 consoles render cleanly. 21 new tests in `tests/test_doctor.py`. Full suite: **1194 passed, 2 skipped** (+21 vs Session 102's 1173).
- **Sprint 43 P0 #4 — default-secret startup guard** (Session 102). New `check_production_safety()` in `backend/config_loader.py` raises `InsecureProductionConfigError` when the API is starting outside of development mode and `OPSMENDER_JWT_SECRET` is still one of the known placeholders (`change-me-in-production` from `.env.example` or `dev-secret-change-in-production` from the `AuthConfig` dataclass default). Activation rule: `OPSMENDER_DEPLOYMENT_MODE` unset OR not equal to `development` (case-insensitive). `scripts/dev_server.py` calls `os.environ.setdefault("OPSMENDER_DEPLOYMENT_MODE", "development")` so the local dev server keeps working unchanged. `create_app()` in `backend/api/app.py` runs the check immediately after loading config — `uvicorn backend.api.app:create_app --factory` now refuses to start with the placeholder secret. 8 new tests in `tests/test_production_safety_guard.py`. Full suite: **1173 passed, 2 skipped** (+8 vs Session 101's 1165).
- **Sprint 42 closes — Steps 10 + 11 audit pass** (Session 101). Step 10 ("sweep tests") and Step 11 ("docs sweep") were both absorbed step-by-step as Sprint 42 progressed; the audit confirms the original targets are met. **108 MCP-OAuth-stack tests** across `tests/test_mcp_oauth_client.py` (30), `tests/test_mcp_oauth_tokens.py` (12), `tests/test_mcp_client_oauth.py` (13), `tests/test_mcp_json.py` (29), `tests/test_cli_mcp.py` (6), `tests/test_mcp_importers.py` (18) — every category from the original Step 10 list is covered. `docs/REFERENCE.md` D-024 (Session 096), the README "Configuration" section's `mcp.json` block (Sessions 098–099), `docs/wiki/skills-guide.md` (Session 096), and the per-step CHANGELOG entries (Sessions 094–101) all carry the changes. **Sprint 42 is now complete** — all 11 steps in across Sessions 092–101.
- **Sprint 42 Step 9 — MCP Config page status pill matrix** (Session 101). The Config page now shows the full four-state pill matrix per HTTP/SSE MCP server: green "OAuth Connected" when a fresh refresh token is on file, amber "Reconnect needed" when refresh failed, a neutral "Bearer" pill for servers using a static bearer token only, and an amber "Not authorized" pill for HTTP servers with neither OAuth nor a bearer token. The green-vs-amber and Connect-vs-Reconnect button states already shipped in Step 5 (Session 097b); Step 9 closes the matrix by surfacing the remaining two states so operators can see at a glance which HTTP servers will actually authenticate. Pure frontend; no schema or API changes. `npm run build` clean.
- **Sprint 42 Step 8 — Claude Code + Codex MCP-server importers** (Session 100). New `backend/mcp/import_from_claude.py` and `backend/mcp/import_from_codex.py` produce a normalized `ImportableServer` dataclass from existing tool configs so an OpsMender operator with a Claude Code or Codex install gets one-click MCP setup. Claude importer handles both shapes: project-scope `./.mcp.json` (`{"mcpServers": {...}}`) and user-scope `~/.claude.json` (`{"projects": {"<path>": {"mcpServers": {...}}}}`); `streamable-http` is normalized to `http`; `Authorization: Bearer X` headers are lifted into the `token` field. Codex importer parses TOML `[mcp_servers.<name>]` tables, captures inline `[mcp_servers.<name>.env]` values, surfaces `bearer_token_env_var` via the `OPSMENDER_BEARER_TOKEN_ENV_VAR` env-var hint (never inlines the actual secret), folds `http_headers` into `env_vars`, and drops entries explicitly disabled via `enabled = false`. Both modules are pure parsing — no DB writes, no env reads. 18 new tests in `tests/test_mcp_importers.py`. Frontend exposure (the first-run setup checklist) lands in Sprint 43; the `opsmender mcp import` CLI flow is also Sprint 43 work since it depends on the same operator-facing surface. Full suite: **1165 passed, 2 skipped** (+18 vs Session 099's 1147).
- **Sprint 42 Step 7 — `opsmender mcp export` / `opsmender mcp reload` CLI** (Session 099). New `opsmender mcp` subcommand exposes the file mirror to operators outside the running web service. `opsmender mcp export [--path P] [--org-id UUID]` writes the current DB state for an org to `mcp.json`. `opsmender mcp reload [--path P] [--org-id UUID] [--apply] [--prune]` reads the file, computes a create/update/db-only/delete plan, and either prints it (dry-run, default) or commits it (`--apply`); `--prune` additionally deletes DB servers that aren't in the file. Org selection falls through `--org-id` → `OPSMENDER_MCP_JSON_ORG_ID` env → the only org when exactly one exists; ambiguous multi-org calls exit 2 with a list of known orgs. CLI invocations bypass the `OPSMENDER_MCP_JSON_SYNC` opt-in (the operator just typed the command — consent enough). `MCPJSONSyncer.reconcile_on_startup` gained `prune: bool` and `dry_run: bool` parameters used by the new CLI; `ReconcileResult.deleted` tracks pruned names. 8 new tests (`tests/test_mcp_json.py::TestReconcilePrune` + `TestReconcileDryRun` + `tests/test_cli_mcp.py` covering arg parsing, export round-trip, dry-run vs apply, bad-uuid exit code). Full suite: **1147 passed, 2 skipped** (+8 vs Session 098's 1139). **Sprint 42 Steps 8–11 remain** (Claude/Codex importers · frontend modal updates · sweep test pass · final docs sweep).
- **Sprint 42 Step 6 — `mcp.json` file mirror** (Session 098). New `backend/mcp/mcp_json.py` provides two-way sync between the org's `mcp_servers` rows and a Claude-Code-compatible JSON file (default `~/.opsmender/mcp.json`, overridable via `OPSMENDER_MCP_CONFIG_PATH`). The file shape is byte-for-byte compatible with Claude Code's project-scope `.mcp.json` — same `{ "mcpServers": { "<name>": { "type": "stdio"|"http"|"sse", "command"/"args"/"env" or "url"/"headers", "opsmender": { "is_active": ... } } } }` — so a file checked into a repo works in both tools. OAuth tokens are NEVER written to disk (they live encrypted in `mcp_server_oauth_tokens`); the static bearer `token` column is also omitted from exports and preserved across file→DB reconciles when the file omits it. Sync is opt-in via `OPSMENDER_MCP_JSON_SYNC=true` (conservative default for multi-tenant deployments) with an optional `OPSMENDER_MCP_JSON_ORG_ID` pin. The MCP server CRUD routes now call `MCPJSONSyncer.export_org` after every commit so the file stays in lock-step with UI mutations (DB→file). The app lifespan runs `reconcile_on_startup` for every known org on boot so file edits made while the service was down are applied (file→DB, additive — DB-only servers are reported but never deleted; destructive sync is reserved for the upcoming `opsmender mcp reload --prune` CLI in Step 7). 36 new tests in `tests/test_mcp_json.py` cover env-var config resolution, serialization in both directions, file IO + malformed-JSON rejection, syncer export (including pinned-org gating), and reconcile semantics (create-from-file, update-with-file-wins, DB-only preservation, token preservation when file omits, missing-file no-op, malformed-file error capture). Full suite: **1139 passed, 2 skipped** (+36 vs Session 097b's 1103); `npm run build` → clean. **Step 7 (`opsmender mcp reload` / `opsmender mcp export` CLI) is next.**
- **Sprint 42 Step 5 — MCP client OAuth token refresh + reconnect UX** (Session 097b). Alembic migration `f1a2b3c4d5e6` adds `client_id` + `client_secret_encrypted` to `mcp_server_oauth_tokens` so the refresh path can reconstruct `ClientRegistration` without re-running DCR. `resolve_oauth_access_token()` in `backend/mcp/client.py` checks expiry (T-300 s margin), refreshes via `refresh_access_token`, rotates via `MCPServerOAuthTokenRepo.rotate`, deletes stale rows on `invalid_grant`, and raises `MCPAuthorizationRequiredError` on all unrecoverable paths. `MCPServerPool._resolve_server_config_with_oauth` injects a fresh bearer token before every MCP connection — the error propagates so the session stream / WebSocket can surface a Reconnect prompt. Callback route updated to persist `client_id`/`client_secret`. `MCPServerResponse.oauth_status` field added (`"connected"` / `"reconnect_needed"` / `null`) via a single `map_by_server_id` bulk query; Config page shows a green/amber status pill and a context-aware Connect/Reconnect button. 13 new tests in `tests/test_mcp_client_oauth.py`. Full suite: **1103 passed, 2 skipped** (+16). `npm run build` → clean.
- **Reliability HTTP checker expected-status matching** (Session 097). The existing non-AI SLA poller now supports healthy HTTP responses beyond a single `expected_status` integer: `expected_statuses` can be a list or comma-separated string with exact codes (`200`, `404`), status classes (`2xx`), and ranges (`200-299`, `500-599`). API create/update validates malformed status config, the Reliability target modal accepts status lists/ranges, and `.env.example` documents `OPSMENDER_SLA_POLLER_ENABLED` / `OPSMENDER_SLA_POLL_INTERVAL_DEFAULT`. Focused verification: `uv run python -m pytest tests/test_sla_poller.py tests/test_sla_api.py -q` → **31 passed**; `npm run build` → clean.
- **Sprint 42 Step 4 — MCP OAuth admin routes + Config-page Connect action** (Session 096). New `GET /mcp-servers/oauth/start?id=<server_id>` admin route runs RFC 9728 PRM discovery, RFC 8414/OIDC authorization-server metadata fetch, PKCE S256 generation, Dynamic Client Registration when no per-server pre-registered client id is supplied, signed-state minting, and authorize-URL assembly. New public `GET /mcp-servers/oauth/callback` validates the signed state, enforces RFC 9207 `iss` matching, exchanges the code, persists encrypted tokens via `MCPServerOAuthTokenRepo.upsert`, and redirects to `/dashboard/config?mcp_oauth=ok|error&detail=…`. The Config page now shows a Connect action for URL-based MCP servers and surfaces callback success/failure banners. State JWTs now carry the short-lived client registration id/secret needed for the callback exchange. 4 new API tests cover authorize URL creation, stdio rejection, encrypted token persistence, and issuer-mismatch rejection. Verification: focused MCP API/OAuth/token tests → **53 passed**; full suite `uv run python -m pytest -q` → **1087 passed, 2 skipped**; `npm run build` → clean. `npm run lint` remains blocked by unrelated pre-existing errors in `frontend/app/dashboard/organizations/page.tsx`, `frontend/app/dashboard/paging/page.tsx`, `frontend/components/reliability/SLATargetModal.tsx`, and `frontend/context/branding.tsx`.
- **Sprint 42 Step 3 — OAuth 2.1 + PKCE client for HTTP-transport MCP servers** (Session 095). New `backend/mcp/oauth.py` (~580 LOC) implements every binding MUST from the Step 0 spec lock. Key surfaces: `discover_protected_resource_metadata` (RFC 9728 PRM discovery via `WWW-Authenticate` header parsing + path-suffixed-then-root `.well-known/oauth-protected-resource` fallback); `fetch_authz_server_metadata` (RFC 8414 + OIDC Discovery with strict `issuer` matching per §3.3, refuses to proceed with `MCPPKCENotSupportedError` when `code_challenge_methods_supported` lacks `S256` per spec §6.1.4); `register_client_dynamically` (RFC 7591 DCR with clean fallback signal for pre-registered credentials); `generate_pkce_pair` (S256 per RFC 7636, 64-byte verifier yielding 86 URL-safe chars); `build_authorize_url` (every required OAuth 2.1 param plus `resource` per RFC 8707); `verify_redirect_issuer` (RFC 9207 §2.4 — raises `MCPIssuerMismatchError` on missing or mismatched `iss`); `exchange_code` + `refresh_access_token` returning a typed `TokenResponse` dataclass; refresh raises `MCPAuthorizationRequiredError` on `invalid_grant` / `expired_token` so the UI can surface a Reconnect button; `sign_state` / `verify_state` for the 10-minute state JWT carrying server_id + recorded issuer + code_verifier + resource + org_id; `canonical_resource_uri` for RFC 8707 §6.2 canonicalization. `http_client_factory` injection point lets tests swap in `httpx.MockTransport`. `SPEC_VERSION = "draft/2026-05-19"` pinned to the Step 0 snapshot. 30 new tests in `tests/test_mcp_oauth_client.py` cover every public surface (PKCE generator · state JWT round-trip / tampering / expiry · WWW-Authenticate parsing · PRM discovery 4 paths · authz metadata 4 paths · DCR success + fallback · authorize URL · RFC 9207 match/mismatch/missing · code exchange success / resource param / invalid_grant · refresh 4 cases · canonical URI 2 cases) — zero real network calls. Full suite: **1083 passed, 2 skipped** (+30 vs Session 094's 1053; zero regressions). Step 4 (admin routes + frontend "Connect" button) is next.
- **Sprint 42 Step 2 — `mcp_server_oauth_tokens` schema + repo + migration** (Session 094). First code-shipping step of Sprint 42 lands the persistence layer for OAuth 2.1 token bundles obtained from HTTP-transport MCP servers. New `MCPServerOAuthToken` SQLAlchemy model with one row per `(org_id, mcp_server_id)` (UNIQUE on `mcp_server_id`, both FKs CASCADE on delete), encrypted `access_token_encrypted` + nullable `refresh_token_encrypted` columns, plus `token_type` / `scopes` JSON / `expires_at` / `issuer` (for RFC 9207 validation) / `obtained_at` / `last_refreshed_at`. New `MCPServerOAuthTokenRepo` with `upsert` / `rotate` / `get_for_server` / `read_plaintext` / `list_expiring_before` / `delete_for_server` — the repo owns the Fernet encrypt/decrypt boundary via `backend/auth/secrets.py` (same key derivation as SSO/SAML). `rotate` honors OAuth 2.1 §4.3.1: when the AS omits `refresh_token` from a refresh response, the prior token is kept (still valid until the AS chooses to rotate). Alembic migration `e9a1b2c3d4e5_add_mcp_server_oauth_tokens.py` (`down_revision=d8e9f0a1b2c3`) creates the table + the `(org_id, expires_at)` auto-refresh sweep index. 12 new tests in `tests/test_mcp_oauth_tokens.py` cover encryption round-trip (asserts plaintext NEVER appears in the column), upsert replacement, the "no refresh token issued" path, rotation keeping prior refresh when AS omits new one, the auto-refresh sweep filter, and FK cascade (with `PRAGMA foreign_keys = ON` enabled in the fixture so SQLite enforces it). Full suite: **1053 passed, 2 skipped** (+12 vs Session 091's 1041; zero regressions). **Step 3 (the OAuth client module) is next** — PRM discovery + RFC 8707 + RFC 9207 + PKCE S256 + DCR + refresh rotation, ~600-800 LOC.
- **Sprint 42 Step 0 — MCP authorization spec-lock pass** (Session 093). Live research against the [MCP authorization draft spec](https://modelcontextprotocol.io/specification/draft/basic/authorization), [Claude Code MCP docs](https://code.claude.com/docs/en/mcp), and [Codex MCP + config-reference docs](https://developers.openai.com/codex/mcp) surfaced **five real corrections** to the original Sprint 42 plan and added five new locked decisions. The corrected Sprint 42 plan is now embedded in `docs/TASKS.md` under a `Step 0 — Spec-lock pass` subsection covering: (a) every binding MUST from the MCP authz spec (RFC 9728 PRM + RFC 8414 authz-server metadata + RFC 8707 Resource Indicators + RFC 9207 issuer validation + PKCE S256 + refresh-token rotation per OAuth 2.1 §4.3.1), (b) verified Claude Code config schema (`~/.claude.json` + `.mcp.json` + per-server `type`/`command`/`args`/`env`/`url`/`headers`), (c) verified Codex config schema (`~/.codex/config.toml` + `[mcp_servers.<name>]` + `bearer_token_env_var` + OAuth knobs), (d) the locked OpsMender `mcp.json` shape — a superset of Claude Code's `.mcp.json` JSON format so import-from-Claude is byte-for-byte and the same file works in both tools, with an optional `opsmender` extension block per server for tier/skill overrides. **OAuth tokens are NOT stored in `mcp.json`** — encrypted DB storage only. Sprint 42 Step 3 (OAuth client module) estimate grew from ~300 LOC to ~600-800 LOC because of the spec MUSTs that were missed in original scoping; other 10 steps unchanged. Step 1 (`mcp_server_oauth_tokens` Alembic migration) is next.
- **`docs/V2_ROADMAP.md` + Sprint 42/43/44 plans captured in TASKS.md** (Session 092). Forward-looking planning artifacts for OpsMender after v1.0.0 tags. `docs/V2_ROADMAP.md` documents the six v2 themes (distributed deployment mode · audit log retention · free-tier compliance posture · UX/onboarding deepening · auditor + connector breadth · SAML hardening) with locked exclusions (no SaaS, no native mobile, no multi-region, no paid certifications, no OpsMender-as-MCP-server) and a sequencing recommendation. `docs/TASKS.md` gains three new sprint sections: **Sprint 42** (MCP OAuth 2.1 + `mcp.json` persistence — 11 steps, Step 0 is an explicit research/spec-lock pass against Claude Code + Codex), **Sprint 43** (v1.0.0 UX refinements — first-run setup checklist + empty states everywhere + `opsmender doctor` + default-secret guard + MCP server templates + connection status indicators + "Fire test incident" button + incidents search), **Sprint 44** (v1.0.0 release-tag cutover — CHANGELOG split + tag push + release workflow verification). Cleaned up the stale "Next Planned Sprints" list (was listing completed Sprints 35-40 as planned). No code changes; pure planning sweep ahead of the v1.0.0 cutover.
- **Sprint 41 step 4 — OCI Container Instances Terraform recipe (Sprint 41 closes)** (Session 091). New `deploy/cloud/oci-container-instances/` ships the fourth and final cloud recipe layered on top of `ghcr.io/shipitpirate/opsmender-ai`. Six `.tf` files (`versions.tf`, `variables.tf`, `network.tf`, `main.tf`, `outputs.tf`, `terraform.tfvars.example`) plus a step-by-step README covering prereqs, quick start, verification (`oci container-instances container-instance get`, log-group wiring, `curl /health`), NLB cutover for production scale-out, image rolls, private OCIR pulls, and tear-down. The module provisions an `oci_container_instances_container_instance` (flexible shape, default `CI.Standard.E4.Flex` with 1 OCPU / 8 GB, restart policy `ALWAYS`, HTTP `/health` health check) + an `oci_core_network_security_group` attached to the instance's VNIC (8000 ingress from operator CIDRs, all egress) + an `oci_logging_log_group`. Compartment, VCN, subnet, Vault, Vault secrets, and Postgres are operator-supplied inputs. Secrets resolved at apply time via `data "oci_secrets_secretbundle"` (base64-decoded) and injected as plain environment variables on the container — **secret material lands in Terraform state**; the README warns operators to use an encrypted remote backend (OCI Object Storage with KMS, or Terraform Cloud) and notes Instance Principals + runtime Vault fetch as a future tightening. IP outputs use a `data "oci_core_vnic"` lookup keyed by `vnics[0].vnic_id` since the OCI provider does not expose IPs as resource attributes. NLB fronting is documented in the README but kept out of the baseline. `terraform fmt -recursive` clean; `terraform validate` Success on Terraform 1.15.3 + OCI provider 6.x. **Sprint 41 is now complete** — all four multi-cloud recipes (AWS ECS, Azure Container Apps, GCP Cloud Run, OCI Container Instances) are in. Full Python suite stays at **1041 passed, 2 skipped**, zero regressions.
- **Sprint 41 step 3 — GCP Cloud Run service YAML recipe** (Session 090). New `deploy/cloud/gcp-cloud-run/` ships the third cloud-specific recipe layered on top of `ghcr.io/shipitpirate/opsmender-ai`. Files: `service.yaml` (Cloud Run `serving.knative.dev/v1` Service) and `README.md` (API enablement, service account, IAM, secrets, deploy, verification, custom domain, image roll, Artifact Registry mirroring, scaling, tear-down). The manifest runs the standard image on port 8000 with a dedicated service identity, Cloud SQL connector annotation, Secret Manager env refs for `OPSMENDER_DATABASE_URL` / `OPSMENDER_JWT_SECRET` / provider keys, Cloud Run HTTPS ingress, min/max scaling defaults, and `/health` startup/liveness/readiness probes. YAML parse check clean; live `gcloud run services replace` remains operator-driven because `gcloud` and a target project were not available in the local environment. Sprint 41 step 4 (OCI Container Instances) remains.
- **Sprint 41 step 2 — Azure Container Apps Bicep recipe** (Session 089). New `deploy/cloud/azure-containerapps/` ships the second cloud-specific recipe layered on top of `ghcr.io/shipitpirate/opsmender-ai`. Three files: `main.bicep` (single-file template, ~230 lines), `main.bicepparam` (example parameters with inline `az keyvault` setup commands), `README.md` (prereqs + quick start + verification + custom-domain cutover + tear-down + architecture diagram). Provisions a Log Analytics workspace + user-assigned managed identity (granted **Key Vault Secrets User** on the operator's vault) + Container Apps managed environment + the Container App itself with external ingress on port 8000, HTTP-concurrency autoscaling (1–3 replicas default), and Liveness + Readiness probes against `/health`. Secrets resolved from Key Vault via the managed identity at replica start; `containerImage` defaults to public GHCR (no ACR pull setup), with optional `acrServer` for private registries. Unlike the ECS recipe, ACA terminates TLS automatically on the default `*.azurecontainerapps.io` FQDN — custom-domain binding is covered as a follow-on `az containerapp hostname bind` recipe in the README. One real Bicep BCP182 caught + fixed inline (`keyVault.properties.vaultUri` → `'https://${keyVaultName}${az.environment().suffixes.keyvaultDns}/'`, which works on public + Gov + China clouds). `bicep build` clean; `bicep lint` clean. Sprint 41 steps 3–4 (GCP Cloud Run, OCI Container Instances) remain.
- **Top-level `.gitignore` extended for Bicep** (Session 089). `deploy/cloud/**/main.json` (compiled ARM artifact) and `deploy/cloud/**/*.bicepparam.local` (operator-edited parameters with secret references) now ignored.
- **Sprint 41 step 1 — AWS ECS (Fargate) Terraform recipe** (Session 088). New `deploy/cloud/aws-ecs/` module ships the first cloud-specific deployment recipe layered on top of the canonical `ghcr.io/shipitpirate/opsmender-ai` image. Eight files (`versions.tf`, `variables.tf`, `iam.tf`, `network.tf`, `alb.tf`, `main.tf`, `outputs.tf`, `terraform.tfvars.example`) plus a step-by-step `README.md` walkthrough covering prereqs, `terraform init/plan/apply`, post-deploy verification (`aws ecs wait`, `aws logs tail`, target-group health), DNS cutover, image roll, and tear-down. The module creates an ECS Fargate cluster + task definition + service + Application Load Balancer (HTTPS listener when `acm_certificate_arn` is supplied, HTTP-only otherwise) + target group health-checking `/health` + two minimal security groups + execution + task IAM roles + CloudWatch log group. Secrets (`OPSMENDER_JWT_SECRET`, `OPSMENDER_DATABASE_URL`, one-or-more LLM provider keys) are pulled from Secrets Manager; VPC, subnets, Postgres, Route 53 records, and ACM certs are operator-supplied inputs (never module-managed). Per locked decision **D-023**, the OpsMender framework still ships zero knowledge of which platform it's running on — this is an operator-facing recipe. `terraform fmt` clean; `terraform validate` Success on Terraform 1.15.3 + AWS provider 5.50+. Sprint 41 steps 2–4 (Azure Container Apps, GCP Cloud Run, OCI Container Instances) tracked as follow-on sub-sprints.
- **Top-level `.gitignore` extended for Terraform** (Session 088). `**/.terraform/`, `**/.terraform.lock.hcl`, `**/*.tfstate*`, `**/*.tfplan`, `**/terraform.tfvars`, `**/*.auto.tfvars` are now ignored across the repo; `!**/terraform.tfvars.example` is the explicit allow-back so example files still ship.
- **Helm chart verification + stale image name fix** (Session 087). `helm lint deploy/helm/opsmender` → 1 chart, 0 failures on Helm 4.2.0. `helm template opsmender deploy/helm/opsmender` → 13 Kubernetes manifests rendered cleanly (app Deployment/Service/ServiceAccount/Secret/ConfigMap/PVC + Bitnami Postgres StatefulSet/Services/ServiceAccount/Secret/NetworkPolicies). Helm 4 wart documented in `deploy/helm/opsmender/README.md`: `helm dependency build` no longer auto-extracts the subchart `.tgz`, so the README install recipe now includes an explicit `tar -xzf charts/postgresql-*.tgz` step. Stale image repository name fixed: `deploy/helm/opsmender/values.yaml`, `deploy/helm/opsmender/README.md`, and `.github/workflows/release.yml`'s OCI label all moved from `ai-incident-manager` → `opsmender-ai` (matches what the release workflow actually publishes via `ghcr.io/${GITHUB_REPOSITORY,,}`). README "Supported distribution paths" table is now a five-row table (Docker compose / external Postgres / SQLite dev / standalone binary / Helm).
- **Sprint 41 stub — multi-cloud deployment recipes** (Session 087, planned, post-v1.0.0). Tracked in `docs/TASKS.md` under Next Planned Sprints. Scope per cloud: AWS ECS (Fargate task + service + ALB + Secrets Manager), Azure Container Apps (Bicep + KeyVault + ACR pull identity), GCP Cloud Run (service.yaml + Cloud SQL + Secret Manager), OCI Container Instances (Terraform + Vault + NLB). Each cloud is its own 1–3 day sub-sprint; the full sweep is multi-session. Per D-023 these are operator-facing recipes, not framework code — the OpsMender backend still ships zero knowledge of which platform it's running on. Earmarked `deploy/cloud/` as the directory home.
- **PyInstaller binary build verified end-to-end** (Session 086). `./scripts/build_binary.sh` runs the Next.js static export, installs the PyInstaller build group, and produces `./dist/opsmender` (52 MB on macOS arm64) in ~3 min. Verified: `./dist/opsmender --version` → `1.0.0`; `./dist/opsmender check` → "Config OK"; `OPSMENDER_DATABASE_URL=postgresql+asyncpg://... ./dist/opsmender serve --port 8766` against a fresh Postgres 16 → all 60+ Alembic migrations apply, `/health` → 200, `/auth/register` → 201 Created. The README "Three supported distribution paths" table is now a four-row table — the binary is the fourth path alongside Docker compose, `opsmender serve` + external Postgres, and `scripts/dev_server.py` on SQLite.
- **Postgres port-mapping production note** (Session 086). `docker/docker-compose.yml` now carries an inline comment above the `5432:5432` host port mapping explaining the dev convenience vs production security tradeoff ("comment this out so Postgres is only reachable from inside the compose network"). The README distribution-paths table for the Docker row spells out the same recommendation. No default UX change.

### Verified (no code change)
- **`.env.example` is already free of legacy `AIM_*` env vars** (Session 086). The earlier follow-up was a false positive — `AIM_*` lines lived in my *local* gitignored `.env`, not the example template. Every key in `.env.example` is `OPSMENDER_*` or a standard provider/Postgres/frontend variable.

### Added
- **Distribution verification ahead of v1.0.0** (Session 086). All three documented monolith / Docker paths exercised end-to-end against the live HTTP surface: (1) `docker compose -f docker/docker-compose.yml up -d` — image builds clean (4-stage multi-stage), both containers healthy, all 60+ Alembic migrations apply against fresh Postgres 16, `/health` → 200, `/auth/register` + `/auth/login` + `/auth/me` happy-path returns 200; (2) `OPSMENDER_DATABASE_URL=postgresql+asyncpg://... .venv/bin/opsmender serve` — migrations apply, Uvicorn binds, `/health` → 200; (3) `OPSMENDER_DATABASE_URL=sqlite+aiosqlite:///./opsmender-local.db uv run python scripts/dev_server.py` — schema created via `Base.metadata.create_all`, admin/admin123 + default org seeded, `/health` → 200, login → valid JWT. Confirmed the SQLite + Alembic limitation (first migration references `JSONB`); `opsmender serve` and the PyInstaller binary both require Postgres. README gains a new "Three supported distribution paths" table under Distribution making the split explicit + a warning that `opsmender serve` does not support SQLite. Open follow-ups (not blocking v1.0.0): PyInstaller binary build wasn't exercised this session; `.env.example` still carries legacy `AIM_*` lines alongside the live `OPSMENDER_*` ones; Postgres port `5432:5432` is exposed in compose for dev convenience.

### Removed
- **Stale root `docker-compose.yml`** (Session 086 follow-up). The sibling of the deleted root `Dockerfile` — line 5 declared `dockerfile: Dockerfile`, pointing at the file removed earlier this session, so any `docker compose up` from the repo root would fail with `Dockerfile: not found`. Old "dev sandbox" workflow (mounted the project into a container, dropped into bash) that nothing in the docs referenced. Canonical stack stays at `docker/docker-compose.yml`. Zero downstream impact.
- **Stale root `Dockerfile`** (Session 086). Referenced `test-session.sh` (deleted long ago) and would crash any `docker build .` from the repo root. The canonical production image lives at `docker/Dockerfile` — every README / REFERENCE / TASKS / LOGS reference already points there. Zero downstream impact.

### Added
- **Sprint 40 steps 3–4 — Paging Guide wiki page + README rewrite around the incident-response loop** (Session 085). New `docs/wiki/paging-guide.md` is the top-level operator orientation for OpsMender's paging surface: the five-stage incident-response loop diagram, the conceptual model (services / teams / rosters / escalation chains / priority rules / response modes / maintenance windows / notification preferences / incident assignment), a step-by-step setup walkthrough (team → service → roster → chain → priority rule → notification preferences → ingest token → verify), a one-off maintenance-window recipe, a cross-reference table mapping each concept to its UI tab + API surface + dedicated wiki page, a troubleshooting matrix with the six most common operator-side failure modes, and an end-to-end verification recipe pointing at `tests/test_e2e_paging_flow.py` + `tests/test_on_call_perf.py`. Linked from `docs/wiki/README.md` under the Operator Guide section. README's lead reframed around the same five-stage loop (alert → AI → ack → fix → resolve) with a new ASCII diagram between the hero and "How OpsMender thinks"; the surface-by-surface deep dive now opens by tying each surface to its stage in the loop, Ingest is reframed as "stage 1 of the loop", and a new Paging section ("stage 3 of the loop") was inserted between Ingest and Workflows pointing at the new wiki guide. Day-1 onboarding paragraph updated to recommend wiring one paging service + roster + chain right after Ingest. No code changes. Full suite stays at **1041 passed, 2 skipped**.
- **Sprint 40 step 2 — `on_call_at` performance benchmark** (Session 085). New `tests/test_on_call_perf.py` ships two micro-benchmarks for the deterministic on-call resolver: (a) 1000 in-memory roster contexts × 50 members × 4 timestamps = 4000 invocations under a 500 ms budget, and (b) per-call average under 1 ms for a 50-member roster. Dev-hardware median **~8 ms total / ~2.1 µs per call** — about 60× under budget. No hot spots: the function is pure, the per-call `sorted(members)` on 50 items is negligible, and `ZoneInfo` is internally memoized by the stdlib. Both tests ship as regression guards so a future refactor that accidentally adds DB I/O (or balloons the timezone math) inside `on_call_at` will trip the bound immediately. No production code changes. Full suite: **1041 passed, 2 skipped** (+2 vs the step 1 run's 1039).
- **Sprint 40 step 1 — End-to-end incident-response loop test** (Session 085). New `tests/test_e2e_paging_flow.py::TestIncidentResponseLoop::test_alert_to_page_to_ack_to_takeover_to_resolve` exercises the canonical operator path through the real FastAPI app: an inbound alert hits `/incidents/ingest` with a service-scoped ingest token; the priority rule (`severity: critical → P1 + page`) fires; `select_chain_for_incident` resolves the linked escalation chain; `start_chain` records the page row for the on-call operator; a signed Slack `block_actions` payload (ACTION_ACK) on `/bot/slack/interactions` acks the chain and creates the operator's assignment; an admin's `POST /incidents/{id}/take {force: true}` swaps the assignment over via the web UI; a final ACTION_RESOLVE Slack click cancels the chain and flips `incidents.status` to `resolved`. No engine monkey-patching — the channel factory naturally returns `None` for every channel (no env credentials in tests), so the dispatcher writes the audit-anchor `incident_pages` row and short-circuits delivery, which is exactly the production path on a deployment with no configured channels. Full suite: **1039 passed, 2 skipped** (+4 vs Session 084's 1035).
- **Sprint 39 step 3 — Detector-to-audit-schedule migration** (Session 083). New `backend/auditor/detector_migration.py` plans and applies migrations from legacy `detector_rules` into `audit_schedules`. The CLI command `opsmender detectors-migrate` dry-runs by default and `--apply` persists rows, mapping detector name/server/prompt/interval/active state into scheduled `environment-scan` runs with best-effort focus areas. 11 new tests cover prompt extraction, planning, skips, and collisions.
- **Sprint 39 step 2 — Scheduled audit runs** (Session 082). New `audit_schedules` table (Alembic `d7e8f9a0b1c2`) — org-scoped with `analyzers` / `mcp_server_name` / `focus_areas` / `interval_minutes ≥ 15` / `is_active` / `last_run_at` / `next_run_at` and a `(is_active, next_run_at)` due-index. `AuditScheduleRepo` provides CRUD + `list_due` + `mark_run`. New `backend/auditor/scheduler.py::AuditScheduler` background loop mirrors `EscalationScheduler` — 60-second tick, restart-safe via the watermark column, fans each due schedule through `run_audit` with `mcp_server_name` + `focus_areas` baked into every analyzer's params, then advances the watermark by `interval_minutes`. Wired into the app lifespan next to `EscalationScheduler`. Admin-only `GET/POST/PATCH/DELETE /audits/schedules` endpoints validate every analyzer key against the registry on writes; `interval_minutes` is bounded `[15, 43200]` (15 min to 30 days) by Pydantic. Newly-created schedules get `next_run_at = now()` so the first tick after create fires the run. Cron syntax intentionally deferred — `interval_minutes` keeps the dep surface tight; v1 ships no UI either (admin API only). 5 new tests in `tests/test_audits.py` (`TestAuditScheduleAPI` × 3 + `TestAuditSchedulerTick` × 2). Full suite: **1035 passed, 2 skipped** (+5 vs Session 081's 1030).
- **Sprint 38 — Mobile-optimized incident-response path** (Session 081). The OpsMender web UI is now usable on a phone for the most time-sensitive operations. Three surfaces tuned: `/dashboard/incidents/detail` (heading `text-xl sm:text-3xl`, header padding `px-4 py-4 sm:px-6 sm:py-6`, Quick View sidebar drops `min-w-[240px]` below `lg` so it stacks under the title instead of overflowing, "New Session" CTA shortens to "New" below `sm`), the pending-approval card on `/dashboard/sessions/detail` (Approve / Reject move from a right-hand column to a 2-col grid below the JSON action context on phones — each button gets ~50% viewport width for comfortable thumb tapping), and the escalation chain step editor on `/dashboard/paging` (fixed `grid-cols-[120px_1fr_120px_auto]` becomes `grid-cols-1 sm:grid-cols-[…]` so Type / Target / Timeout each take full width on phones). New `docs/wiki/mobile-incident-response.md` operator guide covers the chat → web UI flow, the three optimized surfaces with the exact Tailwind classes shipped, recommended workflow (Slack/Teams DM + quiet hours), a real-device verification checklist for iOS Safari (390px) + Android Chrome (360px), and an honest list of authoring surfaces that still target tablet+. Frontend build clean; full suite **1030 passed, 2 skipped** (zero regressions — Sprint 38 touched no backend code). Native push remains intentionally out of scope: OpsMender relies on Slack and Teams to deliver the deep link.
- **Sprint 37 step 5 — Wiki page "Teams as your paging surface"** (Session 080). Closes out Sprint 37 with `docs/wiki/teams-paging-surface.md`. Mirrors `slack-paging-surface.md`: what the adaptive card looks like, button + action-data semantics, full Azure setup walkthrough (App registration → client secret → Graph application permissions + admin consent → Bot Framework registration + messaging endpoint → OpsMender connector fields → env-var checklist → linking Teams users), a verification recipe, and a troubleshooting table covering the six most common Teams-side failures. Linked from `docs/wiki/README.md` under the Operator Guide section. **Sprint 37 is now complete** — Sprint 38 (mobile-optimized session detail + approval flow) is next.
- **Sprint 37 step 4 — Teams bot-activity endpoint + Bot Framework JWT verification** (Session 080). Closes the loop on Sprint 37's outbound side. New `POST /bot/teams/activity` (`backend/api/routes/teams_paging.py`) is the inbound counterpart to the adaptive cards Sprint 37 step 3 ships. Pipeline: parse the Activity JSON → match the bot via `activity.recipient.id` (shape `28:{bot_app_id}`) against `bot_connectors.credentials.bot_app_id` → verify the Bot Framework JWT in `Authorization: Bearer …` → resolve the clicker via `BotUserLinkRepo.get_by_platform_user(connector_id, activity.from.aadObjectId)` → route by `value.action` to the same `handle_ack` / `handle_takeover_request` / `cancel_chain` handlers Slack uses. Unknown Teams users get the same friendly "isn't linked" reply as Slack. New `backend/auth/bot_framework.py::verify_bot_framework_token` lazily fetches the JWKS from `https://login.botframework.com/v1/.well-known/openidconfiguration` (24h cache), decodes the token with `python-jose`, and validates `aud` against the bot app id + `iss` against `https://api.botframework.com` + the standard expiry/nbf claims. Same `http_client_factory` injection pattern as the Graph OAuth helper. Route registered in `backend/api/app.py` next to `slack_paging_router`. 9 new tests in `tests/test_teams_paging.py` — tests generate a real RSA keypair, sign JWTs with `jose_jwt.encode`, and serve the matching JWKS through an `httpx.MockTransport` so the verification path runs end-to-end with no real network. Full suite: **1030 passed, 2 skipped** (+9 vs the step 3 run's 1021).
- **Sprint 37 step 3 — Adaptive card rendering for Teams page cards** (Session 080). New `backend/paging/teams_cards.py` mirrors the Sprint 36 `slack_cards.py` shape. `build_page_card_adaptive(incident, base_url=...)` returns an `AdaptiveCard` v1.4 with a bold TextBlock header, a FactSet (Priority / Status / Severity / Incident id), an optional subtle description snippet (≤300 chars, first line only), three `Action.Submit` buttons carrying `data.action` ∈ {`opsmender:ack`, `opsmender:take`, `opsmender:resolve`} + `data.incident_id`, and an optional `Action.OpenUrl` deep-link to `/dashboard/incidents/detail?id=…&from=teams` when `base_url` is set (so the Sprint 36 step 7 `?from=` breadcrumb works for Teams too). `wrap_card_as_attachment` produces the Graph attachment shape; `build_graph_chat_message` returns the complete `chats/{id}/messages` payload with the attachment referenced via `<attachment id="…"></attachment>` in the HTML body. `parse_incident_id_from_action` accepts both raw and `{"value": {...}}`-wrapped Submit payloads for the Sprint 37 step 4 inbound endpoint. `TeamsGraphDMChannel.send` now embeds adaptive cards when the dispatcher passes them via `blocks`; the dispatcher builds and embeds the card whenever it fans out to `teams_dm_graph` (`CHANNEL_KEYS` now includes the new key). 13 new tests in `tests/test_teams_cards.py`. Full suite: **1021 passed, 2 skipped** (+13 vs the step 2 run's 1008).
- **Sprint 37 step 2 — Outbound Teams DM via Microsoft Graph `chats/{id}/messages`** (Session 080). New `TeamsGraphDMChannel` (channel key `teams_dm_graph`) in `backend/paging/channels.py` runs alongside the legacy MessageCard `TeamsDMChannel` so existing webhook deployments keep working. Constructor takes `tenant_id` / `client_id` / `client_secret`; `send(recipient, subject, body)` acquires a token through `acquire_app_only_token` (cache is shared with the connector test-connection path), posts an HTML body to `https://graph.microsoft.com/v1.0/chats/{recipient}/messages`, and maps Graph error codes / OAuth errors into the standard `DeliveryAttempt(failed, …)` shape. Recipient must be a Teams chat id the OpsMender service principal is a member of. `build_channel_factory` resolves the new key when `OPSMENDER_TEAMS_GRAPH_TENANT_ID`, `OPSMENDER_TEAMS_GRAPH_CLIENT_ID`, and `OPSMENDER_TEAMS_GRAPH_CLIENT_SECRET` are all set; missing any short-circuits to `None`. 6 new tests in `tests/test_teams_graph_channel.py`. Full suite: **1008 passed, 2 skipped** (+6 vs the step 1 run's 1002).
- **Sprint 37 step 1 — Microsoft Graph app-only OAuth + Teams bot connector adapter** (Session 080). Kicks off Sprint 37 (Teams chat-surface parity). New `backend/auth/graph_oauth.py` implements the **client-credentials** flow against `https://login.microsoftonline.com/{tenant_id}/oauth2/v2.0/token` with `scope=https://graph.microsoft.com/.default`. `acquire_app_only_token` caches the result in-process keyed by `(tenant_id, client_id)` and evicts entries 60 seconds before expiry; `force_refresh=True` bypasses the cache; every transport / Azure error is wrapped in `GraphOAuthError` so callers never see raw httpx exceptions. `verify_graph_credentials` then hits `GET /v1.0/organization` so the connector "Test connection" button confirms both the credentials AND the Graph permissions are wired up. New `TeamsAdapter` registers under platform `teams` with a typed form schema (tenant_id / client_id / client_secret required; optional bot_app_id + default_chat_id) — outbound delivery via Graph `chats/{id}/messages` lands in step 2 and the inbound activity endpoint in step 4, but the adapter exposes itself in the registry today so admins can paste credentials and verify them via the existing test endpoint. 10 new tests in `tests/test_graph_oauth.py`. Full suite: **1002 passed, 2 skipped** (+10 vs Session 079's 992).
- **Sprint 36 step 9 — Wiki page "Slack as your paging surface"** (Session 079). New `docs/wiki/slack-paging-surface.md` is an operator-facing walkthrough of the full Slack paging surface: Block Kit page card and buttons, slash commands with examples, the opt-in per-incident channel mirror, Slack app setup with required scopes, a verification recipe (POST a synthetic incident → click through → confirm via `/status` → `/resolve`), and a troubleshooting table covering the five most common Slack-side failure modes (unlinked Slack accounts, signature mismatches, unknown slash commands, missing channels scope for the mirror, snoozed-but-not-resuming chains). Linked from `docs/wiki/README.md` under the Operator Guide section. **Sprint 36 is now complete** — Sprint 37 (Teams chat-surface parity) is the next sprint.
- **Sprint 36 step 5 — Per-incident Slack channel mirror** (Session 079). Operators can opt their org into automatic Slack channel creation for paged incidents. Migration `c6d7e8f9a0b1` adds `organizations.slack_incident_channels_enabled` (default false) and `incidents.slack_channel_id` / `slack_channel_name`. New `backend/paging/slack_channel_mirror.py::mirror_incident_to_slack_channel` builds a Slack-safe channel name (`inc-<first8hex>` via a slug-safe regex), calls `conversations.create`, persists the resulting channel id on the incident, and posts the Block Kit page card via `chat.postMessage`. The helper is idempotent — once `slack_channel_id` is populated subsequent calls short-circuit — and non-fatal: a Slack error (e.g. `name_taken`, `missing_scope`) is logged and the chain still runs. Bot token from `OPSMENDER_SLACK_BOT_TOKEN`. REST `/incidents` and inbound-ingest kickoff sites both invoke the mirror right after `start_chain` when the response mode is `page` / `escalate_immediate`. Admin `PUT /organizations/{id}/notification-settings` now accepts the new flag (and turns both settings into optional partial-update fields). 7 new tests in `tests/test_slack_channel_mirror.py`. Full suite: **992 passed, 2 skipped** (+7 vs the slash-command run's 985).
- **Sprint 36 step 7 — Frontend `?from=slack` deep-link breadcrumb** (Session 079). `/dashboard/incidents/detail` reads the `from` query parameter and renders an accent-tinted info banner ("You opened this incident from Slack. Anything you do here syncs back to the chat surface.") above the back link when the value is `slack`, `teams`, or `discord`. The Block Kit page card already emits `?id={id}&from=slack`, so the end-to-end loop is closed: a paged operator clicks "View in OpsMender", lands on the incident detail, and sees a contextual breadcrumb that names the source surface. Build clean.
- **Sprint 36 step 3 — Slack slash command endpoint** (Session 079). `POST /bot/slack/commands` lands next to the interactivity endpoint, sharing signature verification and connector resolution. Slack apps point each slash command's Request URL at this route. Supported verbs: `/ack`, `/take`, `/release`, `/resolve`, `/snooze`, `/status`. Pipeline: verify the v0 HMAC against every enabled Slack connector's `signing_secret` (5-min replay window), parse the form-encoded `command` / `text` / `user_id`, reject unknown commands with an ephemeral list of supported verbs, resolve the runner via `BotUserLinkRepo` (unlinked users get the same friendly ephemeral as the buttons), extract an incident UUID from `text`, and — when none is supplied — fall back to the user's most-recently-paged incident with a running/paused/acked chain. Per-verb routing: `/ack` → `handle_ack(via="slash_command")`; `/take` → `handle_takeover_request` with branch-aware reply (`assigned` / `pending` / `noop` / `requires_admin`); `/release` → `IncidentAssignmentRepo.release`; `/resolve` → `cancel_chain` + `update_status('resolved')`; `/snooze <duration>` parses `30m` / `2h` / `1d`, pushes `next_step_due_at` forward, flips the chain to `paused`; `/status` with an id prints chain state, step index, next-due-at, and assignee; without an id, lists the first 10 active chains for the org. 10 new tests in `TestSlackSlashCommandEndpoint`. Full suite: **985 passed, 2 skipped** (+10 vs Session 078's 975).
- **Sprint 36 step 1 — Slack page card + interactivity endpoint** (Session 078). First slice of the structured-paging-UX sprint. New `backend/paging/slack_cards.py` builds Block Kit JSON for an actionable page card: priority emoji + status badge + truncated description in the section header, an Acknowledge / Take Over / Resolve action row, and (when `OPSMENDER_PUBLIC_URL` is set) a "View in OpsMender" button that deep-links to `/dashboard/incidents/detail?id={id}&from=slack`. Action ids use a stable `opsmender:{ack,take,resolve,view}` shape; the incident UUID rides in `actions[0].value` and the `block_id` for redundancy. New `backend/api/routes/slack_paging.py` exposes `POST /bot/slack/interactions`: verifies every request with the Slack signing secret stored on the matching `bot_connectors` row (replay window 5 min), parses the form-encoded `payload`, resolves the Slack user via `BotUserLinkRepo.get_by_platform_user`, and routes block_actions to `handle_ack` / `handle_takeover_request` / `cancel_chain` + `IncidentRepo.update_status('resolved')`. Unlinked Slack users get a friendly ephemeral "your Slack account isn't linked" message instead of silent failure. `Channel.send` gained an optional `blocks` kwarg (other channels accept and ignore it); `SlackDMChannel` posts the Block Kit card alongside the fallback `text`, and `dispatch_page` builds the card whenever it fans out to `slack_dm`. 9 new tests in `tests/test_slack_paging.py` (`TestPageCardBuilder` × 4 + `TestSlackInteractionsEndpoint` × 5). Full suite: **975 passed, 2 skipped** (+9 vs Session 077's 966).
- **Sprint 35 engine → dispatcher wiring** (Session 077). The escalation chain engine now auto-invokes `dispatch_page` for every step it fires, so a paged user actually gets a Slack DM / Teams message / Email / SMS without any extra wiring at the call site. New `backend/paging/channel_factory.py` reads global channel credentials from env (`OPSMENDER_SLACK_BOT_TOKEN`, `OPSMENDER_TEAMS_WEBHOOK_URL`, `OPSMENDER_SMTP_HOST` + SMTP family, `OPSMENDER_TWILIO_ACCOUNT_SID` + Twilio family) and returns a `ChannelFactory`; channels with unset credentials yield `None` and the dispatcher records a `skipped` row with reason `channel_unconfigured`. `_fire_step` / `start_chain` / `tick` / `tick_all_due` all accept an optional `channel_factory` (default `None` keeps Sprint 34 record-only behavior for tests). Both incident-creation kickoff sites (REST route + ingest service) and the `EscalationScheduler` background loop now pass `build_channel_factory()` so production paging fans out end-to-end. 5 new tests in `tests/test_escalation.py` cover the wiring: `TestEngineDispatchWiring` (factory present → `recorded` audit anchor **and** `slack_dm`/`sent` rows + a real `MockTransport` chat.postMessage call; factory absent → only the audit anchor) and `TestChannelFactoryBuilder` (unconfigured env → all-`None`, partial Twilio → no SMS, complete Twilio → `SMSChannel`). Full suite: **966 passed, 2 skipped** (+5 vs Session 076's 961).
- **Sprint 35 notification API + frontend** (Session 076). Steps 7–11 close out the operator-facing surface for Sprint 35. New routes: `GET/PUT /users/me/notification-preferences` (auto-creates an empty row on first GET); `GET/PUT /organizations/{id}/notification-settings` (admin-only — `notification_dedup_window_minutes`, 0–1440). The `/maintenance-windows` schema gains `description`, `scope_type`, `scope_id`; `target_ids` defaults to empty so paging-style windows no longer need the SLA-era stub. `/incidents/{id}/paging` now joins through `suppressed_by_maintenance_window_id` and returns a `SuppressedByMaintenanceWindow` block when the incident was suppressed. Frontend adds two new tabs to `/dashboard/paging`: **Maintenance Windows** (Active/Scheduled/Past sub-tabs with counts, From/To range filter, scope-aware create modal for global/service/roster/team) and **My Notifications** (per-channel toggle + destination inputs for Slack DM / Teams DM / Email / SMS, P0–P3 × channel routing matrix that disables until the channel is enabled, quiet-hours panel with start/end, time zone, and `min_priority_to_break`). The incident detail page now fires `getIncidentPaging` alongside `getIncident` and renders an amber "Paging suppressed by maintenance window" banner above the title when the window is set. End-to-end `TestDispatchEndToEnd::test_slack_dm_via_mock_transport` ties `dispatch_page` to a real `SlackDMChannel` via `httpx.MockTransport`. Full suite: **961 passed, 2 skipped** (+14 vs Session 075's 947).
- **Sprint 35 dispatcher + channels** (Session 075). `backend/paging/dispatch.py` ships a single entry point `dispatch_page(...)` that runs the full notification pipeline: maintenance-window suppression (page → suppressed; `escalate_immediate` never downgraded; stamps `incidents.suppressed_by_maintenance_window_id`) → per-user prefs lookup → quiet-hours block with `min_priority_to_break` override → per-channel dedup (within `organizations.notification_dedup_window_minutes`) → channel `send()` → one `incident_pages` row per attempt with `delivery_status` + error reason. `backend/paging/channels.py` ships the four v1 transports: `SlackDMChannel` (chat.postMessage), `TeamsDMChannel` (MessageCard webhook), `EmailChannel` (stdlib smtplib via `asyncio.to_thread`), `SMSChannel` (Twilio Messages API). All HTTP channels accept a `http_client_factory` so tests inject `httpx.MockTransport`; `EmailChannel` accepts a `smtp_factory` for stub SMTP servers. New `IncidentPageRepo.has_recent_delivery` query backs the dedup check. 25 new tests in `tests/test_paging_dispatch.py`. Full suite: **947 passed, 2 skipped**.
- **Sprint 35 schema foundation** (Session 074). Paging notification plumbing now has the persistence layer it needs: `organizations.notification_dedup_window_minutes` (default 10), scoped maintenance windows (`scope_type` / `scope_id` on the existing SLA `maintenance_windows` table), `incidents.suppressed_by_maintenance_window_id`, and new `user_notification_prefs` rows keyed by user + org with channels, priority routing, and quiet-hours JSON. Added repository helpers and tests for scoped window lookup, notification preference upsert, and org dedup settings. Added `tzdata` as a runtime dependency so IANA time zones work consistently on Windows.
- **Platform-agnostic Environment Scans** (post-Sprint-34, Session 073). Major principle clarification + UX simplification: the framework now ships **zero** hard-coded knowledge of any specific runtime platform (Kubernetes, ECS, Cloud Run, Azure Container Apps, OCI Container Instances, Nomad, systemd, …). New locked decision **D-023** in `docs/REFERENCE.md`. Concrete changes:
  - The three-analyzer registry collapsed to one: `kube-score` and `istioctl-analyze` adapters moved to `backend/auditor/example_analyzers.py` (reference only, no auto-register). The default `EnvironmentScanAnalyzer` (renamed from `GenericLLMAnalyzer`, key `environment-scan`) is the only built-in. Operators who want deterministic per-platform output register example adapters explicitly at startup.
  - **SKILL.md gains an optional `focus_areas:` list.** Free-form, platform-neutral phrases ("crashlooping containers", "tasks stuck in PROVISIONING", "high systemd restart counts") that the LLM weights its scan toward. `SkillDefinition.focus_areas` exposed via `SkillResponse.focus_areas`. Comma-separated strings accepted too.
  - **Environment Scans UI redesign.** The analyzer chip selector is gone. After picking an MCP server, the user sees focus-area toggle chips drawn from that server's SKILL.md (or "No focus areas configured. The LLM will pick what to scan." with a pointer to add them). One "Run scan" button.
- **Simplification pass** (post-Sprint-34, Session 072). Six UX gaps addressed before pushing further into the roadmap:
  - **Renamed routes + nav.** `/dashboard/audits` → `/dashboard/scans` (label: **Environment Scans**) and `/dashboard/audit` → `/dashboard/activity` (label: **Activity**) so the AI-action history and the read-only environment scans stop sharing the word "audit". Keyboard shortcut `Alt+L` now opens Activity.
  - **Detectors marked "Legacy"** in the sidebar with a colored pill — Sprint 39 deletes the surface; new users should invest in Environment Scans instead.
  - **MCP server picker on Environment Scans.** Every scan now requires picking an MCP server up front, and that server's name is auto-passed as `mcp_server_name` to every selected analyzer. The earlier failure mode (analyzer returns "Analyzer requires `mcp_server_name` in params") is no longer reachable through the UI. Helper copy makes the read-only / skill enforcement guarantee explicit.
  - **Paging "How it works" diagram.** New info button on the Paging page opens an inline SVG flow showing the full pipeline (inbound alert → priority rules → response mode → service → escalation chain → step targets → roster on-call resolution → ack / takeover / hard timeout). Replaces having to read `docs/paging-model.md` for first-pass understanding.
  - **Per-service ingest tokens.** `ingest_tokens` gains a nullable `service_id` (Alembic `a4b5c6d7e8f9`). When a token is bound to a service, every incident it creates gets `service_id` pre-filled, which lets the Sprint 34 escalation engine pick the owning team's chain without the alert payload having to encode the service. `IngestTokenCreate` accepts the new field.
  - **Approvals page → recovery surface.** Every row now has an explicit "Open session →" link so a user who closed the chat tab can come back to the Approvals page and jump straight to the conversation. The session detail page already had inline Approve / Reject cards from Sprint 9, so the Approvals page becomes the catch-up inbox rather than a mandatory hop; empty-state copy now says so.
- **Escalation chains + ack lifecycle** (Sprint 34). New tables: `escalation_chains`, `escalation_steps`, `service_escalation_chains`, `incident_pages`, `incident_chain_states`. New chain execution engine `backend/paging/escalation.py` with `start_chain` / `tick` / `handle_ack` / `handle_takeover_request` / `handle_takeover_confirm` / `handle_force_takeover` / `cancel_chain` / `tick_all_due`. Additive paging per D-021 #5 — once paged, stay paged. Soft-takeover with 5-minute window, hard inactivity timeout at 15 minutes, admin force-takeover (audit-logged via `assigned_by="admin_force"`). New `EscalationScheduler` runs every 10s during app lifespan and advances chains whose timers have expired — idempotent / restart-safe via the `status` column. New routes: `GET/POST /escalation-chains`, `PUT/DELETE /escalation-chains/{id}`, `GET/POST /escalation-chains/{id}/steps`, `DELETE /escalation-chains/{id}/steps/{step_id}`, `POST/DELETE /services/{id}/escalation-chains[/{chain_id}]`, `GET /incidents/{id}/chain`, `POST /incidents/{id}/ack`, `POST /incidents/{id}/take` (request / confirm / force). Chain auto-kicks off at incident creation when `response_mode` is `page` or `escalate_immediate` (REST + ingest paths). New Escalation Chains tab on `/dashboard/paging` with inline step editor. Notification channel delivery (Slack/Teams/Email/SMS) remains in Sprint 35; v1 chain runs are testable via the `incident_pages` audit log alone.
- **Paging foundation** (Sprint 33). First slice of OpsMender-owned paging (D-021). New tables: `teams`, `team_members`, `services`, `rosters`, `roster_members`, `roster_overrides`, `service_rosters`, `priority_rules`, `priority_llm_override_log`, `incident_assignments`. New incident columns: `priority` (P0–P3), `response_mode` (`auto_resolve`/`notify`/`page`/`escalate_immediate`), `service_id`. Pure-function modules `backend/paging/on_call.py` (`on_call_at` — deterministic rotation math + override + handoff-time boundary + IANA tz) and `backend/paging/priority.py` (`assign_priority` — first-match-wins rules + one-way LLM escalation). New routes: `/teams`, `/teams/{id}/members`, `/services`, `/rosters`, `/rosters/{id}/members`, `/rosters/{id}/overrides`, `/rosters/{id}/on-call`, `/priority-rules`, `/incidents/{id}/paging`, `/incidents/{id}/assign`, `/incidents/{id}/release`. Priority + response mode are computed at incident creation time (both REST and inbound ingest paths) and locked thereafter. Incident assignment grants incident-scoped operator authority per D-021 #9 — an assignee can release without being a global admin/operator. New `/dashboard/paging` page (tabbed: Teams / Services / Rosters / Priority Rules). Escalation chains, maintenance windows, notification preferences, and channel fan-out are deferred to Sprints 34–35.
- **Auditor v1** (Sprint 32). New `/audits/*` surface separates *latent* findings from *paging* incidents. `POST /audits/runs` kicks off a scan across one or more analyzers; findings land in `audit_findings` (not `incidents`) and stay quiet until a human triages them. Three built-in analyzers ship: `kube-score`, `istioctl-analyze`, and `generic-llm-analyzer` (LLM-driven fallback for any read-only MCP server). Each finding carries a "Fix with AI" action that spawns a tier-gated session with the finding text as the goal prompt. New tables: `audit_runs`, `audit_findings`. New routes: `GET /audits/analyzers`, `POST/GET /audits/runs`, `GET /audits/runs/{id}`, `GET /audits/findings`, `POST /audits/findings/{id}/{remediate,dismiss}`. New page: `/dashboard/audits`.
- **Bot Connector UX overhaul** (Sprint 31). The Bot Connectors modal now renders a typed, per-platform form instead of two raw JSON boxes. Every adapter publishes a `FieldSpec` schema (label, kind, required, helper text, doc URL) through `GET /bot-connectors/platforms[/{platform}/schema]`. Secret fields use password masks with show/hide toggles; required fields gate the Save button; an inline "Test connection" button calls the existing `POST /bot-connectors/{id}/test` and surfaces pass/fail in the modal. All 15 registered adapters carry typed schemas. Platforms without a schema fall back to the legacy JSON UI.
- **Slack / Discord OAuth install** (Sprint 31 Step 5–6). When `OPSMENDER_SLACK_OAUTH_CLIENT_ID` / `OPSMENDER_SLACK_OAUTH_CLIENT_SECRET` (or Discord equivalents) are set in env, the modal shows a "Connect to Slack/Discord" button that walks the operator through the provider's consent screen and writes the bot token back automatically. New routes: `GET /bot-connectors/oauth/{platform}/start` (returns `{authorize_url}`) and `GET /bot-connectors/oauth/{platform}/callback`. State is a signed JWT (5-min TTL) carrying `connector_id` so callbacks can't be cross-pollinated. OAuth populates the bot token only — signing secrets remain a manual paste because providers don't return them through OAuth.
- **Per-tenant SAML 2.0 SSO** (Sprint 30). New `org_saml_configs` table next to `org_sso_configs`. Admin CRUD under `/organizations/{id}/saml`. SP-initiated flow under `/auth/saml/{slug}/login` + `/acs` + `/metadata`. IdP described by metadata URL (auto-fetched + cached) or raw XML paste. Global SP keypair via `OPSMENDER_SAML_SP_CERT` / `OPSMENDER_SAML_SP_KEY` (use `opsmender saml gen-sp-keys` to generate). JIT user provisioning, `allowed_email_domains` allowlist, signed-AuthnRequest. SLO and encrypted assertions are out of scope for this drop. Docker image installs `libxmlsec1` / `libxml2` runtime libs; PyInstaller binary fails-loud if those libs are missing on the host.
- **Helm chart** for Kubernetes deployments at `deploy/helm/opsmender/`. App Deployment/Service/Ingress, optional bundled Bitnami Postgres subchart or external DB, persistent `/app/logs` PVC, ConfigMap + Secret env wiring, liveness/readiness probes, optional HPA.
- TopBar: active org name now visible at all breakpoints with a `host-pinned` qualifier in the user menu when Domain Isolation is in effect.
- TopBar: per-org role (admin / operator / viewer) now shown as a colour-coded pill next to the active org name in the org switcher, host-pinned badge, and user dropdown panel. Source is the per-org role from `listMyOrganizations`, since role can differ per tenant.

### Fixed
- Dev seed (`scripts/dev_server.py`) now idempotently creates the default "Main" organization, sets `users.primary_org_id`, and inserts the `user_organizations` link row for the seeded admin. Older local DBs that pre-date multi-tenancy get backfilled on every startup.
- Static SPA catch-all in `backend/api/static.py` now accepts both `GET` and `HEAD`. Previously HEAD requests from health checkers, link previewers, and prefetchers got a default 405 from FastAPI.
- Static SPA catch-all now rewrites Next.js 16 RSC prefetch URLs (`/<route>/__next.<a>.<b>.<c>.txt`) to their nested on-disk form (`/<route>/__next.<a>/<b>/<c>.txt`). Without this, client-side route prefetches 404'd on the FastAPI-served production image.

### Removed
- **Sprint 39 step 4 — Legacy Detector surface.** Removed `backend/detector/`, `/detectors` API routes and schemas, Detector ORM models/repos/tests, `/dashboard/detectors`, Detector sidebar/config links, and Detector env/Helm settings. Alembic `d8e9f0a1b2c3` drops `detector_history` and `detector_rules`; run `opsmender detectors-migrate --apply` before applying that migration on older deployments that still have rules.

## [Archived Draft Release Notes] — 2026-04-22

Archived draft release notes from the earlier Sprints 1–23 milestone planning. Kept for historical context; the actual v1.0.0 release is the 2026-05-20 section above.

### Added

#### Core framework (Phase 1)
- Tiered access control (Tier 0–3) enforced programmatically at every tool call.
- MCP-first infrastructure integration — three transports (`stdio`, `sse`, `http`) with optional bearer-token auth.
- Skill definition system — org-owned `SKILL.md` classifies each operation as `safe`, `caution`, or `destructive`. Cannot be overridden by the agent.
- LangGraph incident-response workflow: `observe → diagnose → plan → tier_gate → execute → verify → summarize`.
- `opsmender` CLI — `run`, `check`, `config`, `audit`, `approvals`, `serve`.
- JSONL audit logger capturing every node transition and every tool call.

#### API + persistence (Phase 2)
- FastAPI REST + WebSocket API, JWT auth, three roles (`admin`, `operator`, `viewer`).
- PostgreSQL persistence via SQLAlchemy + Alembic (SQLite fallback for local dev).
- Tier 1 human-in-the-loop approval flow — workflow pauses, UI/CLI approve/reject, configurable timeout, WebSocket push.
- BYOM provider abstraction — Anthropic, OpenAI, Azure OpenAI, Ollama (local).
- Next.js 16 dashboard — incidents, live session view, audit log, approvals, config, co-pilot chat.
- Co-pilot chat — real-time parallel LLM channel that doesn't block the workflow.
- Single-container deployment — `opsmender serve` runs the FastAPI API plus the embedded static frontend. Docker image bundles Node.js so MCP servers shipping as `npx` packages work out of the box.
- PyInstaller binary build (`scripts/build_binary.sh`).

#### Ingestion + detection (Sprints 14–15)
- Inbound webhook ingestion — CloudWatch/SNS, Azure Monitor, GCP Monitoring, OCI, generic JSON.
- Universal adapter (`auto` provider) — heuristics + LLM-fallback shape learning so new tools work without a bespoke adapter. Per-token shape cache.
- External fingerprint dedup — repeated alerts update the existing incident.
- Rate limiting per ingest token. Raw-payload audit log for replay.
- MCP-driven incident detector — scheduled rules ask the agent to inspect an MCP server and auto-file an incident when it spots a problem. Read-only enforced by locked skill profile. Cost guardrails per rule + global.

#### Safety controls (Sprint 17)
- Tier 0 sandbox — spawn-time MCP tool allowlist, rollback-safe-ops-only runtime guard.
- Tier 0 hard time limits — per-session and per-node wall clocks.
- Automatic compensating-inverse rollback on Tier 0 failure or timeout. Admin "Rollback session" UI + API.
- `reversible` + `compensating_inverse` skill metadata enforced at the tier gate.

#### Outbound integrations (Sprints 18–21)
- Outbound webhook triggers for session lifecycle (`session.created`, `session.awaiting_approval`, `session.active`, `session.completed`, `session.failed`, `session.timed_out`).
- First-class payload formats: `generic`, `slack`, `teams`, `sumo`.

#### Workflow + agent customization (Sprints 22–23)
- Custom workflow builder — saved workflow profiles with ordered node lists. Tier gate always immediately precedes execute. Profiles picked at session start.
- Multi-agent support — saved agent team profiles run specialist-role passes plus a final synthesis pass. Single-path execution; no arbitrary parallel branches.

#### Model onboarding + bootstrap (Sprint 24)
- First-run model setup no longer depends on pre-seeded `.env` values.
- Dashboard and `opsmender config model bootstrap` CLI path for entering provider, model ID, API-key env-var reference, temperature, max tokens, base URL, and API version.
- Manual model IDs accepted when discovery is stale or unavailable — warnings returned, not hard failures.
- `GET /models/bootstrap` API exposes first-run state for the dashboard.

### Security

- Bearer-token auth for inbound ingest endpoints (JWT reserved for dashboard / API users).
- Webhook trigger stored headers/tokens preserved on edit unless the admin explicitly clears them.
- Env-var-reference-only secret model for model configs — API keys never persisted in the DB.

### Docs

- `docs/REFERENCE.md` — architecture, data model, tier semantics, rollback semantics, ingest provider recipes (CloudWatch, Azure Monitor, GCP Monitoring, OCI, generic + universal adapter), detector model, webhook event payloads.
- `docs/TASKS.md`, `docs/CURRENT_STATE.md`, `docs/LOGS.md`, `docs/PROMPT_CONTEXT.md` — development workflow.

### Infrastructure

- Docker Compose for the full stack (`docker/docker-compose.yml`).
- Single multi-stage production Dockerfile (`docker/Dockerfile`) with health checks and a logs volume mount.
- Node.js LTS bundled inside the Docker image for `npx`-based MCP servers. `OPSMENDER_NODE_PATH` override supported for binary installs.

[Unreleased]: https://github.com/SpicyDaemon/OpsMender-AI/compare/v1.0.0...HEAD
[1.0.0]: https://github.com/SpicyDaemon/OpsMender-AI/releases/tag/v1.0.0
